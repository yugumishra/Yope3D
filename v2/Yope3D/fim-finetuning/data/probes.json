{
 "n_prefix": 120,
 "n_suffix": 10,
 "heldout": [
  "attach_script_demo.py",
  "vehicle_demo.py",
  "physics_gallery.py",
  "sandbox_gallery.py"
 ],
 "probes": [
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0108.py:35:12:scene_payload",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0108.py",
   "line": 35,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth108Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.95},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(0.42, 0.64, 2.62)\n        cmp.swing_limit = 0.88\n        cmp.local_twist_axis_b = yope3d.Vec3(0.6, 0.44, 0.25)\n        cmp.local_anchor_b = yope3d.Vec3(3.66, 1.59, 3.24)\n        world.remove_joint_between(entity, entity)\n        yope3d.set_velocity(entity, yope3d.Vec3(0.1, 0.71, 0.65))\n        v = yope3d.save_path(\"textures/tnail.png\")\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.depth = 2\n        e = world.add_ui_curved_background(yope3d.Vec2(3.52, 4.54), yope3d.Vec2(2.69, 1.98), yope3d.Vec4(0.95, 0, 3.14, 2.53))\n\n    def init(self, world, entity, params):\n        world.make_kinematic(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_E):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.35, 1.3, 1.06)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.47, 1.27, 0.72)\n        for ent, script, uitext in yope3d.view(\"ScriptComponent\", \"UITexturedBackground\"):\n            script.params_blob = \"crate\"\n        v = yope3d.",
   "suffix": "        h = world.get_contact_count()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        body = world.add_ui_curved_background(yope3d.Vec2(3.43, 1.85), yope3d.Vec2(0.33, 4), yope3d.Vec4(1.53, 3.1, 2.23, 0.93), 0.35, 4)\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"textures/tnail.png\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.99)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.58, 2.78, yope3d.EASE_QUAD_IN_OUT)\n",
   "target": "scene_payload",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:28:35:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 28,
   "col": 35,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.",
   "suffix": "    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "camera",
   "rest": ".get_forward()",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0047.py:47:34:BUS_SFX",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0047.py",
   "line": 47,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth047Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.21},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.max_x = 3.36\n        m.max_y = 0.37\n        v = yope3d.reg_add(entity, 1.56)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.53)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.95, 0.67, yope3d.EASE_QUAD_OUT)\n        n = yope3d.Vec3(2.47, 0, 0.57).normalize()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 20.62)\n        c = yope3d.reg_add(entity, \"Material\")\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.albedo_map = \"textures/crate_albedo.png\"\n        c.albedo = (4.36, 2.36, 0.81, 4.49)\n        c.normal_map = \"textures/crate_normal.png\"\n        c.metal_rough_map = \"textures/crate_mr.png\"\n        h = world.get_registry()\n        h = world.get_registry()\n        form = yope3d.reg_get(entity, \"SpringConstraint\")\n        if form is not None:\n            form.rest_length = 0.98\n        c = yope3d.reg_add(entity, \"UIText\")\n        c.a = 2\n        c.b = 0.4\n        c.r = 4.23\n\n    def update(self, world, entity, dt):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        yope3d.audio.resume_all()\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 4.48\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        h = world.add_vehicle(entity, [])\n        world.attach_obb_collider(entity, 4.52, yope3d.Vec3(0.91, 0.54, 0.62))\n        for ent, uitext, aabbfo in yope3d.view(\"UIText\", \"AABBForm\"):\n            uitext.font = \"fonts/inter_ascii.msdf\"\n\n    def init(self, world, entity, params):\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.pitch = 1.24\n        for ent, sphere, cylind in yope3d.view(\"SphereForm\", \"CylinderForm\"):\n            sphere.radius = 0.78\n",
   "target": "BUS_SFX",
   "rest": ", 0.29)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:131:28:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 131,
   "col": 28,
   "prefix": "DRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.",
   "suffix": "                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n",
   "target": "Vec3",
   "rest": "(0.0, 0.72, -0.25),",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:332:41:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 332,
   "col": 41,
   "prefix": "\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        rb = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(-20.0))\n        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_rest(self, world):\n        self.plane_floor(world)\n        self.obb(world, H, 1.0, (0, 0.5, 0), OBB_A)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n",
   "target": "Vec3",
   "rest": "(0, 1, 0), math.radians(20.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0097.py:18:14:play_sound",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0097.py",
   "line": 18,
   "col": 14,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth097Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.21},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        rot = yope3d.Quat.from_euler(0.19, 0.32)\n        for ent, uitext in yope3d.view(\"UITexturedBackground\"):\n            uitext.tint_b = 0.03\n        world.add_spring_with_proxies(entity, entity, 5, 0.1, 0, 0.74)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.06\n        src = yope3d.",
   "suffix": "        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 1.43, yope3d.EASE_CUBIC_IN_OUT)\n        rot = yope3d.Quat.from_euler(-1.03, -0.32)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_twist_axis_a = yope3d.Vec3(0.45, 0.15, 0.46)\n        t.target = entity\n        t.local_anchor_a = yope3d.Vec3(4.97, 4.03, 0.09)\n        t.local_anchor_b = yope3d.Vec3(2.91, 0.68, 2.73)\n",
   "target": "play_sound",
   "rest": "(\"audios/impact.ogg\", None, 0.44)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0070.py:43:39:KEY_SLASH",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0070.py",
   "line": 43,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth070Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.85},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"Material\")\n        if hull is not None:\n            hull.metallic = 0.54\n        world.add_spring_with_proxies(entity, entity, 1.85, 0.84, 4, 0.53)\n        for ent, uiback, uitext in yope3d.view(\"UIBackground\", \"UITexturedBackground\"):\n            uiback.a = 4.19\n        cmp = yope3d.reg_add(entity, \"Hull\")\n        cmp.omega = yope3d.Vec3(2.58, 2.88, 3.47)\n        cmp.linear_damping = 0.13\n        form = yope3d.reg_get(entity, \"Name\")\n        if form is not None:\n            form.value = \"ground\"\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 1.4\n        hull.half_height = 1\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(2.31, 2.02, 0.47)\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.pitch = 0.87\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.97\n        world.attach_sphere_collider(entity, 86.71, 1.36)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(3.35, 0, 1.3) - origin).normalize())\n        c = yope3d.reg_get(entity, \"OBBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.18, 0.85, 0.32)\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.5)\n        cmp = yope3d.reg_add(entity, \"LightSource\")\n        cmp.position = yope3d.Vec3(0.34, 0.71, 0.79)\n        cmp.direction = yope3d.Vec3(0.11, 0.31, 0.23)\n        cmp.type = 0.96\n        cmp.constant = 0.65\n        hull = yope3d.reg_add(entity, \"LightSource\")\n",
   "target": "KEY_SLASH",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:326:42:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 326,
   "col": 42,
   "prefix": "        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(20.0))\n        rb = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(-20.0))\n        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n",
   "target": "Vec3",
   "rest": "(0, 0, 1), math.radians(15.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:154:27:KEY_W",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 154,
   "col": 27,
   "prefix": "1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.",
   "suffix": "        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n",
   "target": "KEY_W",
   "rest": "): throttle += 1.0",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0061.py:50:12:is_fixed",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0061.py",
   "line": 50,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth061Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.85},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_get(entity, \"CapsuleForm\")\n        if m is not None:\n            m.half_height = 0.48\n        world.remove_light(entity)\n        res = world.get_contact_point_count()\n        for ent, uitext, sphere in yope3d.view(\"UIText\", \"SphereForm\"):\n            uitext.display_px = 4\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n        hull.r = 4.48\n        hull.font = \"fonts/inter_ascii.msdf\"\n        world.wake(entity)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.87)\n        c = yope3d.reg_get(entity, \"AABBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.53, 0.97, 1)\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(2.74, 4.82, 0.49)\n        cmp.target = entity\n        cmp.local_twist_axis_a = yope3d.Vec3(0.16, 0.89, 0.79)\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.half_height = 0.57\n        hull.radius = 1.26\n        if yope3d.input.is_key_down(yope3d.KEY_APOSTROPHE):\n            world.apply_impulse(entity, yope3d.Vec3(0, 20.76, 0))\n\n    def init(self, world, entity, params):\n        wheel = world.add_kinematic_capsule(0.7, 1.1)\n        for ent, obbfor, uitext in yope3d.view(\"OBBForm\", \"UIText\"):\n            obbfor.extent = yope3d.Vec3(0.18, 0.91, 0.66)\n        hull = yope3d.reg_get(entity, \"SphereForm\")\n        if hull is not None:\n            hull.radius = 1.82\n        prop = world.add_static_aabb(yope3d.Vec3(2.41, 4.73, 2.71), yope3d.Vec3(0.19, 0.32, 0.3))\n        if yope3d.input.is_key_pressed(yope3d.KEY_TAB):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.remove_entity(entity)\n        v = yope3d.",
   "suffix": "        v = yope3d.camera.screen_to_ray(4.3, 2.07)\n\n    def update(self, world, entity, dt):\n        t = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        t.limit_enabled = False\n        t.target = entity\n        t.local_axis_a = yope3d.Vec3(2.41, 4.93, 0.64)\n        t.local_anchor_a = yope3d.Vec3(3.09, 0.62, 3.35)\n        t.local_axis_b = yope3d.Vec3(4.43, 0.42, 4.99)\n        t = yope3d.reg_add(entity, \"Transform\")\n",
   "target": "is_fixed",
   "rest": "(wheel)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0104.py:32:39:KEY_F4",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0104.py",
   "line": 32,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth104Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.08},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        world.tween_ui_opacity(entity, 0.78, 0.41, 4)\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.42, 0.71, 0.02)\n        world.add_spring(entity, entity, 4.93, 0.16)\n        n = yope3d.Vec3(1.5, 0, 2.02).normalize()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.23)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.7, 2.48, yope3d.EASE_QUAD_IN)\n        if yope3d.input.is_key_down(yope3d.KEY_F8):\n            world.apply_impulse(entity, yope3d.Vec3(0, 22.26, 0))\n        yope3d.set_position(entity, yope3d.Vec3(4.49, 4.35, 4.47))\n\n    def on_ui_press(self, world, entity):\n        rot = yope3d.Quat.from_euler(0.38, 1.39)\n        anchor = world.add_cylinder(0.26, 0.21, 76.82)\n        for ent, conetw, capsul in yope3d.view(\"ConeTwistJointConstraint\", \"CapsuleForm\"):\n            conetw.swing_limit = 1.14\n        target = world.add_text_label_3d(\"Paused\", \"Press E to interact\", yope3d.Vec3(2.56, 2.54, 1.78))\n        n = yope3d.Vec3(1.86, 0, 3.12).normalize()\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.path = \"audios/impact.ogg\"\n        t.gain = 0.38\n        t.loop = False\n        t.bus = 0\n        yope3d.settings.set_bool(\"player\", True)\n\n    def update(self, world, entity, dt):\n        v = yope3d.to_degrees(1.36)\n",
   "target": "KEY_F4",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0029.py:58:36:KEY_0",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0029.py",
   "line": 58,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth029Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.98},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"SphereForm\")\n        if form is not None:\n            form.radius = 1.05\n        v = yope3d.input.is_lmb_down()\n        v = yope3d.window.is_cursor_locked()\n        yope3d.settings.set_int(\"ground\", 2)\n        v = yope3d.get_behavior(entity)\n        rot = yope3d.Quat.from_euler(0.5, -0.08)\n        world.attach_cylinder_mesh(entity, 1.27, 1.11)\n        v = yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.43)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.39, 1.01, yope3d.EASE_QUAD_IN)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"hud_root\"\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 3.14\n        c.a = 3.23\n        c.g = 1.17\n        c.r = 1.44\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.98)\n        if yope3d.input.is_key_pressed(yope3d.KEY_SEMICOLON):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 0.34\n\n    def on_ui_press(self, world, entity):\n        if yope3d.input.is_key_pressed(yope3d.KEY_7):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        prop = world.add_kinematic_capsule(1.31, 1.04)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.25)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.32, 2, yope3d.EASE_CUBIC_OUT)\n        body = world.add_ui_textured_background(yope3d.Vec2(1.67, 2.64), yope3d.Vec2(1.4, 3.37), yope3d.Vec4(0.97, 0.19, 0.62, 0.16), \"models/prop.glb\")\n        yope3d.audio.fade_gain(body, 0.1, 2.37)\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.a = 4.71\n        hull.font = \"fonts/inter_ascii.msdf\"\n        hull.b = 3.24\n        hull.g = 3.81\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 7.54, 0))\n        world.remove_spring_between(entity, entity)\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        yope3d.load_scene(\"scenes/level_01.yscene\")\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.path = \"models/prop.glb\"\n\n",
   "target": "KEY_0",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0056.py:17:12:save_path",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0056.py",
   "line": 17,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth056Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.68},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.target = entity\n        t.rest_length = 0.22\n        t.k = 4.1\n        rot = yope3d.Quat.from_euler(-0.9, 0.02)\n        v = yope3d.",
   "suffix": "        for ent, script, conetw in yope3d.view(\"ScriptComponent\", \"ConeTwistJointConstraint\"):\n            script.script_class = \"ground\"\n        m = yope3d.reg_add(entity, \"CylinderForm\")\n        m.radius = 1.39\n        m.half_height = 1.17\n\n    def on_ui_press(self, world, entity):\n        yope3d.set_text(entity, \"Press E to interact\")\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"hud_root\"\n",
   "target": "save_path",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0005.py:13:39:KEY_1",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0005.py",
   "line": 13,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth005Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.42},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_skybox([])\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.reg_remove(entity, \"ConeTwistJointConstraint\")\n        yope3d.clear_collision_observers()\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.anchor = 3\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n        world.attach_box_mesh(entity, yope3d.Vec3(0.93, 3.05, 2.2))\n",
   "target": "KEY_1",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0058.py:39:12:is_fixed",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0058.py",
   "line": 39,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth058Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_wheel_steer(entity, 0.11)\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.b = 3.49\n        hull.r = 1.93\n        hull.a = 3.12\n        hull.g = 2.03\n        form = yope3d.reg_get(entity, \"TextLabel3D\")\n        if form is not None:\n            form.r = 1.17\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.82, 0.25, 0.32)\n        t = yope3d.reg_add(entity, \"PointJointConstraint\")\n        t.local_anchor_b = yope3d.Vec3(4.92, 2.64, 3.04)\n        t.local_anchor_a = yope3d.Vec3(0.2, 4.97, 3.98)\n        t.target = entity\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.script_class = \"ground\"\n        cmp.params_blob = \"crate\"\n        v = yope3d.input.is_key_released(4)\n        v = yope3d.is_sleeping(entity)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        res = world.ui_hovered()\n        r = yope3d.to_radians(151.0)\n        m = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        m.target = entity\n        m.local_twist_axis_a = yope3d.Vec3(0.23, 0.74, 0.48)\n        v = yope3d.",
   "suffix": "        v = yope3d.attach_script(other, \"ground\", \"spawn_point\")\n        res = world.get_contact_point_count()\n        v = yope3d.reg_get(other, 0.54)\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_a = yope3d.Vec3(3.05, 0.27, 3.88)\n        for ent, aabbfo, uiback in yope3d.view(\"AABBForm\", \"UIBackground\"):\n            aabbfo.extent = yope3d.Vec3(0.07, 0.99, 0.33)\n",
   "target": "is_fixed",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:175:13:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 175,
   "col": 13,
   "prefix": "\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.",
   "suffix": "        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n",
   "target": "reg_get",
   "rest": "(self.chassis, \"Transform\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0040.py:57:8:load_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0040.py",
   "line": 57,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth040Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.54},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        hull.metal_rough_map = \"textures/crate_mr.png\"\n        hull.albedo = (0.42, 1.57, 1.88, 4.48)\n        world.add_spring_with_proxies(entity, entity, 2.08, 0.73, 3, 1.11)\n        yope3d.draw_line(yope3d.Vec3(3.65, 2.05, 4.92), yope3d.Vec3(2.88, 2.44, 3.38))\n        yope3d.set_velocity(entity, yope3d.Vec3(0.7, 0.95, 0.41))\n        v = yope3d.input.is_rmb_down()\n        form = yope3d.reg_get(entity, \"OBBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.74, 0.38, 0.83)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.params_blob = \"crate\"\n        c = yope3d.reg_add(entity, \"CapsuleForm\")\n        c.radius = 1.12\n        c.half_height = 0.45\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.79, 2.71, yope3d.EASE_CUBIC_IN_OUT)\n        t = yope3d.reg_add(entity, \"UITexturedBackground\")\n        t.tint_g = 0.22\n        t.has_texture = False\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.intensity = 6.46\n        m.constant = 0.31\n        m.direction = yope3d.Vec3(0.87, 0.34, 0.48)\n        m.position = yope3d.Vec3(0.46, 0.65, 0.72)\n        m.color = yope3d.Vec3(4.53, 3.71, 1.86)\n        c = yope3d.reg_add(entity, \"Parent\")\n        c.parent = entity\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.9)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.37, 1.9, yope3d.EASE_QUAD_IN)\n        world.remove_entity(entity)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.constant = 0.8\n        t.position = yope3d.Vec3(0.83, 0.12, 0.94)\n        t.type = 0.91\n        t.color = yope3d.Vec3(2.37, 0.55, 3.98)\n        t.direction = yope3d.Vec3(0.16, 0.67, 0.87)\n\n    def update(self, world, entity, dt):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.99)\n        yope3d.",
   "suffix": "        wheel = world.add_ui_background(yope3d.Vec2(1.97, 2.3), yope3d.Vec2(1.99, 2.72), yope3d.Vec4(2.29, 2.28, 3.24, 4.34))\n        yope3d.audio.pause_all()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        r = world.add_vehicle(other, [])\n        world.add_cone_twist_joint(entity, entity, yope3d.Vec3(0.89, 4.66, 3.43), yope3d.Vec3(0.34, 0.07, 0.7))\n        yope3d.set_text(other, \"Paused\")\n        if yope3d.input.is_key_down(yope3d.KEY_GRAVE_ACCENT):\n            world.apply_impulse(entity, yope3d.Vec3(0, 22.18, 0))\n        target = world.add_kinematic_sphere(yope3d.Vec3(2.35, 4.74, 1.54), 0.18)\n",
   "target": "load_game",
   "rest": "(\"crate_01\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0005.py:40:8:reg_remove",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0005.py",
   "line": 40,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth005Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.42},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_skybox([])\n        if yope3d.input.is_key_pressed(yope3d.KEY_1):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.reg_remove(entity, \"ConeTwistJointConstraint\")\n        yope3d.clear_collision_observers()\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.anchor = 3\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n        world.attach_box_mesh(entity, yope3d.Vec3(0.93, 3.05, 2.2))\n        h = world.add_vehicle(entity, [])\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 0.26\n        m.half_height = 0.5\n        res = world.get_contact_point_count()\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.33)\n        c = yope3d.reg_get(entity, \"Transform\")\n        if c is not None:\n            c.rotation = yope3d.Quat()\n        world.set_ui_parent(entity, entity)\n        v = yope3d.audio.load_sound(\"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.add_cone_twist_joint(other, other, yope3d.Vec3(1.92, 2.34, 3.3), yope3d.Vec3(0.3, 0.75, 0.47))\n        yope3d.",
   "suffix": "        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.11, 0.24, 0.75)\n        cmp = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        cmp.target = entity\n        cmp.local_anchor_a = yope3d.Vec3(3.45, 1.38, 1.56)\n        cmp.limit_enabled = False\n\n    def update(self, world, entity, dt):\n        v = yope3d.time()\n",
   "target": "reg_remove",
   "rest": "(entity, \"Transform\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:10:31:raycast",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 10,
   "col": 31,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.",
   "suffix": "    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n",
   "target": "raycast",
   "rest": "(o, d, 100.0, entity)",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0031.py:34:12:play_sound",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0031.py",
   "line": 34,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth031Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_sphere_collider(entity, 88.72, 0.66)\n        v = yope3d.get_behavior(entity)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(0.48, 1.66, 0.66)\n        t.rotation = yope3d.Quat()\n        t.position = yope3d.Vec3(0.46, 0.44, 0.61)\n        for ent, sphere, uibutt in yope3d.view(\"SphereForm\", \"UIButton\"):\n            sphere.radius = 0.68\n        world.remove_spring_between(other, entity)\n        cmp = yope3d.reg_add(entity, \"Name\")\n        cmp.value = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.window.get_f11_fullscreen()\n        wheel = world.add_audio_source_entity()\n        world.attach_sphere_mesh(entity, 0.66, 3.83, 2.63, 1.47, 0)\n        for ent, uiback, capsul in yope3d.view(\"UIBackground\", \"CapsuleForm\"):\n            uiback.a = 2.51\n\n    def init(self, world, entity, params):\n        r = world.get_contact_count()\n        if yope3d.input.is_key_pressed(yope3d.KEY_F):\n            yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"AudioSource\")\n        m.gain = 0.51\n        m.path = \"models/prop.glb\"\n        m.autoplay = True\n        m.pitch = 1.11\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.45\n        t.k = 3.1\n",
   "target": "play_sound",
   "rest": "(\"audios/impact.ogg\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0062.py:30:12:get_world_position",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0062.py",
   "line": 30,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth062Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.13},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.8)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.86, yope3d.EASE_LINEAR)\n        anchor = world.add_kinematic_capsule(1.46, 0.71, yope3d.Vec3(0.22, 3.27, 1.25))\n        v = yope3d.to_radians(1.75)\n        if yope3d.input.is_key_pressed(yope3d.KEY_SLASH):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.set_position(entity, yope3d.Vec3(4.59, 0.66, 3.21))\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_b = 0.75\n        c.normal_a = 1.14\n        c.hover_r = 3.93\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.input.get_key_name(1)\n        for ent, textla, conetw in yope3d.view(\"TextLabel3D\", \"ConeTwistJointConstraint\"):\n            textla.text = \"Score: 0\"\n        world.set_ui_parent(entity, entity)\n        v = yope3d.",
   "suffix": "        h = world.get_pair_count()\n        yope3d.window.set_f11_fullscreen(True)\n        hull = yope3d.reg_get(entity, \"Transform\")\n        if hull is not None:\n            hull.rotation = yope3d.Quat.from_euler(0.0, -1.13)\n        m = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if m is not None:\n            m.tint_a = 0.07\n\n    def update(self, world, entity, dt):\n",
   "target": "get_world_position",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:141:8:camera",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 141,
   "col": 8,
   "prefix": "STEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.",
   "suffix": "\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n",
   "target": "camera",
   "rest": ".look_at(spawn_pos)",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:260:31:audio",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 260,
   "col": 31,
   "prefix": "\n    # ---- Spring cloth ----------------------------------------------------\n    # variant:  0 = top row fixed, 1 = 4 corners fixed, 2 = 2 top corners fixed\n    # shape:    0 = sphere, 1 = AABB, 2 = OBB\n    # Variant 1 hangs the sheet horizontally (a trampoline); 0 and 2 hang it\n    # vertically (a curtain).\n\n    def build_cloth(self, world, variant, shape):\n        fh = (GRID_N + 1) * GRID_STEP * 2.0\n        self.static_box(world, (0.0, -5.0, 0.0), (fh, 5.0, fh), FLOOR_COLOR)\n\n        horizontal = variant == 1\n        half_w = (GRID_N - 1) * GRID_STEP * 0.5\n        top_y = 25.0 if horizontal else (GRID_N - 1) * GRID_STEP + 25.0\n\n        grid = [[None] * GRID_N for _ in range(GRID_N)]\n        half = (NODE_HALF, NODE_HALF, NODE_HALF)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N):\n                if horizontal:\n                    pos = (-half_w + i * GRID_STEP, top_y, -half_w + j * GRID_STEP)\n                else:\n                    pos = (-half_w + i * GRID_STEP, top_y - j * GRID_STEP, 0.0)\n\n                fi = i / (GRID_N - 1)\n                fj = j / (GRID_N - 1)\n\n                if shape == 0:\n                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.audio.load_sound(\"audios/test.ogg\")\n        if buf is not None:\n            self.doppler_src = yope3d.",
   "suffix": "            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n        self.self_collide = bool(params.get(\"cloth_self_collision\", False))\n        self.cloth_nodes = []\n",
   "target": "audio",
   "rest": ".create_source(buf)",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:95:12:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 95,
   "col": 12,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.",
   "suffix": "        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n",
   "target": "Vec3",
   "rest": "( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:134:28:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 134,
   "col": 28,
   "prefix": "wheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.",
   "suffix": "                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n",
   "target": "Vec3",
   "rest": "(0.62, 0.30, 0.05),    # windshield panel (front, +Z)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0089.py:25:12:save_path",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0089.py",
   "line": 25,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth089Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.71},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_skybox([])\n        n = yope3d.Vec3(0.76, 0, 4.84).normalize()\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.bus = 3\n        c.path = \"textures/tnail.png\"\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.tint_g = 0.06\n        hull.tint_r = 0.21\n        hull.tint_b = 0.65\n        hull.tint_a = 0.95\n\n    def on_ui_press(self, world, entity):\n        world.detach_physics_body(entity)\n        v = yope3d.",
   "suffix": "        for ent, script, cylind in yope3d.view(\"ScriptComponent\", \"CylinderForm\"):\n            script.script_class = \"player\"\n        h = world.attach_animation(entity, \"textures/tnail.png\")\n        m = yope3d.reg_get(entity, \"Name\")\n        if m is not None:\n            m.value = \"ground\"\n\n    def init(self, world, entity, params):\n        v = yope3d.reg_valid(entity)\n        m = yope3d.reg_get(entity, \"CapsuleForm\")\n",
   "target": "save_path",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0028.py:37:39:KEY_M",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0028.py",
   "line": 37,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth028Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.76},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.half_height = 0.61\n        hull.radius = 0.51\n        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_a = 2\n        hull.hover_r = 4.12\n        hull.hover_g = 4.74\n        hull.normal_b = 1.6\n        prop = world.add_trigger_box(yope3d.Vec3(1.97, 0.37, 4.38), yope3d.Vec3(0.6, 0.43, 0.4))\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.56\n        cmp.tint_r = 0.62\n        cmp.tint_b = 0.73\n        cmp.path = \"audios/impact.ogg\"\n        m = yope3d.reg_get(entity, \"LightSource\")\n        if m is not None:\n            m.color = yope3d.Vec3(2.36, 1.24, 3.89)\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.lower_angle = -0.29\n        if yope3d.input.is_key_down(yope3d.KEY_EQUAL):\n            world.apply_impulse(entity, yope3d.Vec3(0, 9.56, 0))\n\n    def update(self, world, entity, dt):\n        yope3d.set_velocity(entity, yope3d.Vec3(0.88, 0.76, 0.96))\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.86)\n        world.set_ui_group_visible(entity, False)\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_r = 0.22\n        cmp.has_texture = False\n        anchor = world.add_ui_background(yope3d.Vec2(3.81, 3), yope3d.Vec2(3.87, 0.08), yope3d.Vec4(4.33, 2.01, 3.51, 1.33))\n        v = yope3d.settings.save()\n\n",
   "target": "KEY_M",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0049.py:56:39:KEY_TAB",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0049.py",
   "line": 56,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth049Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.73},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.fade_gain(entity, 0.82, 1.59)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.26, 0.64, yope3d.EASE_QUAD_IN_OUT)\n        yope3d.audio.set_bus_gain(1, 0.47)\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 3.1\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.77, 0, 4.51) - origin).normalize())\n        if yope3d.input.is_key_down(yope3d.KEY_F2):\n            world.apply_impulse(entity, yope3d.Vec3(0, 4.76, 0))\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_get(entity, \"SpringConstraint\")\n        if c is not None:\n            c.rest_length = 0.3\n        world.apply_impulse(entity, yope3d.Vec3(29.62, 1.29, 20.19))\n        r = yope3d.to_radians(6.0)\n        rot = yope3d.Quat.from_euler(0.23, -0.88)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(1.25, 0, 3.52)).length()\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        v = yope3d.capsule_cast(yope3d.Vec3(1.11, 3.61, 0.69), 0.87, 0.69, yope3d.Vec3(3.42, 0.19, 0.76), 0.82)\n        other = world.add_static_aabb(yope3d.Vec3(2.23, 1.32, 4.94), yope3d.Vec3(0.31, 0.51, 0.21))\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.loop = 3\n        wheel = world.add_sphere(20.84, 1.87, yope3d.Vec3(0.12, 1.38, 1.48))\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.44, 0, 0.34) - origin).normalize())\n        yope3d.audio.set_master_gain(0.91)\n        rot = yope3d.Quat.from_euler(-0.48, -0.18)\n        v = yope3d.get_world_position(entity)\n\n    def update(self, world, entity, dt):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.35)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.22, 1.35, yope3d.EASE_CUBIC_IN_OUT)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"UITexturedBackground\")\n        m.has_texture = True\n        m.tint_b = 0.13\n        m.tint_a = 0.29\n        m.tint_r = 0.13\n        m.tint_g = 0.02\n        v = yope3d.input.is_key_released(2)\n        world.set_wheel_steer(entity, 1.27)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.48)\n",
   "target": "KEY_TAB",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0103.py:46:39:KEY_BACKSLASH",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0103.py",
   "line": 46,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth103Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.16},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (4.01, 2.41, 2.08, 3.96)\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        hull.albedo_map = \"textures/crate_albedo.png\"\n        hull.normal_map = \"textures/metal_normal.png\"\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (3.81, 4.88, 1.37, 0.46)\n        hull.normal_map = \"textures/crate_normal.png\"\n        c = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(4.75, 4.43, 3.92)\n        c.local_axis_a = yope3d.Vec3(3.92, 2.2, 4.51)\n        for ent, textla, hull in yope3d.view(\"TextLabel3D\", \"Hull\"):\n            textla.text = \"Press E to interact\"\n        m = yope3d.reg_get(entity, \"Transform\")\n        if m is not None:\n            m.scale = yope3d.Vec3(1.78, 1.96, 1.07)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.05, 0, 2.65) - origin).normalize())\n        m = yope3d.reg_add(entity, \"Hull\")\n        m.angular_damping = 0.03\n        m.omega = yope3d.Vec3(3.08, 0.02, 0.41)\n        m.velocity = yope3d.Vec3(0.08, 0.03, 0.31)\n        m.linear_damping = 0.02\n        m.friction = 0.79\n        m = yope3d.reg_add(entity, \"UIText\")\n        m.text = \"Score: 0\"\n        m.b = 2.81\n        c = yope3d.reg_add(entity, \"PointJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(0.21, 2.37, 3.83)\n        c.target = entity\n        c.local_anchor_b = yope3d.Vec3(2.63, 1.54, 2.52)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        hull = yope3d.reg_add(entity, \"OBBForm\")\n        hull.extent = yope3d.Vec3(0.57, 0.7, 0.24)\n        form = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if form is not None:\n            form.swing_limit = 0.94\n        c = yope3d.reg_get(entity, \"CapsuleForm\")\n        if c is not None:\n            c.half_height = 1.18\n\n",
   "target": "KEY_BACKSLASH",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:120:19:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 120,
   "col": 19,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.",
   "suffix": "            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n",
   "target": "Vec3",
   "rest": "(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0060.py:13:39:KEY_8",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0060.py",
   "line": 13,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth060Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_game(\"player\")\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        r = yope3d.to_radians(43.0)\n        for ent, lights, spring in yope3d.view(\"LightSource\", \"SpringConstraint\"):\n            lights.type = 1\n        body = world.add_ui_button(yope3d.Vec2(0.6, 1.55), yope3d.Vec2(0.17, 0.39), yope3d.Vec4(4.22, 0.3, 4.35, 0.53))\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.63)\n        v = yope3d.get_position(entity)\n        cmp = yope3d.reg_add(entity, \"Parent\")\n",
   "target": "KEY_8",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:17:17:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 17,
   "col": 17,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.",
   "suffix": "    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n",
   "target": "Vec3",
   "rest": "(0, math.cos(self.t) * 1.5, 0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0011.py:48:36:KEY_C",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0011.py",
   "line": 48,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth011Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.09},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.add_point_joint(other, other, yope3d.Vec3(4.6, 0.66, 3.68))\n        res = world.get_pair_count()\n        world.finalize_entity(entity, \"spawn_point\")\n        hull = yope3d.reg_get(entity, \"CylinderForm\")\n        if hull is not None:\n            hull.radius = 1.1\n        v = yope3d.play_sound(\"textures/tnail.png\")\n        yope3d.camera.look_at(yope3d.Vec3(0.45, 0.69, 0.85))\n        world.set_mesh_color(other, 2.84, 3.85, 2.33)\n\n    def init(self, world, entity, params):\n        wheel = world.add_obb(yope3d.Vec3(0.65, 0.84, 0.32), 38.76, yope3d.Vec3(4.21, 1.89, 0.9))\n        world.attach_aabb_collider(wheel, 35.23, yope3d.Vec3(0.82, 0.99, 0.39))\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"hud_root\"\n        for ent, animat, uibutt in yope3d.view(\"AnimationPlayer\", \"UIButton\"):\n            animat.playing = 0\n        world.add_cone_twist_joint(entity, wheel, yope3d.Vec3(4.22, 3.15, 4.32), yope3d.Vec3(0.69, 0.76, 0.13), 0.64, 0.32, False)\n\n    def update(self, world, entity, dt):\n        h = world.ui_hovered()\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 20.57)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.96)\n        h = world.ui_hovered()\n\n    def on_ui_press(self, world, entity):\n        world.make_kinematic(entity)\n        r = world.get_ui_focus()\n        m = yope3d.reg_add(entity, \"CylinderForm\")\n        m.half_height = 0.65\n        m.radius = 0.48\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.5, 0.49, 0.63)\n        yope3d.window.set_tab_pauses(False)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 14.11, 0))\n        world.attach_sphere_mesh(entity, 0.57)\n        m = yope3d.reg_add(entity, \"TextLabel3D\")\n        m.r = 2.14\n        m.a = 4.6\n        m.g = 3.99\n        m.font = \"fonts/inter_ascii.msdf\"\n\n",
   "target": "KEY_C",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0077.py:39:52:EASE_QUAD_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0077.py",
   "line": 39,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth077Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.8},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_add(entity, \"Parent\")\n        hull.parent = entity\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.91\n        v = yope3d.window.get_cursor_pos()\n        world.remove_entity(entity)\n        v = yope3d.input.get_key_name(2)\n        yope3d.draw_line(yope3d.Vec3(1.28, 2.24, 0.25), yope3d.Vec3(3.27, 4.26, 1.1))\n        r = yope3d.to_radians(123.0)\n\n    def on_ui_press(self, world, entity):\n        e = world.add_capsule(0.5, 0.57, 64.24, yope3d.Vec3(3.27, 3.86, 3.82))\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.43)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.1)\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.48, 0.13, 0.97)\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.emissive_map = \"textures/lamp_emissive.png\"\n        body = world.add_kinematic_box(yope3d.Vec3(2.98, 2.79, 3.32), yope3d.Vec3(0.62, 0.81, 0.59))\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(0.67, 2.16, 3.27)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.44)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.35, 0.45, yope3d.",
   "suffix": "        res = world.get_ui_focus()\n        world.attach_sphere_collider(entity, 39.29, 0.4, True)\n        yope3d.load_scene(\"scenes/menu.yscene\")\n        world.set_mesh_color(entity, 2.73, 1.34, 2.43)\n\n    def init(self, world, entity, params):\n        n = yope3d.Vec3(3.36, 0, 4.15).normalize()\n        v = yope3d.input.is_mmb_down()\n        world.remove_light(entity)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n",
   "target": "EASE_QUAD_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:14:62:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 14,
   "col": 62,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.",
   "suffix": "    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n",
   "target": "Vec3",
   "rest": "(2, 0.2, 2))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0106.py:48:12:settings",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0106.py",
   "line": 48,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth106Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.79},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.reset_physics()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.71, 0, 1.65) - origin).normalize())\n        v = yope3d.attach_script(entity, \"ground\", \"ground\")\n        world.attach_obb_collider(entity, 69.08, yope3d.Vec3(0.36, 0.71, 0.69))\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.swing_limit = 1.23\n        cmp.local_twist_axis_a = yope3d.Vec3(0.56, 0.46, 0.43)\n\n    def update(self, world, entity, dt):\n        e = world.add_aabb(yope3d.Vec3(0.59, 0.25, 0.43), 88.03)\n        yope3d.camera.set_position(yope3d.Vec3(0.21, 4.71, 2.58))\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.hover_r = 2.8\n        m.normal_b = 0.27\n        m.normal_r = 2.37\n        m.hover_g = 1.94\n        v = yope3d.capsule_overlap(yope3d.Vec3(1.29, 0.94, 2.18), 1.92, 1.01)\n        world.set_ui_focus(e)\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.k = 3.86\n        c.rest_length = 0.34\n        c.target = entity\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.capsule_overlap(yope3d.Vec3(4.11, 4.82, 2.45), 1.05, 0.45)\n        v = yope3d.get_world_position(entity)\n        v = yope3d.window.get_escape_closes()\n        body = world.add_static_aabb(yope3d.Vec3(2.88, 4.18, 3.41), yope3d.Vec3(0.38, 0.01, 0.62))\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.56)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.25, 2.46, yope3d.EASE_QUAD_IN_OUT)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        v = yope3d.",
   "suffix": "        res = world.get_contact_point_count()\n\n",
   "target": "settings",
   "rest": ".save()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0089.py:34:12:reg_valid",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0089.py",
   "line": 34,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth089Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.71},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_skybox([])\n        n = yope3d.Vec3(0.76, 0, 4.84).normalize()\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.bus = 3\n        c.path = \"textures/tnail.png\"\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.tint_g = 0.06\n        hull.tint_r = 0.21\n        hull.tint_b = 0.65\n        hull.tint_a = 0.95\n\n    def on_ui_press(self, world, entity):\n        world.detach_physics_body(entity)\n        v = yope3d.save_path(\"textures/tnail.png\")\n        for ent, script, cylind in yope3d.view(\"ScriptComponent\", \"CylinderForm\"):\n            script.script_class = \"player\"\n        h = world.attach_animation(entity, \"textures/tnail.png\")\n        m = yope3d.reg_get(entity, \"Name\")\n        if m is not None:\n            m.value = \"ground\"\n\n    def init(self, world, entity, params):\n        v = yope3d.",
   "suffix": "        m = yope3d.reg_get(entity, \"CapsuleForm\")\n        if m is not None:\n            m.half_height = 0.87\n        world.attach_sphere_collider(entity, 8.83, 0.51)\n        hull = yope3d.reg_get(entity, \"UIText\")\n        if hull is not None:\n            hull.alignment = 0.69\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"AnimationPlayer\")\n",
   "target": "reg_valid",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0066.py:53:36:KEY_RIGHT_BRACKET",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0066.py",
   "line": 53,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth066Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        c = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if c is not None:\n            c.target = entity\n        if yope3d.input.is_key_pressed(yope3d.KEY_Q):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.35)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.63, 0.68, yope3d.EASE_QUAD_OUT)\n        world.set_paused(False)\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.text = \"Score: 0\"\n        c.font = \"fonts/inter_ascii.msdf\"\n        h = world.add_vehicle(entity, [])\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.68)\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(3.94, 1.39, 3.61), yope3d.Vec3(0.88, 4.06, 1.25), True, -0.74, -1.17, True)\n        for ent, hingej, uibutt in yope3d.view(\"HingeJointConstraint\", \"UIButton\"):\n            hingej.local_anchor_a = yope3d.Vec3(3.04, 4.55, 0.33)\n        world.clear_shadow_caster()\n\n    def update(self, world, entity, dt):\n        yope3d.set_profile_scene(\"scenes/level_01.yscene\")\n        m = yope3d.reg_add(entity, \"AABBForm\")\n        m.extent = yope3d.Vec3(0.96, 0.12, 0.91)\n        e = world.add_kinematic_box(yope3d.Vec3(0.44, 4.74, 4.91), yope3d.Vec3(0.02, 0.81, 0.79))\n        res = world.get_contact_point_count()\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 32.19, 0))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.15)\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.normal_a = 1.95\n        m.normal_r = 1.95\n        target = world.add_point_light(yope3d.Vec3(4.68, 4.22, 2.91))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_cylinder_mesh(other, 0.2, 0.73)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 2.45, 0))\n        v = yope3d.play_sound(\"textures/tnail.png\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.91)\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_g = 0.34\n        cmp.tint_a = 0.13\n        cmp.path = \"models/prop.glb\"\n        cmp.tint_b = 0.86\n        cmp.tint_r = 0.01\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n",
   "target": "KEY_RIGHT_BRACKET",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:180:15:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 180,
   "col": 15,
   "prefix": "SUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.",
   "suffix": "        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.camera.look_at(tf.position)\n",
   "target": "reg_get",
   "rest": "(self.chassis, \"Hull\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0057.py:22:39:KEY_LEFT_BRACKET",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0057.py",
   "line": 22,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth057Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.49},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.input.is_key_released(3)\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.loop = 4\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.8, 0.24, 0.57)\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_b = yope3d.Vec3(2.83, 2.35, 1.18)\n        cmp.target = entity\n        cmp.local_anchor_a = yope3d.Vec3(2.6, 4.4, 1.93)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.input.is_mouse_released(0)\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"ScriptComponent\")\n        m.script_class = \"hud_root\"\n        m.params_blob = \"hud_root\"\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_a = 0.21\n",
   "target": "KEY_LEFT_BRACKET",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:190:8:camera",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 190,
   "col": 8,
   "prefix": "\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.",
   "suffix": "\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.reg_get(e, \"Transform\")\n            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 1.0, 0.0), self.steer_angle)\n                wt.rotation = rot * (steer_q * self._wheel_align)\n            else:\n",
   "target": "camera",
   "rest": ".look_at(tf.position)",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:332:13:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 332,
   "col": 13,
   "prefix": "\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.",
   "suffix": "        rb = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(-20.0))\n        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_rest(self, world):\n        self.plane_floor(world)\n        self.obb(world, H, 1.0, (0, 0.5, 0), OBB_A)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(20.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:289:15:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 289,
   "col": 15,
   "prefix": "                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.audio.load_sound(\"audios/test.ogg\")\n        if buf is not None:\n            self.doppler_src = yope3d.audio.create_source(buf)\n            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n        self.self_collide = bool(params.get(\"cloth_self_collision\", False))\n        self.cloth_nodes = []\n        self.prev_v = False\n        GalleryBase.init(self, world, entity, params)\n\n    def on_update(self, world, dt):\n        v = yope3d.input.is_key_down(yope3d.KEY_V)\n        if v and not self.prev_v and self.cloth_nodes:\n            self.self_collide = not self.self_collide\n            self.apply_cloth_filter(world)\n            self.refresh_stats(world)\n        self.prev_v = v\n\n        # The audio source is not an entity, so it needs a manual per-frame sync of\n        # position + velocity (velocity is what OpenAL derives the Doppler shift from).\n        src = getattr(self, \"doppler_src\", None)\n        ent = getattr(self, \"doppler_ent\", None)\n        if src is None or ent is None:\n            return\n        tf = yope3d.reg_get(ent, \"Transform\")\n        hull = yope3d.",
   "suffix": "        if tf is not None and hull is not None:\n            src.set_position(tf.position)\n            src.set_velocity(hull.velocity)\n\n    def controls_extra(self):\n        return \"   V self-collide\" if self.cloth_nodes else \"\"\n\n    def stats_extra(self, world):\n        if not self.cloth_nodes:\n            return \"\"\n",
   "target": "reg_get",
   "rest": "(ent, \"Hull\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:333:41:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 333,
   "col": 41,
   "prefix": "    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(20.0))\n        rb = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_rest(self, world):\n        self.plane_floor(world)\n        self.obb(world, H, 1.0, (0, 0.5, 0), OBB_A)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n\n",
   "target": "Vec3",
   "rest": "(0, 1, 0), math.radians(-20.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0112.py:36:12:lerp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0112.py",
   "line": 36,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth112Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.48},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.78, 0, 0.48) - origin).normalize())\n        n = yope3d.Vec3(3.9, 0, 2.74).normalize()\n        m = yope3d.reg_get(entity, \"UIBackground\")\n        if m is not None:\n            m.r = 0.66\n        v = yope3d.reg_get(other, 2.37)\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"CylinderForm\")\n        t.half_height = 0.71\n        t.radius = 1.66\n        r = world.get_island_count()\n        world.attach_sphere_mesh(entity, 0.56, 4.16, 2.82, 4.76, 2)\n        cmp = yope3d.reg_add(entity, \"UITransform\")\n        cmp.max_y = 1.76\n        cmp.depth = 2\n        cmp.visible = True\n        for ent, script, capsul in yope3d.view(\"ScriptComponent\", \"CapsuleForm\"):\n            script.params_blob = \"player\"\n\n    def update(self, world, entity, dt):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        t = yope3d.clamp(dt * 4.06, 0.0, 1.0)\n        v = yope3d.",
   "suffix": "        v = yope3d.window.get_width()\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.loaded = True\n        t.asset_path = \"models/prop.glb\"\n        n = yope3d.Vec3(1.68, 0, 4.1).normalize()\n        for ent, compou, sphere in yope3d.view(\"CompoundCollider\", \"SphereForm\"):\n            compou.asset_path = \"audios/impact.ogg\"\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.r = 0.07\n        hull.g = 1.04\n",
   "target": "lerp",
   "rest": "(3.27, 4.51, t)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:92:12:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 92,
   "col": 12,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.",
   "suffix": "            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n",
   "target": "Vec3",
   "rest": "(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0109.py:24:52:EASE_CUBIC_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0109.py",
   "line": 24,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth109Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        yope3d.load_scene(\"scenes/menu.yscene\")\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.normal_scale = 1.84\n        for ent, cylind, aabbfo in yope3d.view(\"CylinderForm\", \"AABBForm\"):\n            cylind.half_height = 1.1\n        for ent, spring, audios in yope3d.view(\"SpringConstraint\", \"AudioSource\"):\n            spring.rest_length = 0.24\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.lower_angle = -1.11\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.94)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.57, 1.05, yope3d.",
   "suffix": "        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"hud_root\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.r = 0.24\n        c.b = 2.73\n        if yope3d.input.is_key_pressed(yope3d.KEY_8):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"Hull\")\n",
   "target": "EASE_CUBIC_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:204:18:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 204,
   "col": 18,
   "prefix": "        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.camera.look_at(tf.position)\n\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.reg_get(e, \"Transform\")\n            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 1.0, 0.0), self.steer_angle)\n                wt.rotation = rot * (steer_q * self._wheel_align)\n            else:\n                wt.rotation = rot * self._wheel_align\n\n        for e, offset, local_rot in self._decor:\n            dtf = yope3d.",
   "suffix": "            dtf.position = tf.position + rot.rotate(offset)\n            dtf.rotation = (rot * local_rot) if local_rot is not None else rot\n",
   "target": "reg_get",
   "rest": "(e, \"Transform\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0037.py:50:12:lerp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0037.py",
   "line": 50,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth037Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.raycast(yope3d.Vec3(3.35, 0.61, 3.77), yope3d.Vec3(3.24, 2.03, 0.12), 0.69)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.39\n        prop = world.add_ui_background(yope3d.Vec2(4.29, 3.67), yope3d.Vec2(0.07, 3.63), yope3d.Vec4(0.2, 2.93, 4.26, 1.47), 1)\n        cmp = yope3d.reg_add(entity, \"AABBForm\")\n        cmp.extent = yope3d.Vec3(0.95, 0.68, 0.69)\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.rest_length = 0.1\n        hull.target = entity\n        hull.k = 4.64\n        world.set_skybox([])\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n\n    def init(self, world, entity, params):\n        res = world.get_contact_point_count()\n        for ent, uitext in yope3d.view(\"UITexturedBackground\"):\n            uitext.tint_a = 0.71\n        v = yope3d.camera.get_forward()\n        for ent, uitran, aabbfo in yope3d.view(\"UITransform\", \"AABBForm\"):\n            uitran.anchor = 2\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.26)\n\n    def on_ui_press(self, world, entity):\n        h = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.KEY_Y):\n            world.apply_impulse(entity, yope3d.Vec3(0, 26.93, 0))\n        form = yope3d.reg_get(entity, \"OBBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.22, 0.49, 0.13)\n        r = yope3d.to_radians(54.0)\n        res = world.get_contact_count()\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_down(yope3d.KEY_LEFT_CONTROL):\n            world.apply_impulse(entity, yope3d.Vec3(0, 21.73, 0))\n        v = yope3d.settings.get_int(\"player\")\n        yope3d.camera.set_clip_planes(3.1, 1.64)\n        t = yope3d.clamp(dt * 7.07, 0.0, 1.0)\n        v = yope3d.",
   "suffix": "        t = yope3d.reg_add(entity, \"UIButton\")\n        t.normal_g = 3.08\n        t.normal_r = 3.67\n        t.normal_a = 0.67\n        t.normal_b = 3.17\n        t.hover_g = 3.06\n        e = world.add_aabb(yope3d.Vec3(0.1, 0.96, 0.57), 18.11)\n\n",
   "target": "lerp",
   "rest": "(0.61, 3.19, t)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0082.py:25:39:KEY_2",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0082.py",
   "line": 25,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth082Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.74\n        if yope3d.input.is_key_pressed(yope3d.KEY_Z):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_wheel_steer(entity, 1.09)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 21.57)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat()\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.camera.set_position(yope3d.Vec3(3.48, 1.05, 4.94))\n        for ent, conetw in yope3d.view(\"ConeTwistJointConstraint\"):\n            conetw.target = entity\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.camera.screen_to_ray(1.39, 0.09)\n        c = yope3d.reg_get(entity, \"CapsuleForm\")\n        if c is not None:\n            c.radius = 1.7\n",
   "target": "KEY_2",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0092.py:51:39:KEY_PAGE_UP",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0092.py",
   "line": 51,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth092Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.22},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.upper_angle = 1.21\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.g = 1.03\n        hull.a = 3.5\n        hull.b = 0.12\n        hull.r = 4.28\n        for ent, transf, name in yope3d.view(\"Transform\", \"Name\"):\n            transf.position = yope3d.Vec3(0.4, 0.55, 0.38)\n        world.remove_spring_between(entity, entity)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.draw_line(yope3d.Vec3(4.38, 2.28, 3.85), yope3d.Vec3(4.89, 2.32, 1.59))\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -1.28)\n        c = yope3d.reg_add(entity, \"UITransform\")\n        c.depth = 0\n        c.min_x = 3.2\n        c.max_x = 2.77\n        c.visible = True\n        c.max_y = 1.39\n        if yope3d.input.is_key_down(yope3d.KEY_Q):\n            world.apply_impulse(entity, yope3d.Vec3(0, 37.46, 0))\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"Parent\")\n        if form is not None:\n            form.parent = entity\n        v = yope3d.settings.has(\"hud_root\")\n        v = yope3d.is_fixed(entity)\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.pressed_b = 3.16\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 1.79\n\n    def on_ui_press(self, world, entity):\n        yope3d.settings.set_bool(\"player\", False)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        world.attach_sphere_mesh(entity, 1.11)\n        world.wake(entity)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.type = 0.93\n        t.intensity = 5.13\n        t.constant = 0.04\n        t.color = yope3d.Vec3(2.7, 4.25, 1.72)\n        t.position = yope3d.Vec3(0.71, 0.1, 0.64)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.26)\n",
   "target": "KEY_PAGE_UP",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:155:27:KEY_S",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 155,
   "col": 27,
   "prefix": "enough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.",
   "suffix": "\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n",
   "target": "KEY_S",
   "rest": "): throttle -= 1.0",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0030.py:67:34:BUS_MUSIC",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0030.py",
   "line": 67,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth030Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.43},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.09)\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.script_class = \"crate\"\n        for ent, sphere, cylind in yope3d.view(\"SphereForm\", \"CylinderForm\"):\n            sphere.radius = 1.75\n        target = world.add_kinematic_capsule(0.77, 0.22, yope3d.Vec3(2.18, 0.28, 4.7))\n        v = yope3d.is_sleeping(target)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.65)\n        world.wake(target)\n        yope3d.set_velocity(target, yope3d.Vec3(0.7, 0.93, 0.35))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 1.31\n        m.half_height = 1.03\n        world.apply_impulse(other, yope3d.Vec3(8.76, 17.61, 27.87))\n        anchor = world.add_ui_curved_background(yope3d.Vec2(3.99, 3.35), yope3d.Vec2(2.14, 2.13), yope3d.Vec4(0.91, 4.73, 4.99, 2.29))\n        form = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if form is not None:\n            form.tint_a = 0.17\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo_map = \"textures/crate_albedo.png\"\n        hull.albedo = (3.37, 4.67, 3.65, 3.32)\n        hull.normal_map = \"textures/crate_normal.png\"\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.path = \"textures/tnail.png\"\n        for ent, spring in yope3d.view(\"SpringConstraint\"):\n            spring.k = 3.3\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"SpringConstraint\")\n        m.target = entity\n        m.k = 1.31\n        m.rest_length = 0.23\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.r = 4.83\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (1.11, 0.69, 2.3, 1.03)\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        hull.albedo_map = \"textures/metal_basecolor.png\"\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_skybox([])\n        hull = yope3d.reg_get(entity, \"SpringConstraint\")\n        if hull is not None:\n            hull.rest_length = 0.81\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        world.clear_shadow_caster()\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 12.93)\n        for ent, animat in yope3d.view(\"AnimationPlayer\"):\n            animat.loop = 1\n        v = yope3d.is_sleeping(entity)\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.gain = 0.47\n",
   "target": "BUS_MUSIC",
   "rest": ", 0.26)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0042.py:17:12:to_degrees",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0042.py",
   "line": 17,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth042Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.64},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.min_y = 4.8\n        m.max_x = 4.39\n        m.depth = 4\n        m.visible = True\n        v = yope3d.",
   "suffix": "        for ent, lights, capsul in yope3d.view(\"LightSource\", \"CapsuleForm\"):\n            lights.quadratic = 0.84\n        v = yope3d.input.get_cursor_pos()\n        v = yope3d.settings.keys()\n\n    def update(self, world, entity, dt):\n        yope3d.load_game(\"spawn_point\")\n        yope3d.settings.set_float(\"crate\", 1.46)\n        c = yope3d.reg_get(entity, \"UIText\")\n        if c is not None:\n",
   "target": "to_degrees",
   "rest": "(4.96)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:258:14:audio",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 258,
   "col": 14,
   "prefix": "\n        self.set_camera((4.0, MR_LEVELS * spacing * 0.6, 5.0), pitch=-0.15, yaw=0.7)\n\n    # ---- Spring cloth ----------------------------------------------------\n    # variant:  0 = top row fixed, 1 = 4 corners fixed, 2 = 2 top corners fixed\n    # shape:    0 = sphere, 1 = AABB, 2 = OBB\n    # Variant 1 hangs the sheet horizontally (a trampoline); 0 and 2 hang it\n    # vertically (a curtain).\n\n    def build_cloth(self, world, variant, shape):\n        fh = (GRID_N + 1) * GRID_STEP * 2.0\n        self.static_box(world, (0.0, -5.0, 0.0), (fh, 5.0, fh), FLOOR_COLOR)\n\n        horizontal = variant == 1\n        half_w = (GRID_N - 1) * GRID_STEP * 0.5\n        top_y = 25.0 if horizontal else (GRID_N - 1) * GRID_STEP + 25.0\n\n        grid = [[None] * GRID_N for _ in range(GRID_N)]\n        half = (NODE_HALF, NODE_HALF, NODE_HALF)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N):\n                if horizontal:\n                    pos = (-half_w + i * GRID_STEP, top_y, -half_w + j * GRID_STEP)\n                else:\n                    pos = (-half_w + i * GRID_STEP, top_y - j * GRID_STEP, 0.0)\n\n                fi = i / (GRID_N - 1)\n                fj = j / (GRID_N - 1)\n\n                if shape == 0:\n                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.",
   "suffix": "        if buf is not None:\n            self.doppler_src = yope3d.audio.create_source(buf)\n            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n",
   "target": "audio",
   "rest": ".load_sound(\"audios/test.ogg\")",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0062.py:57:14:play_sound",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0062.py",
   "line": 57,
   "col": 14,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth062Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.13},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.8)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.86, yope3d.EASE_LINEAR)\n        anchor = world.add_kinematic_capsule(1.46, 0.71, yope3d.Vec3(0.22, 3.27, 1.25))\n        v = yope3d.to_radians(1.75)\n        if yope3d.input.is_key_pressed(yope3d.KEY_SLASH):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.set_position(entity, yope3d.Vec3(4.59, 0.66, 3.21))\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_b = 0.75\n        c.normal_a = 1.14\n        c.hover_r = 3.93\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.input.get_key_name(1)\n        for ent, textla, conetw in yope3d.view(\"TextLabel3D\", \"ConeTwistJointConstraint\"):\n            textla.text = \"Score: 0\"\n        world.set_ui_parent(entity, entity)\n        v = yope3d.get_world_position(entity)\n        h = world.get_pair_count()\n        yope3d.window.set_f11_fullscreen(True)\n        hull = yope3d.reg_get(entity, \"Transform\")\n        if hull is not None:\n            hull.rotation = yope3d.Quat.from_euler(0.0, -1.13)\n        m = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if m is not None:\n            m.tint_a = 0.07\n\n    def update(self, world, entity, dt):\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.enabled = False\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.b = 1.35\n        c.font = \"fonts/inter_ascii.msdf\"\n        h = world.add_vehicle(entity, [])\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = True\n        yope3d.window.set_cursor_locked(True)\n        yope3d.settings.clear()\n        body = world.add_ui_text(\"Score: 0\", \"Score: 0\", yope3d.Vec2(4.69, 4.56), yope3d.Vec2(0.04, 1.11))\n\n    def init(self, world, entity, params):\n        other = world.add_aabb(yope3d.Vec3(0.63, 0.31, 0.69), 69.6, yope3d.Vec3(0.67, 0.52, 4.95))\n        src = yope3d.",
   "suffix": "        if src is not None:\n            yope3d.audio.fade_gain(src, 0.32, 2.82, yope3d.EASE_LINEAR)\n        world.set_ui_group_visible(other, False)\n        c = yope3d.reg_add(entity, \"UITexturedBackground\")\n        c.tint_r = 0.16\n        c.tint_a = 0.61\n        world.tween_ui_opacity(other, 0.58, 1.07)\n        m = yope3d.reg_get(entity, \"LightSource\")\n        if m is not None:\n            m.direction = yope3d.Vec3(0.57, 0.48, 0.25)\n",
   "target": "play_sound",
   "rest": "(\"audios/impact.ogg\", None, 0.74)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:130:28:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 130,
   "col": 28,
   "prefix": "\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.",
   "suffix": "                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n",
   "target": "Vec3",
   "rest": "(0.7, 0.32, 0.8),      # cabin greenhouse",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "typings/yope3d/__init__.pyi:20:4:draw_ray",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 20,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.",
   "suffix": "    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n",
   "target": "draw_ray",
   "rest": "(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0039.py:56:36:KEY_SLASH",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0039.py",
   "line": 56,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth039Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.12},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Score: 0\"\n        wheel = world.add_kinematic_capsule(1.54, 0.54)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"crate\"\n        res = world.get_registry()\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.4, 2.68, 1.73)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.03, 2.91, 3.91)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.58, 1.34), yope3d.Vec2(1.62, 0.21), yope3d.Vec4(0.69, 0.66, 0.06, 0.99), \"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.46\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"player\"\n        t.script_class = \"hud_root\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_RIGHT_BRACKET):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 23.33, 0))\n        for ent, lights, conetw in yope3d.view(\"LightSource\", \"ConeTwistJointConstraint\"):\n            lights.color = yope3d.Vec3(0.02, 0.24, 1.28)\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.71)\n        body = world.add_capsule(0.5, 0.32, 13.11)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.52, 0.8, 1.61)\n        t.rotation = yope3d.Quat.from_euler(0.0, -0.97)\n        t.position = yope3d.Vec3(0.43, 0.38, 0.93)\n        h = world.get_pair_count()\n        res = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_I):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, uibutt, hingej in yope3d.view(\"UIButton\", \"HingeJointConstraint\"):\n            uibutt.normal_a = 0.27\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 9.66, 0))\n        anchor = world.add_capsule(1.24, 0.3, 14.4)\n        prop = world.add_ui_button(yope3d.Vec2(2.22, 2.41), yope3d.Vec2(0.95, 1.55), yope3d.Vec4(0.19, 3.54, 2.11, 4.2))\n        if yope3d.input.is_key_pressed(yope3d.KEY_UP):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n",
   "target": "KEY_SLASH",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:15:37:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 15,
   "col": 37,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.",
   "suffix": "    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n",
   "target": "Vec3",
   "rest": "(2, 0.2, 2), 0.6, 0.6, 0.7)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0024.py:17:39:KEY_5",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0024.py",
   "line": 17,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth024Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.18},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.half_height = 1.16\n        hull.radius = 0.96\n        yope3d.audio.pause_all()\n        v = yope3d.play_music(\"textures/tnail.png\")\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"Transform\")\n        m.scale = yope3d.Vec3(0.68, 0.56, 0.4)\n        m.position = yope3d.Vec3(0.06, 0.37, 0.42)\n        form = yope3d.reg_get(entity, \"OBBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.6, 0.68, 0.82)\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.half_height = 0.69\n        m.radius = 0.7\n",
   "target": "KEY_5",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0105.py:59:36:KEY_X",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0105.py",
   "line": 59,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth105Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.69},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.37\n        t = yope3d.clamp(dt * 2.01, 0.0, 1.0)\n        v = yope3d.lerp(4.44, 4.16, t)\n        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 1.63\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.r = 2.72\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.37, 3.58), yope3d.Vec2(4.04, 3.48), yope3d.Vec4(0.95, 0.01, 0.66, 0.92), \"audios/impact.ogg\")\n        yope3d.window.set_f11_fullscreen(True)\n        world.set_mesh_color(entity, 0.69, 0.67, 1.35)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.2\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat()\n        h = world.get_ui_focus()\n        world.apply_impulse_at(other, yope3d.Vec3(19.13, 8.98, 30.3), yope3d.Vec3(0.32, 0.83, 0.55))\n        h = world.get_ui_focus()\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        n = yope3d.Vec3(4.42, 0, 3.67).normalize()\n        yope3d.load_scene(\"scenes/level_01.yscene\")\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.1\n        t.target = entity\n        t.k = 0.19\n        e = world.add_kinematic_capsule(1.12, 0.42)\n        hull = yope3d.reg_get(entity, \"OBBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.72, 0.85, 0.01)\n        world.set_ui_group_opacity(e, 0.24)\n        body = world.add_aabb(yope3d.Vec3(0.86, 0.06, 0.25), 34)\n        for ent, transf, capsul in yope3d.view(\"Transform\", \"CapsuleForm\"):\n            transf.position = yope3d.Vec3(0.12, 0.68, 0.1)\n        prop = world.add_ui_textured_background(yope3d.Vec2(1.34, 4.58), yope3d.Vec2(3.55, 0.7), yope3d.Vec4(0.61, 0.98, 0.58, 0.86), \"models/prop.glb\")\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"UIBackground\")\n        m.b = 0.56\n        m.a = 4.73\n        m.r = 1.49\n        m.g = 0.08\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 17.16, 0))\n        c = yope3d.reg_add(entity, \"UIText\")\n        c.font = \"fonts/inter_ascii.msdf\"\n        c.text = \"Press E to interact\"\n        c.a = 2.68\n        if yope3d.input.is_key_down(yope3d.KEY_A):\n            world.apply_impulse(entity, yope3d.Vec3(0, 5.71, 0))\n        world.remove_entity(entity)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.52)\n        r = yope3d.to_radians(34.0)\n",
   "target": "KEY_X",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:13:4:draw_ray",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 13,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.",
   "suffix": "    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n",
   "target": "draw_ray",
   "rest": "(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:9:11:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 9,
   "col": 11,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.",
   "suffix": "    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n",
   "target": "camera",
   "rest": ".position, yope3d.camera.get_forward()",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0113.py:30:8:clear_collision_observers",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0113.py",
   "line": 30,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth113Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.76},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        res = world.get_ui_focus()\n        yope3d.camera.set_fov(68.47)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.76, 0, 4.64) - origin).normalize())\n        world.remove_spring_between(entity, entity)\n\n    def on_ui_press(self, world, entity):\n        form = yope3d.reg_get(entity, \"CompoundCollider\")\n        if form is not None:\n            form.loaded = True\n        yope3d.set_profile_scene(\"scenes/level_01.yscene\")\n        world.add_point_joint(entity, entity, yope3d.Vec3(4.5, 0.12, 1.61))\n        v = yope3d.reg_add(entity, 3.04)\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.hover_g = 3.59\n        m.normal_b = 3.65\n        m.normal_a = 3.59\n        m.hover_r = 0.77\n        yope3d.",
   "suffix": "\n    def init(self, world, entity, params):\n        v = yope3d.reg_get(entity, 1.79)\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.pressed_g = 2.56\n        cmp = yope3d.reg_add(entity, \"Transform\")\n        cmp.position = yope3d.Vec3(0.64, 0.81, 0.34)\n        cmp.rotation = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 1.37)\n        cmp.scale = yope3d.Vec3(1.3, 1.01, 1.05)\n",
   "target": "clear_collision_observers",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0052.py:50:36:KEY_Z",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0052.py",
   "line": 50,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth052Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, materi, capsul in yope3d.view(\"Material\", \"CapsuleForm\"):\n            materi.albedo_map = \"textures/crate_albedo.png\"\n        v = yope3d.clamp(1.41, 3.98, 0.36)\n        for ent, textla, conetw in yope3d.view(\"TextLabel3D\", \"ConeTwistJointConstraint\"):\n            textla.size_meters = 0.54\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n        if hull is not None:\n            hull.script_class = \"crate\"\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.loaded = True\n        t.asset_path = \"audios/impact.ogg\"\n        for ent, name, conetw in yope3d.view(\"Name\", \"ConeTwistJointConstraint\"):\n            name.value = \"ground\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.get_world_position(entity)\n        yope3d.audio.stop_all()\n        v = yope3d.settings.save()\n        world.add_cone_twist_joint(other, entity, yope3d.Vec3(1.32, 3.78, 1.76), yope3d.Vec3(0.27, 0.29, 0.26), 1.45, 0.38, True)\n        prop = world.add_aabb(yope3d.Vec3(0.38, 0.51, 0.46), 24.77, yope3d.Vec3(4.21, 1.08, 4.54))\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.rest_length = 0.78\n        hull.k = 2.87\n\n    def init(self, world, entity, params):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.display_px = 3\n        yope3d.camera.set_rotation(yope3d.Vec3(2.41, 3.95, 4.85))\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.pitch = 1.07\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.r = 3.08\n        c.b = 0.59\n        world.set_wheel_steer(entity, 0.82)\n\n    def update(self, world, entity, dt):\n        v = yope3d.raycast(yope3d.Vec3(2.92, 2.69, 3.67), yope3d.Vec3(4.43, 4.94, 4.52), 0.35)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 27.18, 0))\n        world.detach_physics_body(entity)\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"textures/tnail.png\"\n        v = yope3d.view()\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.09)\n\n",
   "target": "KEY_Z",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0105.py:69:12:to_radians",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0105.py",
   "line": 69,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth105Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.69},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.37\n        t = yope3d.clamp(dt * 2.01, 0.0, 1.0)\n        v = yope3d.lerp(4.44, 4.16, t)\n        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 1.63\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.r = 2.72\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.37, 3.58), yope3d.Vec2(4.04, 3.48), yope3d.Vec4(0.95, 0.01, 0.66, 0.92), \"audios/impact.ogg\")\n        yope3d.window.set_f11_fullscreen(True)\n        world.set_mesh_color(entity, 0.69, 0.67, 1.35)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.2\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat()\n        h = world.get_ui_focus()\n        world.apply_impulse_at(other, yope3d.Vec3(19.13, 8.98, 30.3), yope3d.Vec3(0.32, 0.83, 0.55))\n        h = world.get_ui_focus()\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        n = yope3d.Vec3(4.42, 0, 3.67).normalize()\n        yope3d.load_scene(\"scenes/level_01.yscene\")\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.1\n        t.target = entity\n        t.k = 0.19\n        e = world.add_kinematic_capsule(1.12, 0.42)\n        hull = yope3d.reg_get(entity, \"OBBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.72, 0.85, 0.01)\n        world.set_ui_group_opacity(e, 0.24)\n        body = world.add_aabb(yope3d.Vec3(0.86, 0.06, 0.25), 34)\n        for ent, transf, capsul in yope3d.view(\"Transform\", \"CapsuleForm\"):\n            transf.position = yope3d.Vec3(0.12, 0.68, 0.1)\n        prop = world.add_ui_textured_background(yope3d.Vec2(1.34, 4.58), yope3d.Vec2(3.55, 0.7), yope3d.Vec4(0.61, 0.98, 0.58, 0.86), \"models/prop.glb\")\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"UIBackground\")\n        m.b = 0.56\n        m.a = 4.73\n        m.r = 1.49\n        m.g = 0.08\n        if yope3d.input.is_key_down(yope3d.KEY_X):\n            world.apply_impulse(entity, yope3d.Vec3(0, 17.16, 0))\n        c = yope3d.reg_add(entity, \"UIText\")\n        c.font = \"fonts/inter_ascii.msdf\"\n        c.text = \"Press E to interact\"\n        c.a = 2.68\n        if yope3d.input.is_key_down(yope3d.KEY_A):\n            world.apply_impulse(entity, yope3d.Vec3(0, 5.71, 0))\n        world.remove_entity(entity)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.52)\n        r = yope3d.",
   "suffix": "\n",
   "target": "to_radians",
   "rest": "(34.0)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0096.py:62:38:MOUSE_MIDDLE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0096.py",
   "line": 62,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth096Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.11},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.pitch = 0.84\n        if yope3d.input.is_key_down(yope3d.KEY_GRAVE_ACCENT):\n            world.apply_impulse(entity, yope3d.Vec3(0, 12.25, 0))\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.asset_path = \"textures/tnail.png\"\n        t.loaded = False\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"textures/tnail.png\"\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.intensity = 6.37\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_get(entity, \"UIButton\")\n        if m is not None:\n            m.pressed_g = 4.5\n        v = yope3d.settings.get_bool(\"hud_root\")\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.font = \"fonts/inter_ascii.msdf\"\n        c.r = 0.94\n        c.a = 0.17\n        c.b = 1.81\n        c.g = 3.48\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"PointJointConstraint\")\n        t.target = entity\n        t.local_anchor_b = yope3d.Vec3(4.23, 1.09, 3.81)\n        t.local_anchor_a = yope3d.Vec3(3.46, 3.42, 0.76)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.88)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.29, 2.93, yope3d.EASE_CUBIC_IN_OUT)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(3.92, 0, 4.36) - origin).normalize())\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.loaded = True\n        t.asset_path = \"audios/impact.ogg\"\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        world.fix_entity(entity)\n        cmp = yope3d.reg_add(entity, \"UIBackground\")\n        cmp.a = 3.53\n        cmp.b = 4.73\n        cmp.r = 3.61\n\n    def update(self, world, entity, dt):\n        world.set_wheel_drive(entity, 3.92)\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 22.33)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.49)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.6, 2.62, yope3d.EASE_QUAD_IN_OUT)\n        target = world.add_ui_textured_background(yope3d.Vec2(1.65, 1.66), yope3d.Vec2(0.67, 3.49), yope3d.Vec4(0.65, 0.63, 0.02, 0.8), \"models/prop.glb\")\n        hull = yope3d.reg_get(entity, \"SpringConstraint\")\n        if hull is not None:\n            hull.k = 4.66\n        yope3d.window.set_escape_closes(True)\n",
   "target": "MOUSE_MIDDLE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0005.py:15:8:reg_remove",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0005.py",
   "line": 15,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth005Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.42},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_skybox([])\n        if yope3d.input.is_key_pressed(yope3d.KEY_1):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.",
   "suffix": "        yope3d.clear_collision_observers()\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.anchor = 3\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n        world.attach_box_mesh(entity, yope3d.Vec3(0.93, 3.05, 2.2))\n        h = world.add_vehicle(entity, [])\n\n",
   "target": "reg_remove",
   "rest": "(entity, \"ConeTwistJointConstraint\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0060.py:12:12:save_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0060.py",
   "line": 12,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth060Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_8):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        r = yope3d.to_radians(43.0)\n        for ent, lights, spring in yope3d.view(\"LightSource\", \"SpringConstraint\"):\n            lights.type = 1\n        body = world.add_ui_button(yope3d.Vec2(0.6, 1.55), yope3d.Vec2(0.17, 0.39), yope3d.Vec4(4.22, 0.3, 4.35, 0.53))\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.63)\n        v = yope3d.get_position(entity)\n",
   "target": "save_game",
   "rest": "(\"player\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0002.py:56:49:EASE_LINEAR",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0002.py",
   "line": 56,
   "col": 49,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth002Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.87},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.settings.has(\"ground\")\n        hull = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_a = yope3d.Vec3(2.55, 2.96, 4.45)\n        form = yope3d.reg_get(entity, \"TextLabel3D\")\n        if form is not None:\n            form.font = \"fonts/inter_ascii.msdf\"\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 18.09, 0))\n        hull = yope3d.reg_get(entity, \"CompoundCollider\")\n        if hull is not None:\n            hull.loaded = True\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.direction = yope3d.Vec3(0.12, 0.53, 0.93)\n        for ent, materi, audios in yope3d.view(\"Material\", \"AudioSource\"):\n            materi.metal_rough_map = \"textures/crate_mr.png\"\n        hull = yope3d.reg_add(entity, \"PointJointConstraint\")\n        hull.local_anchor_b = yope3d.Vec3(3.62, 4.59, 2.45)\n        hull.local_anchor_a = yope3d.Vec3(4.79, 4.36, 0.26)\n        hull.target = entity\n        if yope3d.input.is_key_down(yope3d.KEY_R):\n            world.apply_impulse(entity, yope3d.Vec3(0, 39.37, 0))\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.28)\n\n    def init(self, world, entity, params):\n        for ent, name, textla in yope3d.view(\"Name\", \"TextLabel3D\"):\n            name.value = \"crate\"\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 11.54)\n        r = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.KEY_F8):\n            world.apply_impulse(entity, yope3d.Vec3(0, 5.64, 0))\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.script_class = \"crate\"\n        cmp.params_blob = \"crate\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        h = world.ui_hovered()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.4)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 1, 2.08, yope3d.",
   "suffix": "        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.83)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.27, 1.98, yope3d.EASE_LINEAR)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.8, 0, 2.79)).length()\n        for ent, parent, textla in yope3d.view(\"Parent\", \"TextLabel3D\"):\n            parent.parent = entity\n        res = world.get_shadow_caster()\n        world.set_wheel_steer(other, 0.2)\n        for ent, sphere, obbfor in yope3d.view(\"SphereForm\", \"OBBForm\"):\n",
   "target": "EASE_LINEAR",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0023.py:17:12:play_music",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0023.py",
   "line": 17,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth023Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.46},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_game(\"player\")\n        v = yope3d.get_position(other)\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.bus = 2\n        prop = world.add_kinematic_capsule(0.23, 0.96, yope3d.Vec3(4.9, 3.67, 2.65))\n        v = yope3d.",
   "suffix": "        v = yope3d.window.get_tab_pauses()\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.radius = 0.84\n        hull.half_height = 0.41\n        v = yope3d.window.get_cursor_pos()\n        wheel = world.add_ui_background(yope3d.Vec2(3.86, 4.1), yope3d.Vec2(0, 0.49), yope3d.Vec4(4.69, 4.14, 4.32, 4.91))\n        yope3d.load_game(\"ground\")\n        res = world.get_contact_count()\n",
   "target": "play_music",
   "rest": "(\"audios/impact.ogg\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0075.py:50:12:look_at",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0075.py",
   "line": 50,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth075Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.14},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"TextLabel3D\")\n        m.font = \"fonts/inter_ascii.msdf\"\n        m.a = 1.68\n        m.r = 4.59\n        m.text = \"Score: 0\"\n        v = yope3d.get_position(entity)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_g = 3.23\n        c.normal_r = 1.27\n        c.normal_b = 1.63\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.billboard = 3\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.text = \"Press E to interact\"\n        hull.font = \"fonts/inter_ascii.msdf\"\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.61)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.direction = yope3d.Vec3(0.09, 0.27, 0.51)\n        t.type = 0.26\n        c = yope3d.reg_get(entity, \"AABBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.52, 0.14, 0.33)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.74)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 2.94, yope3d.EASE_CUBIC_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.play_sound(\"models/prop.glb\")\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.36\n        cmp.has_texture = False\n        cmp.path = \"audios/impact.ogg\"\n        cmp.tint_b = 0.08\n        cmp.tint_g = 0.03\n        v = yope3d.audio.load_sound(\"textures/tnail.png\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.",
   "suffix": "\n    def init(self, world, entity, params):\n        yope3d.settings.clear()\n        t = yope3d.reg_add(entity, \"AnimationPlayer\")\n        t.time = 0.55\n        t.speed = 5.64\n        t.playing = 1\n        t.loop = 0\n        t.clip = \"player\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.54)\n",
   "target": "look_at",
   "rest": "((yope3d.Vec3(4.37, 0, 4.17) - origin).normalize())",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:117:28:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 117,
   "col": 28,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.",
   "suffix": "        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:137:28:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 137,
   "col": 28,
   "prefix": "targeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.",
   "suffix": "\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0081.py:32:8:draw_ray",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0081.py",
   "line": 32,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth081Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.5},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_9):\n            world.apply_impulse(entity, yope3d.Vec3(0, 39.06, 0))\n        world.attach_capsule_collider(entity, 50.37, 0.35, 1.17, False)\n        cmp = yope3d.reg_add(entity, \"UIBackground\")\n        cmp.g = 2.51\n        cmp.a = 3.63\n        cmp.r = 1.57\n        cmp.b = 3.04\n        yope3d.draw_line(yope3d.Vec3(3.94, 3.48, 0.9), yope3d.Vec3(0.62, 2.72, 3.77))\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.reg_valid(entity)\n        m = yope3d.reg_add(entity, \"UIBackground\")\n        m.a = 0.13\n        m.g = 0.53\n        c = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if c is not None:\n            c.local_anchor_b = yope3d.Vec3(1.57, 2.99, 4.68)\n        prop = yope3d.spawn(\"textures/tnail.png\")\n        v = yope3d.view()\n        yope3d.",
   "suffix": "        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.pitch = 1.27\n        t.loop = True\n        t.bus = 0\n        t.path = \"audios/impact.ogg\"\n        t.autoplay = False\n        world.add_spring(entity, entity, 1.87, 0.54)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_get(entity, \"Name\")\n",
   "target": "draw_ray",
   "rest": "(yope3d.Vec3(0.88, 1.5, 1.95), yope3d.Vec3(1.21, 0.04, 1.54))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0079.py:42:12:reg_valid",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0079.py",
   "line": 42,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth079Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.83},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_add(entity, \"Material\")\n        c.albedo_map = \"textures/metal_basecolor.png\"\n        c.metal_rough_map = \"textures/crate_mr.png\"\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.occlusion_map = \"textures/crate_ao.png\"\n        c.albedo = (0.02, 2.44, 3.27, 0.99)\n        yope3d.settings.remove(\"ground\")\n        world.attach_aabb_collider(entity, 70.19, yope3d.Vec3(0.7, 0.11, 0.19), True)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.45, 1.24, yope3d.EASE_QUAD_OUT)\n        cmp = yope3d.reg_add(entity, \"UIText\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.a = 1.31\n        world.wake(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_W):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.82, 0, 1.91) - origin).normalize())\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.fix_entity(other)\n        r = world.get_island_count()\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 2.59\n        t.text = \"Score: 0\"\n        t.g = 2.73\n        t.a = 0.14\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.34, 0, 3.35) - origin).normalize())\n        v = yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.loop = True\n        c.bus = 4\n        if yope3d.input.is_key_pressed(yope3d.KEY_F9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.target = entity\n        c.rest_length = 0.81\n        c.k = 3.03\n\n",
   "target": "reg_valid",
   "rest": "(other)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:49:12:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 49,
   "col": 12,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.Vec3(0, 5, 0)\n\n        floor = world.add_static_aabb(\n            yope3d.",
   "suffix": "        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n        self.spawned += 1\n\n        x = self.pos.x + (self.spawned % 3 - 1) * 1.5\n",
   "target": "Vec3",
   "rest": "(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0021.py:53:12:play_music",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0021.py",
   "line": 53,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth021Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.61},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        v = yope3d.camera.screen_to_ray(0.26, 2)\n        for ent, audios, conetw in yope3d.view(\"AudioSource\", \"ConeTwistJointConstraint\"):\n            audios.loop = True\n        world.add_point_joint(entity, entity, yope3d.Vec3(4.6, 0.14, 3.67))\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.has_texture = True\n        hull.tint_r = 0.59\n        hull.tint_g = 0.27\n        hull.tint_a = 0.32\n        v = yope3d.look_at(yope3d.Vec3(4.43, 4.62, 4.58))\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"UITexturedBackground\")\n        c.tint_r = 0.51\n        c.has_texture = False\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(3.33, 0, 2.71)).length()\n        world.set_wheel_steer(entity, 0.9)\n        m = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if m is not None:\n            m.local_anchor_b = yope3d.Vec3(1.83, 0.9, 1.45)\n\n    def on_ui_press(self, world, entity):\n        world.attach_box_mesh(entity, yope3d.Vec3(1.34, 2.7, 2.11))\n        v = yope3d.to_degrees(4.23)\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat.from_euler(0.0, 0.05)\n        target = world.add_ui_button(yope3d.Vec2(4.62, 0.38), yope3d.Vec2(1.81, 4.4), yope3d.Vec4(1.84, 0.57, 1.03, 4.17))\n        cmp = yope3d.reg_add(entity, \"AudioSource\")\n        cmp.bus = 2\n        cmp.autoplay = True\n        cmp.pitch = 1.04\n        cmp.path = \"textures/tnail.png\"\n        cmp.loop = False\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.local_anchor_b = yope3d.Vec3(1.72, 1.2, 2.39)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.params_blob = \"player\"\n        world.wake(entity)\n        v = yope3d.",
   "suffix": "\n",
   "target": "play_music",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0039.py:38:36:KEY_F6",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0039.py",
   "line": 38,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth039Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.12},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Score: 0\"\n        wheel = world.add_kinematic_capsule(1.54, 0.54)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"crate\"\n        res = world.get_registry()\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.4, 2.68, 1.73)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.03, 2.91, 3.91)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.58, 1.34), yope3d.Vec2(1.62, 0.21), yope3d.Vec4(0.69, 0.66, 0.06, 0.99), \"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.46\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"player\"\n        t.script_class = \"hud_root\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_RIGHT_BRACKET):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 23.33, 0))\n        for ent, lights, conetw in yope3d.view(\"LightSource\", \"ConeTwistJointConstraint\"):\n            lights.color = yope3d.Vec3(0.02, 0.24, 1.28)\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.71)\n        body = world.add_capsule(0.5, 0.32, 13.11)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.52, 0.8, 1.61)\n        t.rotation = yope3d.Quat.from_euler(0.0, -0.97)\n        t.position = yope3d.Vec3(0.43, 0.38, 0.93)\n        h = world.get_pair_count()\n",
   "target": "KEY_F6",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0079.py:41:12:look_at",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0079.py",
   "line": 41,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth079Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.83},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_add(entity, \"Material\")\n        c.albedo_map = \"textures/metal_basecolor.png\"\n        c.metal_rough_map = \"textures/crate_mr.png\"\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.occlusion_map = \"textures/crate_ao.png\"\n        c.albedo = (0.02, 2.44, 3.27, 0.99)\n        yope3d.settings.remove(\"ground\")\n        world.attach_aabb_collider(entity, 70.19, yope3d.Vec3(0.7, 0.11, 0.19), True)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.45, 1.24, yope3d.EASE_QUAD_OUT)\n        cmp = yope3d.reg_add(entity, \"UIText\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.a = 1.31\n        world.wake(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_W):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.82, 0, 1.91) - origin).normalize())\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.fix_entity(other)\n        r = world.get_island_count()\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 2.59\n        t.text = \"Score: 0\"\n        t.g = 2.73\n        t.a = 0.14\n        origin = yope3d.get_position(entity)\n        q = yope3d.",
   "suffix": "        v = yope3d.reg_valid(other)\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.loop = True\n        c.bus = 4\n        if yope3d.input.is_key_pressed(yope3d.KEY_F9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.target = entity\n        c.rest_length = 0.81\n        c.k = 3.03\n",
   "target": "look_at",
   "rest": "((yope3d.Vec3(0.34, 0, 3.35) - origin).normalize())",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0050.py:15:52:EASE_QUAD_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0050.py",
   "line": 15,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth050Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.finalize_entity(entity, \"player\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.99)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.43, 0.71, yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.57, 0.33, 0.37)\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        r = yope3d.to_radians(7.0)\n        t = yope3d.reg_add(entity, \"TextLabel3D\")\n        t.g = 0.93\n        t.text = \"Paused\"\n        t.r = 0.47\n        t.a = 2.36\n",
   "target": "EASE_QUAD_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0005.py:16:8:clear_collision_observers",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0005.py",
   "line": 16,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth005Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.42},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_skybox([])\n        if yope3d.input.is_key_pressed(yope3d.KEY_1):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.reg_remove(entity, \"ConeTwistJointConstraint\")\n        yope3d.",
   "suffix": "        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.anchor = 3\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n        world.attach_box_mesh(entity, yope3d.Vec3(0.93, 3.05, 2.2))\n        h = world.add_vehicle(entity, [])\n\n    def on_ui_press(self, world, entity):\n",
   "target": "clear_collision_observers",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0080.py:61:36:KEY_ESCAPE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0080.py",
   "line": 61,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth080Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.27},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        res = world.get_pair_count()\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.r = 2.09\n        hull.g = 4.81\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.77)\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(0.87, 0.21, 3.94), yope3d.Vec3(1.5, 0.2, 3.53))\n        h = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        for ent, capsul, materi in yope3d.view(\"CapsuleForm\", \"Material\"):\n            capsul.radius = 0.58\n        hull = yope3d.reg_add(entity, \"Hull\")\n        hull.omega = yope3d.Vec3(1.23, 2.54, 4.67)\n        hull.linear_damping = 0.25\n        hull.angular_damping = 0.35\n        hull.friction = 0.25\n        hull.velocity = yope3d.Vec3(0.83, 0.45, 0.8)\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 1.93\n        v = yope3d.play_music(\"textures/tnail.png\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 2.88)\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.path = \"textures/tnail.png\"\n        hull = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if hull is not None:\n            hull.target = entity\n        v = yope3d.settings.get_str(\"hud_root\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.g = 0.43\n        hull.b = 2.29\n        hull.font = \"fonts/inter_ascii.msdf\"\n        hull.a = 1.27\n        if yope3d.input.is_key_pressed(yope3d.KEY_LEFT_CONTROL):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.pressed_b = 1.47\n        world.add_hinge_joint(entity, other, yope3d.Vec3(0.11, 2.11, 0.42), yope3d.Vec3(3.7, 0.09, 3.32), False, -1.02, -0.94, False)\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_get(entity, \"LightSource\")\n        if m is not None:\n            m.intensity = 3.79\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 17.03, 0))\n        t = yope3d.reg_add(entity, \"UIButton\")\n        t.normal_b = 1.41\n        t.normal_r = 2.95\n        t.hover_r = 3.19\n        t.normal_g = 2.06\n        t.normal_a = 0.71\n        yope3d.camera.set_fov(59.26)\n        m = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        m.local_anchor_a = yope3d.Vec3(4.22, 3.69, 3.42)\n",
   "target": "KEY_ESCAPE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0002.py:46:36:KEY_F8",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0002.py",
   "line": 46,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth002Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.87},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.settings.has(\"ground\")\n        hull = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_a = yope3d.Vec3(2.55, 2.96, 4.45)\n        form = yope3d.reg_get(entity, \"TextLabel3D\")\n        if form is not None:\n            form.font = \"fonts/inter_ascii.msdf\"\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 18.09, 0))\n        hull = yope3d.reg_get(entity, \"CompoundCollider\")\n        if hull is not None:\n            hull.loaded = True\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.direction = yope3d.Vec3(0.12, 0.53, 0.93)\n        for ent, materi, audios in yope3d.view(\"Material\", \"AudioSource\"):\n            materi.metal_rough_map = \"textures/crate_mr.png\"\n        hull = yope3d.reg_add(entity, \"PointJointConstraint\")\n        hull.local_anchor_b = yope3d.Vec3(3.62, 4.59, 2.45)\n        hull.local_anchor_a = yope3d.Vec3(4.79, 4.36, 0.26)\n        hull.target = entity\n        if yope3d.input.is_key_down(yope3d.KEY_R):\n            world.apply_impulse(entity, yope3d.Vec3(0, 39.37, 0))\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.28)\n\n    def init(self, world, entity, params):\n        for ent, name, textla in yope3d.view(\"Name\", \"TextLabel3D\"):\n            name.value = \"crate\"\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 11.54)\n        r = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 5.64, 0))\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.script_class = \"crate\"\n        cmp.params_blob = \"crate\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        h = world.ui_hovered()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.4)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 1, 2.08, yope3d.EASE_LINEAR)\n",
   "target": "KEY_F8",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0027.py:31:39:KEY_PERIOD",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0027.py",
   "line": 31,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth027Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.11},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_add(entity, \"UIText\")\n        c.b = 4.61\n        c.r = 4.91\n        v = yope3d.window.get_height()\n        v = yope3d.capsule_overlap(yope3d.Vec3(3.87, 1.32, 3.18), 1.27, 0.92)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.params_blob = \"player\"\n        c.script_class = \"hud_root\"\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n\n    def init(self, world, entity, params):\n        rot = yope3d.Quat.from_euler(0.46, -0.74)\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 1.79\n        hull.half_height = 0.83\n        target = world.add_point_light(yope3d.Vec3(1.64, 2.56, 0.01))\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.normal_b = 3.63\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        anchor = world.add_ui_text(\"Press E to interact\", \"Press E to interact\", yope3d.Vec2(3.5, 0.62), yope3d.Vec2(4.81, 4.79))\n\n    def on_ui_press(self, world, entity):\n        world.set_shadow_caster(entity)\n        cmp = yope3d.reg_add(entity, \"UIButton\")\n        cmp.normal_b = 1.13\n        cmp.normal_g = 2.87\n        cmp.hover_g = 1.99\n        v = yope3d.window.is_cursor_locked()\n",
   "target": "KEY_PERIOD",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0048.py:42:12:clamp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0048.py",
   "line": 42,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth048Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.55},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.load_game(\"player\")\n        cmp = yope3d.reg_add(entity, \"Parent\")\n        cmp.parent = entity\n        world.set_wheel_steer(entity, -0.33)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"hud_root\"\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.local_anchor_a = yope3d.Vec3(4.15, 1.25, 3.25)\n        prop = world.add_capsule(0.4, 0.29, 82.35, yope3d.Vec3(0.64, 3.8, 4.69))\n        world.remove_joint_between(prop, entity)\n\n    def init(self, world, entity, params):\n        form = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if form is not None:\n            form.swing_limit = 0.62\n        if yope3d.input.is_key_down(yope3d.KEY_P):\n            world.apply_impulse(entity, yope3d.Vec3(0, 15.66, 0))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.72)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.94, 2.58, yope3d.EASE_CUBIC_IN)\n        world.add_point_joint(entity, entity, yope3d.Vec3(4.23, 0.12, 2.8))\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_r = 1.9\n        c.hover_g = 4.97\n        c.normal_b = 2.11\n        c.normal_a = 3.61\n        v = yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(1.85, 4.9, 3.22)\n        c.local_anchor_b = yope3d.Vec3(3.8, 1.72, 4.1)\n        c.target = entity\n        v = yope3d.get_world_position(entity)\n        v = yope3d.settings.get_bool(\"crate\")\n        body = world.add_ui_text(\"Paused\", \"Score: 0\", yope3d.Vec2(3.89, 4.23), yope3d.Vec2(0.53, 4.09), 0)\n        v = yope3d.play_music(\"audios/impact.ogg\")\n\n    def on_ui_press(self, world, entity):\n",
   "target": "clamp",
   "rest": "(4.05, 1.88, 2.88)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:31:9:get_behavior",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 31,
   "col": 9,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.",
   "suffix": "    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "get_behavior",
   "rest": "(other, \"Health\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:63:13:attach_script",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 63,
   "col": 13,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.Vec3(0, 5, 0)\n\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))\n        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n        self.spawned += 1\n\n        x = self.pos.x + (self.spawned % 3 - 1) * 1.5\n        proj = world.add_sphere(1.0, 0.3, yope3d.Vec3(x, self.pos.y, self.pos.z))\n        world.attach_sphere_mesh(proj, 0.3, 0.3, 0.6, 1.0)\n\n        ok = yope3d.",
   "suffix": "            proj, \"behaviors.attach_script_demo\", \"SpawnedCounter\",\n            {\"tag\": self.spawned})\n        print(f\"[attach] spawned {proj} with SpawnedCounter -> {ok}\")\n",
   "target": "attach_script",
   "rest": "(",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0018.py:31:36:KEY_X",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0018.py",
   "line": 31,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth018Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.67},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.make_kinematic(entity)\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 0.97\n        yope3d.set_velocity(other, yope3d.Vec3(0.81, 0.91, 0.93))\n        target = world.add_capsule(1.63, 0.89, 42.14, yope3d.Vec3(2.79, 2.96, 1.05))\n        v = yope3d.scene_payload()\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.local_axis_b = yope3d.Vec3(3.37, 3.54, 0.12)\n        if yope3d.input.is_key_down(yope3d.KEY_1):\n            world.apply_impulse(entity, yope3d.Vec3(0, 2.74, 0))\n\n    def update(self, world, entity, dt):\n        v = yope3d.look_at(yope3d.Vec3(0.94, 3.56, 3.42))\n        v = yope3d.to_radians(0.03)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_a = 0.46\n        c.hover_g = 4.15\n        c.normal_r = 3.22\n        c.normal_g = 2.69\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 21.79, 0))\n        cmp = yope3d.reg_add(entity, \"AudioSource\")\n        cmp.autoplay = True\n        cmp.path = \"models/prop.glb\"\n        cmp.gain = 0.79\n        cmp.loop = False\n\n    def on_ui_press(self, world, entity):\n        e = world.add_point_light(yope3d.Vec3(3.31, 1.41, 2.15), yope3d.Vec3(3.39, 2.83, 1.84), 6.19)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n",
   "target": "KEY_X",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:27:31:raycast",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 27,
   "col": 31,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.",
   "suffix": "    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "raycast",
   "rest": "(o, d, 100.0)",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0039.py:34:39:KEY_RIGHT_BRACKET",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0039.py",
   "line": 34,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth039Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.12},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Score: 0\"\n        wheel = world.add_kinematic_capsule(1.54, 0.54)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"crate\"\n        res = world.get_registry()\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.4, 2.68, 1.73)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.03, 2.91, 3.91)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.58, 1.34), yope3d.Vec2(1.62, 0.21), yope3d.Vec4(0.69, 0.66, 0.06, 0.99), \"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.46\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"player\"\n        t.script_class = \"hud_root\"\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 23.33, 0))\n        for ent, lights, conetw in yope3d.view(\"LightSource\", \"ConeTwistJointConstraint\"):\n            lights.color = yope3d.Vec3(0.02, 0.24, 1.28)\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.71)\n        body = world.add_capsule(0.5, 0.32, 13.11)\n        t = yope3d.reg_add(entity, \"Transform\")\n",
   "target": "KEY_RIGHT_BRACKET",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0034.py:40:36:KEY_F4",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0034.py",
   "line": 40,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth034Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.23},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n        hull.a = 4.71\n        hull.b = 5\n        hull.font = \"fonts/inter_ascii.msdf\"\n        n = yope3d.Vec3(1.74, 0, 4.11).normalize()\n        v = yope3d.look_at(yope3d.Vec3(1.05, 0.8, 3.31))\n        world.attach_cylinder_collider(entity, 58.21, 1.19, 0.71)\n        m = yope3d.reg_get(entity, \"UIBackground\")\n        if m is not None:\n            m.b = 1.65\n        world.attach_sphere_mesh(entity, 1.79)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.25)\n        hull = yope3d.reg_add(entity, \"AABBForm\")\n        hull.extent = yope3d.Vec3(0.98, 0.82, 0.1)\n\n    def update(self, world, entity, dt):\n        world.attach_aabb_collider(entity, 7.66, yope3d.Vec3(1, 0.22, 0.16))\n        world.set_paused(False)\n        for ent, animat, uiback in yope3d.view(\"AnimationPlayer\", \"UIBackground\"):\n            animat.playing = 1\n        world.attach_capsule_collider(entity, 89.23, 0.71, 0.86, False)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.camera.get_forward()\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 0.39\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 12.11, 0))\n        world.set_ui_parent(other, entity)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.params_blob = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"AABBForm\")\n        hull.extent = yope3d.Vec3(0.15, 0.94, 0.41)\n        world.set_skybox([])\n        r = yope3d.to_radians(35.0)\n",
   "target": "KEY_F4",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0035.py:49:12:find_entity",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0035.py",
   "line": 49,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth035Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.48},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_skybox([])\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.99, 0.28, 0.29)\n        anchor = world.add_text_label_3d(\"Paused\", \"Paused\", yope3d.Vec3(1.22, 2.28, 3.32))\n        for ent, materi, cylind in yope3d.view(\"Material\", \"CylinderForm\"):\n            materi.emissive_map = \"textures/lamp_emissive.png\"\n        v = yope3d.find_entity(\"crate_01\")\n        world.reset_physics()\n        v = yope3d.settings.keys()\n        world.attach_obb_collider(anchor, 47.23, yope3d.Vec3(0.2, 0.78, 0.43))\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(0.31, 0, 1.93)).length()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.73, 0.34, yope3d.EASE_QUAD_IN)\n        yope3d.window.set_cursor_locked(False)\n        v = yope3d.capsule_cast(yope3d.Vec3(0.14, 3.43, 3.51), 1.97, 0.39, yope3d.Vec3(2.73, 3.32, 1.9), 0.05)\n        c = yope3d.reg_get(entity, \"UIBackground\")\n        if c is not None:\n            c.r = 1.85\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if c is not None:\n            c.time = 0.55\n        anchor = world.add_ui_background(yope3d.Vec2(0.42, 0.22), yope3d.Vec2(0.43, 0.37), yope3d.Vec4(4.6, 0.66, 4.21, 2.97))\n        if yope3d.input.is_key_down(yope3d.KEY_LEFT_SHIFT):\n            world.apply_impulse(entity, yope3d.Vec3(0, 12.84, 0))\n        c = yope3d.reg_get(entity, \"OBBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.56, 0.03, 0.64)\n        m = yope3d.reg_get(entity, \"UIBackground\")\n        if m is not None:\n            m.g = 3.67\n        v = yope3d.",
   "suffix": "        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"ground\"\n\n    def on_ui_press(self, world, entity):\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.upper_angle = -0.55\n        for ent, materi, cylind in yope3d.view(\"Material\", \"CylinderForm\"):\n            materi.emissive_map = \"textures/lamp_emissive.png\"\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.86, 0, 2.38) - origin).normalize())\n",
   "target": "find_entity",
   "rest": "(\"spawn_point\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0097.py:39:12:get_behavior",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0097.py",
   "line": 39,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth097Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.21},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        rot = yope3d.Quat.from_euler(0.19, 0.32)\n        for ent, uitext in yope3d.view(\"UITexturedBackground\"):\n            uitext.tint_b = 0.03\n        world.add_spring_with_proxies(entity, entity, 5, 0.1, 0, 0.74)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.06\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.44)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 1.43, yope3d.EASE_CUBIC_IN_OUT)\n        rot = yope3d.Quat.from_euler(-1.03, -0.32)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_twist_axis_a = yope3d.Vec3(0.45, 0.15, 0.46)\n        t.target = entity\n        t.local_anchor_a = yope3d.Vec3(4.97, 4.03, 0.09)\n        t.local_anchor_b = yope3d.Vec3(2.91, 0.68, 2.73)\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.metal_rough_map = \"textures/crate_mr.png\"\n        world.reset_physics()\n        target = world.add_point_light(yope3d.Vec3(0.15, 4.15, 0.41))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.17)\n        c = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if c is not None:\n            c.local_twist_axis_a = yope3d.Vec3(0.34, 0.91, 0.4)\n        v = yope3d.",
   "suffix": "\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"UITexturedBackground\")\n        t.tint_g = 0.97\n        t.tint_a = 0.84\n        t.tint_b = 0.49\n        t.path = \"models/prop.glb\"\n        t.tint_r = 0.22\n        c = yope3d.reg_add(entity, \"Transform\")\n        c.scale = yope3d.Vec3(1.52, 0.79, 0.58)\n",
   "target": "get_behavior",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0025.py:49:39:KEY_TAB",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0025.py",
   "line": 49,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth025Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.74},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        c = yope3d.reg_get(entity, \"Material\")\n        if c is not None:\n            c.metallic = 0.45\n        c = yope3d.reg_add(entity, \"CapsuleForm\")\n        c.radius = 0.5\n        c.half_height = 0.46\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.friction = 0.73\n        c.omega = yope3d.Vec3(4.36, 0.07, 4.78)\n        c.mass = 69.25\n        c.angular_damping = 0.16\n        c.velocity = yope3d.Vec3(0.54, 0.62, 0.27)\n        v = yope3d.audio.load_sound(\"textures/tnail.png\")\n        rot = yope3d.Quat.from_euler(0.98, -1.37)\n\n    def update(self, world, entity, dt):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        hull = yope3d.reg_get(entity, \"CapsuleForm\")\n        if hull is not None:\n            hull.radius = 0.44\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"crate\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.pitch = 1.17\n        e = world.add_kinematic_box(yope3d.Vec3(0.22, 1.68, 4.39), yope3d.Vec3(0.34, 0.44, 0.78))\n        yope3d.set_velocity(e, yope3d.Vec3(0.99, 0.86, 0.65))\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        yope3d.audio.set_master_gain(0.99)\n\n    def on_ui_press(self, world, entity):\n        prop = world.add_ui_button(yope3d.Vec2(1.63, 1.98), yope3d.Vec2(1.97, 2.65), yope3d.Vec4(1.63, 0.97, 1.71, 1.06), 0)\n        if yope3d.input.is_key_pressed(yope3d.KEY_UP):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        world.attach_cylinder_mesh(entity, 1.87, 1.14, 3.66, 1.68, 3.34)\n        world.remove_spring_between(entity, entity)\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_b = 0.85\n        target = world.add_sphere(1.58, 1.46)\n        if yope3d.input.is_key_down(yope3d.KEY_S):\n            world.apply_impulse(entity, yope3d.Vec3(0, 35.19, 0))\n\n",
   "target": "KEY_TAB",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0026.py:48:39:KEY_RIGHT_SHIFT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0026.py",
   "line": 48,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth026Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.15},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 3.64)\n        m = yope3d.reg_get(entity, \"UIText\")\n        if m is not None:\n            m.alignment = 0.72\n        yope3d.camera.set_position(yope3d.Vec3(3.55, 2.43, 0.4))\n        c = yope3d.reg_add(entity, \"CylinderForm\")\n        c.half_height = 0.7\n        c.radius = 0.53\n        yope3d.camera.set_rotation(yope3d.Vec3(2.24, 3.06, 3.45))\n        hull = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        hull.target = entity\n        hull.local_anchor_a = yope3d.Vec3(3.73, 4.86, 2.86)\n        hull.local_axis_a = yope3d.Vec3(1.51, 4.61, 1.26)\n\n    def update(self, world, entity, dt):\n        rot = yope3d.Quat.from_euler(0.38, 0.34)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.63)\n        for ent, parent in yope3d.view(\"Parent\"):\n            parent.parent = entity\n        r = yope3d.to_radians(170.0)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 27)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.06, 0, 3.47) - origin).normalize())\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.46, 0, 4.17) - origin).normalize())\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.94)\n\n    def init(self, world, entity, params):\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"player\"\n        h = world.ui_consumed_click()\n        world.apply_impulse_at(entity, yope3d.Vec3(9.05, 22.12, 31.93), yope3d.Vec3(0.44, 0.11, 0.56))\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.set_text(entity, \"Paused\")\n        hull = yope3d.reg_get(entity, \"AABBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.92, 0.09, 0.78)\n        yope3d.audio.resume_all()\n        res = world.get_contact_count()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.add_spring_with_proxies(other, entity, 4.79, 0.32, 1, 0.99)\n",
   "target": "KEY_RIGHT_SHIFT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0030.py:74:12:is_sleeping",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0030.py",
   "line": 74,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth030Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.43},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.09)\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.script_class = \"crate\"\n        for ent, sphere, cylind in yope3d.view(\"SphereForm\", \"CylinderForm\"):\n            sphere.radius = 1.75\n        target = world.add_kinematic_capsule(0.77, 0.22, yope3d.Vec3(2.18, 0.28, 4.7))\n        v = yope3d.is_sleeping(target)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.65)\n        world.wake(target)\n        yope3d.set_velocity(target, yope3d.Vec3(0.7, 0.93, 0.35))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 1.31\n        m.half_height = 1.03\n        world.apply_impulse(other, yope3d.Vec3(8.76, 17.61, 27.87))\n        anchor = world.add_ui_curved_background(yope3d.Vec2(3.99, 3.35), yope3d.Vec2(2.14, 2.13), yope3d.Vec4(0.91, 4.73, 4.99, 2.29))\n        form = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if form is not None:\n            form.tint_a = 0.17\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo_map = \"textures/crate_albedo.png\"\n        hull.albedo = (3.37, 4.67, 3.65, 3.32)\n        hull.normal_map = \"textures/crate_normal.png\"\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.path = \"textures/tnail.png\"\n        for ent, spring in yope3d.view(\"SpringConstraint\"):\n            spring.k = 3.3\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"SpringConstraint\")\n        m.target = entity\n        m.k = 1.31\n        m.rest_length = 0.23\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.r = 4.83\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (1.11, 0.69, 2.3, 1.03)\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        hull.albedo_map = \"textures/metal_basecolor.png\"\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_skybox([])\n        hull = yope3d.reg_get(entity, \"SpringConstraint\")\n        if hull is not None:\n            hull.rest_length = 0.81\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.26)\n        world.clear_shadow_caster()\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 12.93)\n        for ent, animat in yope3d.view(\"AnimationPlayer\"):\n            animat.loop = 1\n        v = yope3d.",
   "suffix": "        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.gain = 0.47\n\n",
   "target": "is_sleeping",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:8:10:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 8,
   "col": 10,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.",
   "suffix": "    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n",
   "target": "camera",
   "rest": ".position + yope3d.camera.get_forward() * 3.0",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0036.py:23:12:save_path",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0036.py",
   "line": 23,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth036Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.99},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        world.attach_aabb_collider(entity, 37.93, yope3d.Vec3(0.5, 0.3, 0.94))\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.type = 0.25\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 17.59)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.",
   "suffix": "        v = yope3d.clamp(3.58, 2.42, 1.24)\n        world.set_wheel_steer(entity, -0.54)\n        v = yope3d.capsule_cast(yope3d.Vec3(4.35, 2.7, 1.31), 1.48, 0.31, yope3d.Vec3(3.61, 1.03, 1.78), 1)\n        v = yope3d.settings.get_str(\"ground\")\n        body = world.add_ui_text(\"Paused\", \"Press E to interact\", yope3d.Vec2(0.76, 3.7), yope3d.Vec2(2.54, 0.25), 1)\n\n    def init(self, world, entity, params):\n        v = yope3d.settings.get_int(\"hud_root\")\n        anchor = world.add_cylinder(1.54, 0.42, 83.08)\n        r = yope3d.to_radians(101.0)\n",
   "target": "save_path",
   "rest": "(\"audios/impact.ogg\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0036.py:49:38:MOUSE_MIDDLE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0036.py",
   "line": 49,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth036Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.99},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        world.attach_aabb_collider(entity, 37.93, yope3d.Vec3(0.5, 0.3, 0.94))\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.type = 0.25\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 17.59)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_path(\"audios/impact.ogg\")\n        v = yope3d.clamp(3.58, 2.42, 1.24)\n        world.set_wheel_steer(entity, -0.54)\n        v = yope3d.capsule_cast(yope3d.Vec3(4.35, 2.7, 1.31), 1.48, 0.31, yope3d.Vec3(3.61, 1.03, 1.78), 1)\n        v = yope3d.settings.get_str(\"ground\")\n        body = world.add_ui_text(\"Paused\", \"Press E to interact\", yope3d.Vec2(0.76, 3.7), yope3d.Vec2(2.54, 0.25), 1)\n\n    def init(self, world, entity, params):\n        v = yope3d.settings.get_int(\"hud_root\")\n        anchor = world.add_cylinder(1.54, 0.42, 83.08)\n        r = yope3d.to_radians(101.0)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(3.01, 0, 2.92) - origin).normalize())\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 11.53)\n        t = yope3d.reg_add(entity, \"Name\")\n        t.value = \"hud_root\"\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"ground\"\n        t.script_class = \"ground\"\n        m = yope3d.reg_add(entity, \"CylinderForm\")\n        m.radius = 0.59\n        m.half_height = 0.76\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 29.42)\n        t = yope3d.clamp(dt * 10.31, 0.0, 1.0)\n        v = yope3d.lerp(3.27, 3.67, t)\n        m = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        m.local_anchor_a = yope3d.Vec3(3.41, 1.93, 0.39)\n        m.local_twist_axis_b = yope3d.Vec3(0.53, 0.85, 0.05)\n        m.swing_limit = 1.09\n        m.local_twist_axis_a = yope3d.Vec3(0.57, 0.59, 0.01)\n        m.target = entity\n",
   "target": "MOUSE_MIDDLE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:93:12:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 93,
   "col": 12,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.",
   "suffix": "            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n",
   "target": "Vec3",
   "rest": "( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0094.py:14:52:EASE_QUAD_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0094.py",
   "line": 14,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth094Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.25},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.36)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.34, 2.62, yope3d.",
   "suffix": "        world.attach_capsule_collider(entity, 73.1, 1.04, 1.15)\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.pitch = 0.92\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.angular_damping = 0.36\n        c.mass = 80.71\n        c.omega = yope3d.Vec3(1.25, 2.26, 3.6)\n        c.linear_damping = 0.11\n        c.velocity = yope3d.Vec3(0.26, 0.79, 0.68)\n        c = yope3d.reg_add(entity, \"CylinderForm\")\n",
   "target": "EASE_QUAD_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0028.py:32:36:KEY_EQUAL",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0028.py",
   "line": 32,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth028Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.76},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.half_height = 0.61\n        hull.radius = 0.51\n        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_a = 2\n        hull.hover_r = 4.12\n        hull.hover_g = 4.74\n        hull.normal_b = 1.6\n        prop = world.add_trigger_box(yope3d.Vec3(1.97, 0.37, 4.38), yope3d.Vec3(0.6, 0.43, 0.4))\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.56\n        cmp.tint_r = 0.62\n        cmp.tint_b = 0.73\n        cmp.path = \"audios/impact.ogg\"\n        m = yope3d.reg_get(entity, \"LightSource\")\n        if m is not None:\n            m.color = yope3d.Vec3(2.36, 1.24, 3.89)\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.lower_angle = -0.29\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 9.56, 0))\n\n    def update(self, world, entity, dt):\n        yope3d.set_velocity(entity, yope3d.Vec3(0.88, 0.76, 0.96))\n        if yope3d.input.is_key_pressed(yope3d.KEY_M):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.86)\n        world.set_ui_group_visible(entity, False)\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n",
   "target": "KEY_EQUAL",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:5:11:input",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 5,
   "col": 11,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.",
   "suffix": "    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n",
   "target": "input",
   "rest": "",
   "freq": 15,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0055.py:58:12:save_path",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0055.py",
   "line": 58,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth055Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.61},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.pause_all()\n        world.set_mesh_color(entity, 2.23, 1.26, 3.42)\n        h = world.get_contact_count()\n        world.apply_impulse(other, yope3d.Vec3(32.29, 35.75, 26.71))\n        for ent, materi, cylind in yope3d.view(\"Material\", \"CylinderForm\"):\n            materi.occlusion_map = \"textures/crate_ao.png\"\n\n    def init(self, world, entity, params):\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 1.22)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.96)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.4, 1.31, yope3d.EASE_CUBIC_IN)\n        h = world.ui_hit_test(4.17, 4.17)\n        world.set_wheel_steer(entity, 1.28)\n        hull = yope3d.reg_add(entity, \"Name\")\n        hull.value = \"hud_root\"\n        world.attach_capsule_mesh(entity, 0.51, 1.02)\n        hull = yope3d.reg_add(entity, \"AnimationPlayer\")\n        hull.time = 0.29\n        hull.clip = \"crate\"\n        hull.loop = 2\n        hull.speed = 7.51\n        hull.playing = 2\n\n    def update(self, world, entity, dt):\n        target = world.add_ui_curved_background(yope3d.Vec2(1.61, 2.48), yope3d.Vec2(3.63, 0.52), yope3d.Vec4(2.12, 1.33, 1.87, 0.21))\n        yope3d.draw_ray(yope3d.Vec3(1.1, 4.83, 1.84), yope3d.Vec3(3.37, 1.63, 0.69))\n        m = yope3d.reg_add(entity, \"SpringConstraint\")\n        m.rest_length = 0.97\n        m.target = entity\n        m.k = 2.01\n        res = world.get_ui_focus()\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.34)\n        hull = yope3d.reg_get(entity, \"TextLabel3D\")\n        if hull is not None:\n            hull.billboard = 1\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 20.82)\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 4.51\n        t.text = \"Paused\"\n        t.a = 2.64\n        yope3d.settings.set_bool(\"player\", False)\n        v = yope3d.",
   "suffix": "        v = yope3d.reg_has(entity, \"PointJointConstraint\")\n\n",
   "target": "save_path",
   "rest": "(\"audios/impact.ogg\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0111.py:17:36:KEY_F11",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0111.py",
   "line": 17,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth111Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.7},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        res = world.ui_hovered()\n        yope3d.settings.set_str(\"ground\", \"player\")\n        world.attach_capsule_mesh(entity, 1.37, 0.67)\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.lower_angle = -1.19\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 18.41, 0))\n        world.set_paused(True)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.add_spring(entity, entity, 2.65, 0.02)\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"models/prop.glb\"\n        c = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if c is not None:\n",
   "target": "KEY_F11",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0101.py:34:12:lerp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0101.py",
   "line": 34,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth101Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.23},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.39, 0.04, 0.39)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_a = 0.4\n        c.normal_r = 3.38\n        c.hover_g = 0.11\n        c.hover_r = 0.74\n        c.normal_b = 4.16\n        v = yope3d.reg_add(entity, 0.07)\n        c = yope3d.reg_get(entity, \"Hull\")\n        if c is not None:\n            c.gravity = False\n        world.apply_impulse(entity, yope3d.Vec3(33.96, 31.06, 32.55))\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.asset_path = \"audios/impact.ogg\"\n        m.loaded = False\n\n    def update(self, world, entity, dt):\n        v = yope3d.camera.screen_to_ray(2.94, 3.83)\n        world.attach_obb_collider(entity, 71.39, yope3d.Vec3(0.73, 0.9, 0.47))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.23)\n        t = yope3d.clamp(dt * 4.59, 0.0, 1.0)\n        v = yope3d.",
   "suffix": "        body = world.add_obb(yope3d.Vec3(0.03, 0.98, 0.52), 48.15)\n\n    def on_ui_press(self, world, entity):\n        world.attach_obb_collider(entity, 84.17, yope3d.Vec3(0.37, 0.03, 0.39), True)\n        r = yope3d.to_radians(116.0)\n        yope3d.set_position(entity, yope3d.Vec3(3.81, 3.95, 4.06))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.97)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.29, 2.47, yope3d.EASE_CUBIC_OUT)\n        if yope3d.input.is_key_pressed(yope3d.KEY_F8):\n",
   "target": "lerp",
   "rest": "(3.29, 2.5, t)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:151:14:input",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 151,
   "col": 14,
   "prefix": "springs would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.",
   "suffix": "\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n",
   "target": "input",
   "rest": "",
   "freq": 15,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:159:27:KEY_A",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 159,
   "col": 27,
   "prefix": "VISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.",
   "suffix": "        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n",
   "target": "KEY_A",
   "rest": "): steer_input += 1.0",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0085.py:36:39:KEY_BACKSLASH",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0085.py",
   "line": 36,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth085Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.52},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.get_behavior(other)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.95)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.71, 2.35, yope3d.EASE_QUAD_IN_OUT)\n        world.tween_ui_opacity(entity, 0.95, 2.63)\n        form = yope3d.reg_get(entity, \"Parent\")\n        if form is not None:\n            form.parent = entity\n        world.add_spring_with_proxies(entity, other, 0.67, 0.25, 1, 0.81)\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 0.94\n        wheel = world.add_kinematic_sphere(yope3d.Vec3(4.21, 1.12, 4.59), 0.26)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"player\"\n        c.params_blob = \"crate\"\n\n    def update(self, world, entity, dt):\n        for ent, uibutt, pointj in yope3d.view(\"UIButton\", \"PointJointConstraint\"):\n            uibutt.pressed_a = 2.97\n        if yope3d.input.is_key_down(yope3d.KEY_W):\n            world.apply_impulse(entity, yope3d.Vec3(0, 33.12, 0))\n        h = world.get_ui_focus()\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.target = entity\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        m.target = entity\n        m.local_axis_b = yope3d.Vec3(2.69, 0.32, 0.65)\n        m.local_anchor_a = yope3d.Vec3(3.29, 3.49, 4.27)\n        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.89, 0.1, 0.46)\n\n    def on_ui_press(self, world, entity):\n",
   "target": "KEY_BACKSLASH",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0036.py:16:39:KEY_CAPS_LOCK",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0036.py",
   "line": 16,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth036Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.99},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        world.attach_aabb_collider(entity, 37.93, yope3d.Vec3(0.5, 0.3, 0.94))\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.type = 0.25\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 17.59)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_path(\"audios/impact.ogg\")\n        v = yope3d.clamp(3.58, 2.42, 1.24)\n        world.set_wheel_steer(entity, -0.54)\n        v = yope3d.capsule_cast(yope3d.Vec3(4.35, 2.7, 1.31), 1.48, 0.31, yope3d.Vec3(3.61, 1.03, 1.78), 1)\n",
   "target": "KEY_CAPS_LOCK",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0077.py:42:8:load_scene",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0077.py",
   "line": 42,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth077Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.8},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_add(entity, \"Parent\")\n        hull.parent = entity\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.91\n        v = yope3d.window.get_cursor_pos()\n        world.remove_entity(entity)\n        v = yope3d.input.get_key_name(2)\n        yope3d.draw_line(yope3d.Vec3(1.28, 2.24, 0.25), yope3d.Vec3(3.27, 4.26, 1.1))\n        r = yope3d.to_radians(123.0)\n\n    def on_ui_press(self, world, entity):\n        e = world.add_capsule(0.5, 0.57, 64.24, yope3d.Vec3(3.27, 3.86, 3.82))\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.43)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.1)\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.48, 0.13, 0.97)\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.emissive_map = \"textures/lamp_emissive.png\"\n        body = world.add_kinematic_box(yope3d.Vec3(2.98, 2.79, 3.32), yope3d.Vec3(0.62, 0.81, 0.59))\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(0.67, 2.16, 3.27)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.44)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.35, 0.45, yope3d.EASE_QUAD_IN_OUT)\n        res = world.get_ui_focus()\n        world.attach_sphere_collider(entity, 39.29, 0.4, True)\n        yope3d.",
   "suffix": "        world.set_mesh_color(entity, 2.73, 1.34, 2.43)\n\n    def init(self, world, entity, params):\n        n = yope3d.Vec3(3.36, 0, 4.15).normalize()\n        v = yope3d.input.is_mmb_down()\n        world.remove_light(entity)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 11.31)\n        m = yope3d.reg_get(entity, \"SpringConstraint\")\n",
   "target": "load_scene",
   "rest": "(\"scenes/menu.yscene\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:94:12:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 94,
   "col": 12,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.",
   "suffix": "            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n",
   "target": "Vec3",
   "rest": "(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0013.py:49:8:settings",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0013.py",
   "line": 49,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth013Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.43},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.attach_cylinder_collider(entity, 72.29, 0.55, 0.69, True)\n        for ent, parent, hull in yope3d.view(\"Parent\", \"Hull\"):\n            parent.parent = entity\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.29)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.78, 1.33, yope3d.EASE_QUAD_IN)\n        m = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.28, 3.62, 2.73)\n        m.swing_limit = 0.88\n        m.local_twist_axis_b = yope3d.Vec3(0.38, 0.98, 0.06)\n        m.local_anchor_a = yope3d.Vec3(1.29, 2.65, 0.52)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.98)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.8, 1.9, yope3d.EASE_QUAD_IN)\n        wheel = world.add_static_aabb(yope3d.Vec3(1.77, 1.68, 0.49), yope3d.Vec3(0.25, 0.34, 0.87))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.71)\n        for ent, compou, uiback in yope3d.view(\"CompoundCollider\", \"UIBackground\"):\n            compou.loaded = False\n        world.apply_impulse(other, yope3d.Vec3(9.26, 12.81, 22.87))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 4.19)\n\n    def on_ui_press(self, world, entity):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 0.54\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Paused\"\n        yope3d.set_text(entity, \"Paused\")\n        for ent, animat in yope3d.view(\"AnimationPlayer\"):\n            animat.time = 0.66\n\n    def update(self, world, entity, dt):\n        target = world.add_trigger_box(yope3d.Vec3(4.12, 0.82, 0.57), yope3d.Vec3(0.54, 0.96, 0.28))\n        yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(4.76, 0.19, 0.31)\n        c.target = entity\n        c.local_twist_axis_a = yope3d.Vec3(0.87, 0.45, 0.91)\n        c.swing_limit = 0.49\n        c.local_twist_axis_b = yope3d.Vec3(0.4, 0.94, 0.43)\n        h = world.get_hull_count()\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"crate\"\n\n",
   "target": "settings",
   "rest": ".set_str(\"hud_root\", \"crate\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:32:4:attach_script",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 32,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.",
   "suffix": "    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "attach_script",
   "rest": "(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0045.py:52:12:find_entity",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0045.py",
   "line": 52,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth045Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.48},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_key_pressed(yope3d.KEY_B):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.params_blob = \"player\"\n        v = yope3d.window.get_tab_pauses()\n        yope3d.reg_remove(entity, \"OBBForm\")\n        world.attach_obb_collider(other, 61.98, yope3d.Vec3(0.21, 0.32, 0.41))\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.normal_map = \"textures/crate_normal.png\"\n        v = yope3d.reg_get(other, 2.69)\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.settings.get_str(\"player\")\n        world.attach_sphere_collider(entity, 80.39, 0.34, False)\n        world.fix_entity(entity)\n        m = yope3d.reg_get(entity, \"CylinderForm\")\n        if m is not None:\n            m.half_height = 0.78\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.b = 4.53\n        hull.g = 0.85\n        hull.a = 4.27\n\n    def init(self, world, entity, params):\n        n = yope3d.Vec3(1.91, 0, 1.99).normalize()\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        if yope3d.input.is_key_down(yope3d.KEY_SLASH):\n            world.apply_impulse(entity, yope3d.Vec3(0, 16.89, 0))\n        yope3d.set_text(entity, \"Score: 0\")\n        e = world.add_obb(yope3d.Vec3(0.81, 0.13, 0.94), 40.82, yope3d.Vec3(3.1, 4.43, 1.9))\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(3.88, 0, 3.51)).length()\n        v = yope3d.look_at(yope3d.Vec3(4.15, 0.71, 1.89))\n        m = yope3d.reg_get(entity, \"Parent\")\n        if m is not None:\n            m.parent = entity\n\n    def update(self, world, entity, dt):\n        v = yope3d.",
   "suffix": "        yope3d.audio.set_bus_gain(2, 0.84)\n        for ent, hull in yope3d.view(\"Hull\"):\n            hull.collision_layer = 4\n        world.apply_impulse_at(entity, yope3d.Vec3(8.06, 3.72, 3.09), yope3d.Vec3(0.97, 0.56, 0.46))\n        m = yope3d.reg_add(entity, \"AnimationPlayer\")\n        m.speed = 1.02\n        m.playing = 0\n        m.time = 0\n        m.clip = \"crate\"\n        m.loop = 3\n",
   "target": "find_entity",
   "rest": "(\"spawn_point\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:16:11:attach_script",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 16,
   "col": 11,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.",
   "suffix": "independent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n",
   "target": "attach_script",
   "rest": " (module, class_name, params -> init() dict),",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0059.py:13:39:KEY_INSERT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0059.py",
   "line": 13,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth059Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        v = yope3d.reg_get(entity, 4.56)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_a = yope3d.Vec3(3.24, 0.87, 4.06)\n        m.target = entity\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 0.43\n        hull.half_height = 0.58\n        for ent, pointj, conetw in yope3d.view(\"PointJointConstraint\", \"ConeTwistJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(1.51, 2.65, 3.74)\n        v = yope3d.to_radians(1.56)\n",
   "target": "KEY_INSERT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:11:9:get_behavior",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 11,
   "col": 9,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.",
   "suffix": "    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n",
   "target": "get_behavior",
   "rest": "(other)         # other entity's live instance",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:275:37:KEY_V",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 275,
   "col": 37,
   "prefix": "        grid = [[None] * GRID_N for _ in range(GRID_N)]\n        half = (NODE_HALF, NODE_HALF, NODE_HALF)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N):\n                if horizontal:\n                    pos = (-half_w + i * GRID_STEP, top_y, -half_w + j * GRID_STEP)\n                else:\n                    pos = (-half_w + i * GRID_STEP, top_y - j * GRID_STEP, 0.0)\n\n                fi = i / (GRID_N - 1)\n                fj = j / (GRID_N - 1)\n\n                if shape == 0:\n                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.audio.load_sound(\"audios/test.ogg\")\n        if buf is not None:\n            self.doppler_src = yope3d.audio.create_source(buf)\n            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n        self.self_collide = bool(params.get(\"cloth_self_collision\", False))\n        self.cloth_nodes = []\n        self.prev_v = False\n        GalleryBase.init(self, world, entity, params)\n\n    def on_update(self, world, dt):\n        v = yope3d.input.is_key_down(yope3d.",
   "suffix": "        if v and not self.prev_v and self.cloth_nodes:\n            self.self_collide = not self.self_collide\n            self.apply_cloth_filter(world)\n            self.refresh_stats(world)\n        self.prev_v = v\n\n        # The audio source is not an entity, so it needs a manual per-frame sync of\n        # position + velocity (velocity is what OpenAL derives the Doppler shift from).\n        src = getattr(self, \"doppler_src\", None)\n        ent = getattr(self, \"doppler_ent\", None)\n",
   "target": "KEY_V",
   "rest": ")",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:319:14:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 319,
   "col": 14,
   "prefix": "        # launched across the level.\n        self.plane_floor(world)\n        self.static_box(world, (0, 3, 0), (2.0, 2.0, 2.0), BOX_PURPLE)\n        self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_fast(self, world):\n        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.",
   "suffix": "        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:83:23:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 83,
   "col": 23,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.",
   "suffix": "        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n",
   "target": "Vec3",
   "rest": "(1.0, 0.4, 2.0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0000.py:30:12:get_world_position",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0000.py",
   "line": 30,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth000Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.52},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uibutt, uitext in yope3d.view(\"UIButton\", \"UITexturedBackground\"):\n            uibutt.disabled_r = 3.01\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(0.6, 0, 0.57)).length()\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -1.1)\n        t = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        t.local_anchor_b = yope3d.Vec3(0.74, 2.63, 2.73)\n        t.local_axis_b = yope3d.Vec3(3.31, 4.34, 3.45)\n        m = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        m.local_twist_axis_a = yope3d.Vec3(0.22, 0.56, 0.09)\n        m.local_anchor_b = yope3d.Vec3(0.89, 4.76, 3.73)\n        res = world.get_island_count()\n        m = yope3d.reg_add(entity, \"AABBForm\")\n        m.extent = yope3d.Vec3(0.01, 0.47, 0.88)\n        v = yope3d.look_at(yope3d.Vec3(0.84, 0.6, 4.52))\n\n    def update(self, world, entity, dt):\n        world.tween_ui_opacity(entity, 0.4, 1.16)\n        v = yope3d.",
   "suffix": "        yope3d.draw_line(yope3d.Vec3(0.26, 4.03, 4.43), yope3d.Vec3(1.87, 4.5, 4.17))\n        h = world.ui_hit_test(1.44, 3.31)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.58)\n        t = yope3d.reg_add(entity, \"UITransform\")\n        t.depth = 2\n        t.min_y = 0.49\n        t.min_x = 4.64\n        t.visible = True\n        t.max_y = 3.07\n        v = yope3d.reg_get(entity, 2.31)\n",
   "target": "get_world_position",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:84:20:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 84,
   "col": 20,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.",
   "suffix": "\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n",
   "target": "Vec3",
   "rest": "(0.0, spawn_y, 0.0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0071.py:66:39:KEY_APOSTROPHE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0071.py",
   "line": 66,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth071Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.17},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.find_entity(\"crate_01\")\n        v = yope3d.capsule_cast(yope3d.Vec3(2.12, 4.5, 1.75), 0.9, 1.06, yope3d.Vec3(1.17, 4.6, 4.96), 0.16)\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_b = yope3d.Vec3(4.82, 3.63, 4.67)\n        cmp.local_twist_axis_a = yope3d.Vec3(0.97, 0.16, 0.96)\n        cmp.local_anchor_a = yope3d.Vec3(3.82, 3.15, 1.88)\n        anchor = world.add_obb(yope3d.Vec3(0.42, 0.53, 0.29), 31.31)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"ground\"\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.7, 0.35, 0.98)\n        c = yope3d.reg_add(entity, \"UIText\")\n        c.g = 1.65\n        c.a = 1.3\n        c.text = \"Paused\"\n        c.font = \"fonts/inter_ascii.msdf\"\n        c.b = 2.77\n        c = yope3d.reg_get(entity, \"UITransform\")\n        if c is not None:\n            c.size_mode = 2\n        m = yope3d.reg_get(entity, \"AudioSource\")\n        if m is not None:\n            m.bus = 4\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.rest_length = 0.06\n        hull.target = entity\n        hull.k = 2.01\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        t = yope3d.reg_add(entity, \"UITransform\")\n        t.min_x = 0.57\n        t.depth = 1\n        t.min_y = 2.84\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.direction = yope3d.Vec3(0.47, 0.91, 0.45)\n        other = world.add_ui_background(yope3d.Vec2(2.78, 0.71), yope3d.Vec2(2.34, 4.5), yope3d.Vec4(1.45, 4.44, 4.91, 0.74))\n        wheel = world.add_trigger_sphere(yope3d.Vec3(0.41, 2.54, 0.95), 0.96)\n        yope3d.audio.pause_all()\n\n    def init(self, world, entity, params):\n        v = yope3d.audio.create_source(entity)\n        res = world.attach_animation(entity, \"models/prop.glb\")\n        yope3d.set_velocity(entity, yope3d.Vec3(0.3, 0.92, 0.1))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.79)\n        yope3d.camera.set_clip_planes(2.74, 1.04)\n        hull = yope3d.reg_add(entity, \"ScriptComponent\")\n        hull.params_blob = \"player\"\n        hull.script_class = \"player\"\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_down(yope3d.KEY_LEFT_CONTROL):\n            world.apply_impulse(entity, yope3d.Vec3(0, 22.1, 0))\n\n",
   "target": "KEY_APOSTROPHE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:7:43:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 7,
   "col": 43,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.",
   "suffix": "    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n",
   "target": "Vec3",
   "rest": "(cy, 0, -sy)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0008.py:61:39:KEY_F5",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0008.py",
   "line": 61,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth008Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.4},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.set_wheel_drive(entity, 0.8)\n        v = yope3d.capsule_cast(yope3d.Vec3(4.15, 3.77, 0.27), 0.98, 1.18, yope3d.Vec3(1.41, 0.55, 0.7), 0.22)\n        cmp = yope3d.reg_add(entity, \"UIBackground\")\n        cmp.a = 3.56\n        cmp.r = 1.92\n        target = world.add_ui_background(yope3d.Vec2(1.8, 1.82), yope3d.Vec2(1.26, 1.03), yope3d.Vec4(1.78, 3.23, 3.13, 3.89))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 5.66)\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.albedo = (4.44, 1.7, 1.34, 4.29)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 28.38)\n        v = yope3d.camera.get_forward()\n\n    def update(self, world, entity, dt):\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(1.02, 0, 0.96)).length()\n        for ent, animat, textla in yope3d.view(\"AnimationPlayer\", \"TextLabel3D\"):\n            animat.time = 0.6\n        c = yope3d.reg_get(entity, \"UIBackground\")\n        if c is not None:\n            c.a = 4.37\n        world.finalize_entity(entity, \"player\")\n        v = yope3d.view()\n        c = yope3d.reg_add(entity, \"CapsuleForm\")\n        c.half_height = 0.22\n        c.radius = 1.01\n        if yope3d.input.is_key_down(yope3d.KEY_LEFT_CONTROL):\n            world.apply_impulse(entity, yope3d.Vec3(0, 9.72, 0))\n\n    def on_ui_press(self, world, entity):\n        world.attach_cylinder_mesh(entity, 1.97, 1.03)\n        yope3d.camera.set_rotation(yope3d.Vec3(0.26, 0.88, 2.59))\n        for ent, lights, hull in yope3d.view(\"LightSource\", \"Hull\"):\n            lights.casts_shadow = False\n        other = world.add_kinematic_capsule(1.71, 0.48)\n        prop = world.add_ui_button(yope3d.Vec2(0.01, 4.18), yope3d.Vec2(3.45, 3.92), yope3d.Vec4(1.32, 3.33, 1.34, 2.73), 2)\n        body = world.add_trigger_sphere(yope3d.Vec3(0.21, 2.24, 1.91), 1.67)\n        t = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        t.limit_enabled = True\n        t.target = entity\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.target = entity\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.97, 0, 2.33)).length()\n        body = world.add_ui_button(yope3d.Vec2(0.09, 4.73), yope3d.Vec2(2.57, 4.36), yope3d.Vec4(1.21, 2.51, 3.97, 3.14), 2)\n        m = yope3d.reg_add(entity, \"CylinderForm\")\n        m.half_height = 0.63\n        m.radius = 0.91\n        n = yope3d.Vec3(2.51, 0, 0.17).normalize()\n        yope3d.audio.fade_gain(entity, 0.52, 1.68)\n        wheel = world.add_ui_text(\"Paused\", \"Score: 0\", yope3d.Vec2(0.19, 0.31), yope3d.Vec2(0.67, 4.11))\n",
   "target": "KEY_F5",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0069.py:51:12:reg_valid",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0069.py",
   "line": 51,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth069Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.2},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.63, 0.4, 0.46)\n        v = yope3d.input.is_key_pressed(1)\n        e = world.add_ui_curved_background(yope3d.Vec2(0.38, 1.04), yope3d.Vec2(2.12, 2.93), yope3d.Vec4(2.65, 3.65, 1.28, 2.38), 0.03, 4)\n        for ent, conetw, textla in yope3d.view(\"ConeTwistJointConstraint\", \"TextLabel3D\"):\n            conetw.local_twist_axis_a = yope3d.Vec3(0.71, 0.28, 0.89)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.01)\n        world.set_ui_group_visible(other, False)\n        yope3d.camera.set_rotation(yope3d.Vec3(0.46, 2.22, 0.96))\n        hull = yope3d.reg_add(entity, \"CompoundCollider\")\n        hull.loaded = True\n        hull.asset_path = \"audios/impact.ogg\"\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"Name\")\n        if hull is not None:\n            hull.value = \"hud_root\"\n        world.attach_cylinder_mesh(entity, 1.25, 0.53)\n        res = world.ui_hit_test(1.65, 3.77)\n        form = yope3d.reg_get(entity, \"UIText\")\n        if form is not None:\n            form.g = 0.01\n\n    def on_ui_press(self, world, entity):\n        wheel = world.add_ui_curved_background(yope3d.Vec2(3.61, 1.42), yope3d.Vec2(3.6, 4.38), yope3d.Vec4(4.83, 1.78, 1.24, 4.73), 0.92, 1)\n        h = world.ui_consumed_click()\n        v = yope3d.reg_add(entity, 3.41)\n        hull = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        hull.swing_limit = 1.22\n        hull.local_twist_axis_a = yope3d.Vec3(0.48, 0.01, 0.89)\n\n    def update(self, world, entity, dt):\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.upper_angle = -0.7\n        for ent, cylind, transf in yope3d.view(\"CylinderForm\", \"Transform\"):\n            cylind.half_height = 0.84\n        hull = yope3d.reg_get(entity, \"UIText\")\n        if hull is not None:\n            hull.g = 4.69\n        v = yope3d.",
   "suffix": "        rot = yope3d.Quat.from_euler(-0.77, -0.47)\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.21)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.73)\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.velocity = yope3d.Vec3(0.43, 0.33, 0.46)\n        t.omega = yope3d.Vec3(2.71, 3.46, 2.14)\n        t.friction = 0.43\n        t.angular_damping = 0.03\n        t.linear_damping = 0.13\n\n",
   "target": "reg_valid",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0109.py:12:8:load_scene",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0109.py",
   "line": 12,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth109Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        yope3d.",
   "suffix": "        for ent, materi in yope3d.view(\"Material\"):\n            materi.normal_scale = 1.84\n        for ent, cylind, aabbfo in yope3d.view(\"CylinderForm\", \"AABBForm\"):\n            cylind.half_height = 1.1\n        for ent, spring, audios in yope3d.view(\"SpringConstraint\", \"AudioSource\"):\n            spring.rest_length = 0.24\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.lower_angle = -1.11\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.94)\n",
   "target": "load_scene",
   "rest": "(\"scenes/menu.yscene\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0010.py:60:36:KEY_C",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0010.py",
   "line": 60,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth010Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.26},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"UIButton\")\n        t.normal_a = 1.32\n        t.normal_r = 0.59\n        t.hover_g = 0.59\n        t.normal_b = 2.73\n        t.hover_r = 3.26\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.autoplay = False\n        r = world.attach_animation(entity, \"textures/tnail.png\")\n        r = world.attach_animation(entity, \"audios/impact.ogg\")\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.metallic = 0.88\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"AABBForm\")\n        hull.extent = yope3d.Vec3(0.46, 0.29, 0.03)\n        v = yope3d.settings.get_float(\"hud_root\")\n        m = yope3d.reg_get(entity, \"SphereForm\")\n        if m is not None:\n            m.radius = 0.82\n        world.reset_physics()\n        for ent, hingej, animat in yope3d.view(\"HingeJointConstraint\", \"AnimationPlayer\"):\n            hingej.limit_enabled = True\n        rot = yope3d.Quat.from_euler(-0.84, 0.05)\n        m = yope3d.reg_get(entity, \"UIButton\")\n        if m is not None:\n            m.enabled = True\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.05, 0.88, 0.74)\n\n    def update(self, world, entity, dt):\n        wheel = world.add_ui_button(yope3d.Vec2(2.73, 4.81), yope3d.Vec2(2.77, 3.99), yope3d.Vec4(4.6, 1.92, 1.31, 3.72), 1)\n        world.remove_spring_between(entity, entity)\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.position = yope3d.Vec3(0.71, 0.4, 0.8)\n        v = yope3d.settings.get_int(\"hud_root\")\n        world.attach_aabb_collider(wheel, 83.16, yope3d.Vec3(0.21, 0.02, 0.06), False)\n\n    def init(self, world, entity, params):\n        yope3d.reg_remove(entity, \"Fixed\")\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.36, 0.16, 0.36)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.28)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.55, yope3d.EASE_QUAD_OUT)\n        if yope3d.input.is_key_pressed(yope3d.KEY_EQUAL):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 34.87, 0))\n\n",
   "target": "KEY_C",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0087.py:32:12:is_sleeping",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0087.py",
   "line": 32,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth087Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.62},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        for ent, animat, capsul in yope3d.view(\"AnimationPlayer\", \"CapsuleForm\"):\n            animat.loop = 1\n        anchor = world.add_aabb(yope3d.Vec3(0.87, 0.55, 0.77), 14.59)\n        v = yope3d.input.is_forward_mb_down()\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.hover_a = 1.2\n        m = yope3d.reg_get(entity, \"OBBForm\")\n        if m is not None:\n            m.extent = yope3d.Vec3(0.18, 0.65, 0.26)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_key_pressed(yope3d.KEY_Q):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, conetw, animat in yope3d.view(\"ConeTwistJointConstraint\", \"AnimationPlayer\"):\n            conetw.twist_limit = 0.38\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"hud_root\"\n        world.attach_cylinder_mesh(other, 1.85, 0.24)\n\n    def init(self, world, entity, params):\n        v = yope3d.",
   "suffix": "        v = yope3d.view()\n        target = world.add_point_light(yope3d.Vec3(2.55, 0.43, 4.99), yope3d.Vec3(2.95, 2.51, 3.36), 4.45)\n        for ent, parent in yope3d.view(\"Parent\"):\n            parent.parent = entity\n        hull = yope3d.reg_get(entity, \"SphereForm\")\n        if hull is not None:\n            hull.radius = 0.8\n        yope3d.audio.set_bus_gain(4, 0.51)\n\n    def on_ui_press(self, world, entity):\n",
   "target": "is_sleeping",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0029.py:35:39:KEY_SEMICOLON",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0029.py",
   "line": 35,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth029Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.98},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"SphereForm\")\n        if form is not None:\n            form.radius = 1.05\n        v = yope3d.input.is_lmb_down()\n        v = yope3d.window.is_cursor_locked()\n        yope3d.settings.set_int(\"ground\", 2)\n        v = yope3d.get_behavior(entity)\n        rot = yope3d.Quat.from_euler(0.5, -0.08)\n        world.attach_cylinder_mesh(entity, 1.27, 1.11)\n        v = yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.43)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.39, 1.01, yope3d.EASE_QUAD_IN)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"hud_root\"\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 3.14\n        c.a = 3.23\n        c.g = 1.17\n        c.r = 1.44\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.98)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 0.34\n\n    def on_ui_press(self, world, entity):\n        if yope3d.input.is_key_pressed(yope3d.KEY_7):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        prop = world.add_kinematic_capsule(1.31, 1.04)\n",
   "target": "KEY_SEMICOLON",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0042.py:24:8:load_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0042.py",
   "line": 24,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth042Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.64},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.min_y = 4.8\n        m.max_x = 4.39\n        m.depth = 4\n        m.visible = True\n        v = yope3d.to_degrees(4.96)\n        for ent, lights, capsul in yope3d.view(\"LightSource\", \"CapsuleForm\"):\n            lights.quadratic = 0.84\n        v = yope3d.input.get_cursor_pos()\n        v = yope3d.settings.keys()\n\n    def update(self, world, entity, dt):\n        yope3d.",
   "suffix": "        yope3d.settings.set_float(\"crate\", 1.46)\n        c = yope3d.reg_get(entity, \"UIText\")\n        if c is not None:\n            c.font = \"fonts/inter_ascii.msdf\"\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_b = yope3d.Vec3(0.16, 1.39, 2.75)\n        cmp.local_anchor_a = yope3d.Vec3(4.91, 0.02, 1.89)\n        cmp.target = entity\n        v = yope3d.reg_add(entity, 1.65)\n        form = yope3d.reg_get(entity, \"PointJointConstraint\")\n",
   "target": "load_game",
   "rest": "(\"spawn_point\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0073.py:48:52:EASE_CUBIC_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0073.py",
   "line": 48,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth073Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.55},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, spring in yope3d.view(\"SpringConstraint\"):\n            spring.rest_length = 0.07\n        yope3d.set_profile_scene(\"scenes/level_01.yscene\")\n        h = world.ui_hovered()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.37)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.32, 2.5, yope3d.EASE_QUAD_IN)\n        n = yope3d.Vec3(1.89, 0, 4.46).normalize()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.49)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 1, 2.6, yope3d.EASE_CUBIC_IN_OUT)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        body = world.add_kinematic_sphere(yope3d.Vec3(4.72, 4.64, 2.93), 0.71)\n        form = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if form is not None:\n            form.playing = 1\n        c = yope3d.reg_get(entity, \"Hull\")\n        if c is not None:\n            c.mass = 31.83\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.tint_a = 0.64\n        hull.path = \"audios/impact.ogg\"\n        hull.has_texture = False\n        hull.tint_g = 0.17\n        hull.tint_r = 0.9\n        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 0.89\n        prop = world.add_sphere(39.46, 1.99, yope3d.Vec3(1.43, 4.77, 0.58))\n        res = world.ui_consumed_click()\n\n    def init(self, world, entity, params):\n        world.attach_obb_collider(entity, 82.12, yope3d.Vec3(0.25, 0.28, 0.09))\n        v = yope3d.reg_has(entity, \"CapsuleForm\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.52)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 2.65, yope3d.",
   "suffix": "        yope3d.audio.resume_all()\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.casts_shadow = False\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.params_blob = \"player\"\n        cmp.script_class = \"player\"\n        for ent, cylind, aabbfo in yope3d.view(\"CylinderForm\", \"AABBForm\"):\n",
   "target": "EASE_CUBIC_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0105.py:16:12:lerp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0105.py",
   "line": 16,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth105Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.69},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.37\n        t = yope3d.clamp(dt * 2.01, 0.0, 1.0)\n        v = yope3d.",
   "suffix": "        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 1.63\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.r = 2.72\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.37, 3.58), yope3d.Vec2(4.04, 3.48), yope3d.Vec4(0.95, 0.01, 0.66, 0.92), \"audios/impact.ogg\")\n        yope3d.window.set_f11_fullscreen(True)\n        world.set_mesh_color(entity, 0.69, 0.67, 1.35)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.2\n\n",
   "target": "lerp",
   "rest": "(4.44, 4.16, t)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:333:13:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 333,
   "col": 13,
   "prefix": "    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(20.0))\n        rb = yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_rest(self, world):\n        self.plane_floor(world)\n        self.obb(world, H, 1.0, (0, 0.5, 0), OBB_A)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(-20.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0065.py:17:16:spawn",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0065.py",
   "line": 17,
   "col": 16,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth065Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.68},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.hover_g = 1.54\n        v = yope3d.is_fixed(other)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = True\n        wheel = yope3d.",
   "suffix": "        world.remove_entity(wheel)\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.87, 0.75, 0.01)\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 19.85)\n        world.remove_light(entity)\n        c = yope3d.reg_get(entity, \"Hull\")\n",
   "target": "spawn",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0018.py:20:36:KEY_1",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0018.py",
   "line": 20,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth018Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.67},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.make_kinematic(entity)\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 0.97\n        yope3d.set_velocity(other, yope3d.Vec3(0.81, 0.91, 0.93))\n        target = world.add_capsule(1.63, 0.89, 42.14, yope3d.Vec3(2.79, 2.96, 1.05))\n        v = yope3d.scene_payload()\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.local_axis_b = yope3d.Vec3(3.37, 3.54, 0.12)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 2.74, 0))\n\n    def update(self, world, entity, dt):\n        v = yope3d.look_at(yope3d.Vec3(0.94, 3.56, 3.42))\n        v = yope3d.to_radians(0.03)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_a = 0.46\n        c.hover_g = 4.15\n        c.normal_r = 3.22\n        c.normal_g = 2.69\n",
   "target": "KEY_1",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0108.py:65:52:EASE_QUAD_IN",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0108.py",
   "line": 65,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth108Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.95},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(0.42, 0.64, 2.62)\n        cmp.swing_limit = 0.88\n        cmp.local_twist_axis_b = yope3d.Vec3(0.6, 0.44, 0.25)\n        cmp.local_anchor_b = yope3d.Vec3(3.66, 1.59, 3.24)\n        world.remove_joint_between(entity, entity)\n        yope3d.set_velocity(entity, yope3d.Vec3(0.1, 0.71, 0.65))\n        v = yope3d.save_path(\"textures/tnail.png\")\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.depth = 2\n        e = world.add_ui_curved_background(yope3d.Vec2(3.52, 4.54), yope3d.Vec2(2.69, 1.98), yope3d.Vec4(0.95, 0, 3.14, 2.53))\n\n    def init(self, world, entity, params):\n        world.make_kinematic(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_E):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.35, 1.3, 1.06)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.47, 1.27, 0.72)\n        for ent, script, uitext in yope3d.view(\"ScriptComponent\", \"UITexturedBackground\"):\n            script.params_blob = \"crate\"\n        v = yope3d.scene_payload()\n        h = world.get_contact_count()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        body = world.add_ui_curved_background(yope3d.Vec2(3.43, 1.85), yope3d.Vec2(0.33, 4), yope3d.Vec4(1.53, 3.1, 2.23, 0.93), 0.35, 4)\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"textures/tnail.png\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.99)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.58, 2.78, yope3d.EASE_QUAD_IN_OUT)\n        hull = yope3d.reg_get(entity, \"SpringConstraint\")\n        if hull is not None:\n            hull.k = 3.71\n        v = yope3d.capsule_cast(yope3d.Vec3(3.3, 0.75, 4.27), 0.45, 0.91, yope3d.Vec3(1.81, 0.55, 2.32), 0.78)\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_get(entity, \"SphereForm\")\n        if c is not None:\n            c.radius = 1.14\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.visible = True\n        for ent, hingej, lights in yope3d.view(\"HingeJointConstraint\", \"LightSource\"):\n            hingej.lower_angle = 0.5\n        world.detach_physics_body(entity)\n        h = world.get_registry()\n        v = yope3d.lerp(0.87, 3.82, 0.36)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.57)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.59, 1.58, yope3d.",
   "suffix": "\n",
   "target": "EASE_QUAD_IN",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0116.py:39:8:reg_remove",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0116.py",
   "line": 39,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth116Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.27},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        e = world.add_aabb(yope3d.Vec3(0.53, 0.16, 0.87), 38.3)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.99)\n        m = yope3d.reg_add(entity, \"Transform\")\n        m.rotation = yope3d.Quat()\n        m.scale = yope3d.Vec3(0.64, 0.93, 0.68)\n        m.position = yope3d.Vec3(0.07, 0.24, 0.76)\n        v = yope3d.reg_has(entity, \"HingeJointConstraint\")\n        v = yope3d.settings.get_int(\"crate\")\n        m = yope3d.reg_add(entity, \"CylinderForm\")\n        m.radius = 1.34\n        m.half_height = 0.55\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, uiback, transf in yope3d.view(\"UIBackground\", \"Transform\"):\n            uiback.g = 1.92\n        world.set_ui_focus(entity)\n        rot = yope3d.Quat.from_euler(0.07, -0.47)\n        r = world.get_registry()\n        v = yope3d.play_music(\"audios/impact.ogg\")\n        yope3d.clear_collision_observers()\n        world.set_skybox([])\n        v = yope3d.input.is_backward_mb_down()\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_get(entity, \"SpringConstraint\")\n        if c is not None:\n            c.target = entity\n        yope3d.",
   "suffix": "        res = world.ui_consumed_click()\n        e = world.build_sphere_compound([], 0.21, False, yope3d.Vec3(0.99, 0.41, 4.44))\n\n    def init(self, world, entity, params):\n        n = yope3d.Vec3(3.33, 0, 2.35).normalize()\n        c = yope3d.reg_get(entity, \"Material\")\n        if c is not None:\n            c.emissive_map = \"textures/lamp_emissive.png\"\n        for ent, transf, sphere in yope3d.view(\"Transform\", \"SphereForm\"):\n            transf.scale = yope3d.Vec3(1.25, 0.37, 1.13)\n",
   "target": "reg_remove",
   "rest": "(entity, \"Name\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0088.py:30:36:KEY_INSERT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0088.py",
   "line": 30,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth088Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.66},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        for ent, uibutt, transf in yope3d.view(\"UIButton\", \"Transform\"):\n            uibutt.hover_b = 1.92\n        if yope3d.input.is_key_down(yope3d.KEY_S):\n            world.apply_impulse(entity, yope3d.Vec3(0, 26.25, 0))\n        v = yope3d.attach_script(entity, \"hud_root\", \"spawn_point\")\n        yope3d.settings.remove(\"ground\")\n        world.add_spring(entity, entity, 3.43, 0.27)\n        body = world.add_point_light(yope3d.Vec3(4.94, 4.43, 0.62))\n        v = yope3d.window.get_escape_closes()\n        form = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if form is not None:\n            form.lower_angle = -1.38\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.position = yope3d.Vec3(0.54, 0.94, 0.13)\n        t.rotation = yope3d.Quat()\n        yope3d.load_game(\"crate_01\")\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 24.72, 0))\n        m = yope3d.reg_add(entity, \"ScriptComponent\")\n        m.params_blob = \"ground\"\n        m.script_class = \"player\"\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.half_height = 0.38\n        m.radius = 0.45\n        c = yope3d.reg_get(entity, \"SphereForm\")\n        if c is not None:\n            c.radius = 1.79\n",
   "target": "KEY_INSERT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:21:34:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 21,
   "col": 34,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.",
   "suffix": "    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n",
   "target": "Vec3",
   "rest": "(1, 1, 1), 1.0, 0.85, 0.15)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0049.py:47:12:look_at",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0049.py",
   "line": 47,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth049Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.73},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.fade_gain(entity, 0.82, 1.59)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.26, 0.64, yope3d.EASE_QUAD_IN_OUT)\n        yope3d.audio.set_bus_gain(1, 0.47)\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 3.1\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.77, 0, 4.51) - origin).normalize())\n        if yope3d.input.is_key_down(yope3d.KEY_F2):\n            world.apply_impulse(entity, yope3d.Vec3(0, 4.76, 0))\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_get(entity, \"SpringConstraint\")\n        if c is not None:\n            c.rest_length = 0.3\n        world.apply_impulse(entity, yope3d.Vec3(29.62, 1.29, 20.19))\n        r = yope3d.to_radians(6.0)\n        rot = yope3d.Quat.from_euler(0.23, -0.88)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(1.25, 0, 3.52)).length()\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        v = yope3d.capsule_cast(yope3d.Vec3(1.11, 3.61, 0.69), 0.87, 0.69, yope3d.Vec3(3.42, 0.19, 0.76), 0.82)\n        other = world.add_static_aabb(yope3d.Vec3(2.23, 1.32, 4.94), yope3d.Vec3(0.31, 0.51, 0.21))\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.loop = 3\n        wheel = world.add_sphere(20.84, 1.87, yope3d.Vec3(0.12, 1.38, 1.48))\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        origin = yope3d.get_position(entity)\n        q = yope3d.",
   "suffix": "        yope3d.audio.set_master_gain(0.91)\n        rot = yope3d.Quat.from_euler(-0.48, -0.18)\n        v = yope3d.get_world_position(entity)\n\n    def update(self, world, entity, dt):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.35)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.22, 1.35, yope3d.EASE_CUBIC_IN_OUT)\n        if yope3d.input.is_key_pressed(yope3d.KEY_TAB):\n            yope3d.play_sound(\"audios/impact.ogg\")\n",
   "target": "look_at",
   "rest": "((yope3d.Vec3(1.44, 0, 0.34) - origin).normalize())",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:137:56:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 137,
   "col": 56,
   "prefix": "targeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n",
   "target": "Vec3",
   "rest": "(1.0, 0.0, 0.0), 0.6))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:50:37:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 50,
   "col": 37,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.Vec3(0, 5, 0)\n\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))\n        world.attach_box_mesh(floor, yope3d.",
   "suffix": "\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n        self.spawned += 1\n\n        x = self.pos.x + (self.spawned % 3 - 1) * 1.5\n        proj = world.add_sphere(1.0, 0.3, yope3d.Vec3(x, self.pos.y, self.pos.z))\n",
   "target": "Vec3",
   "rest": "(4, 0.5, 4), 0.5, 0.5, 0.55)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:46:50:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 46,
   "col": 50,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.",
   "suffix": "\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))\n        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n",
   "target": "Vec3",
   "rest": "(0, 5, 0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0014.py:14:34:BUS_SFX",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0014.py",
   "line": 14,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth014Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.28},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        world.clear_shadow_caster()\n        world.tween_ui_opacity(entity, 0.57, 2.78)\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"OBBForm\")\n        c.extent = yope3d.Vec3(0.06, 0.49, 0.64)\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_get(entity, \"Parent\")\n        if m is not None:\n            m.parent = entity\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = True\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(2.13, 3.39, 2), yope3d.Vec3(3.81, 3.57, 1.6), True, -0.04, 0.63, False)\n",
   "target": "BUS_SFX",
   "rest": ", 0.84)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0038.py:29:39:KEY_0",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0038.py",
   "line": 29,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth038Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.66},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.visible = False\n        m.max_y = 0.19\n        m.max_x = 1.54\n        for ent, parent, uitext in yope3d.view(\"Parent\", \"UITexturedBackground\"):\n            parent.parent = entity\n        world.set_mesh_visible(entity, True)\n        world.set_skybox([])\n        anchor = world.add_cylinder(1.67, 0.34, 71)\n        hull = yope3d.reg_add(entity, \"AnimationPlayer\")\n        hull.time = 0.79\n        hull.playing = 3\n        hull.speed = 4.11\n        hull.loop = 1\n        hull.clip = \"crate\"\n        for ent, spring in yope3d.view(\"SpringConstraint\"):\n            spring.target = entity\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"UIButton\")\n        cmp.normal_b = 1.54\n        cmp.normal_g = 0.98\n        cmp.normal_r = 0.69\n        yope3d.camera.set_position(yope3d.Vec3(1.91, 2.39, 2.12))\n        m = yope3d.reg_get(entity, \"AudioSource\")\n        if m is not None:\n",
   "target": "KEY_0",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:160:27:KEY_D",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 160,
   "col": 27,
   "prefix": "windshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.",
   "suffix": "\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n",
   "target": "KEY_D",
   "rest": "): steer_input -= 1.0",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0062.py:14:52:EASE_LINEAR",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0062.py",
   "line": 14,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth062Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.13},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.8)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.86, yope3d.",
   "suffix": "        anchor = world.add_kinematic_capsule(1.46, 0.71, yope3d.Vec3(0.22, 3.27, 1.25))\n        v = yope3d.to_radians(1.75)\n        if yope3d.input.is_key_pressed(yope3d.KEY_SLASH):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.set_position(entity, yope3d.Vec3(4.59, 0.66, 3.21))\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_b = 0.75\n        c.normal_a = 1.14\n        c.hover_r = 3.93\n\n",
   "target": "EASE_LINEAR",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:18:4:reg_get",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 18,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.",
   "suffix": "    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n",
   "target": "reg_get",
   "rest": "(e, \"Hull\").observe_layers = LAYER_DEBRIS",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0010.py:48:12:settings",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0010.py",
   "line": 48,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth010Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.26},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"UIButton\")\n        t.normal_a = 1.32\n        t.normal_r = 0.59\n        t.hover_g = 0.59\n        t.normal_b = 2.73\n        t.hover_r = 3.26\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.autoplay = False\n        r = world.attach_animation(entity, \"textures/tnail.png\")\n        r = world.attach_animation(entity, \"audios/impact.ogg\")\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.metallic = 0.88\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"AABBForm\")\n        hull.extent = yope3d.Vec3(0.46, 0.29, 0.03)\n        v = yope3d.settings.get_float(\"hud_root\")\n        m = yope3d.reg_get(entity, \"SphereForm\")\n        if m is not None:\n            m.radius = 0.82\n        world.reset_physics()\n        for ent, hingej, animat in yope3d.view(\"HingeJointConstraint\", \"AnimationPlayer\"):\n            hingej.limit_enabled = True\n        rot = yope3d.Quat.from_euler(-0.84, 0.05)\n        m = yope3d.reg_get(entity, \"UIButton\")\n        if m is not None:\n            m.enabled = True\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.05, 0.88, 0.74)\n\n    def update(self, world, entity, dt):\n        wheel = world.add_ui_button(yope3d.Vec2(2.73, 4.81), yope3d.Vec2(2.77, 3.99), yope3d.Vec4(4.6, 1.92, 1.31, 3.72), 1)\n        world.remove_spring_between(entity, entity)\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.position = yope3d.Vec3(0.71, 0.4, 0.8)\n        v = yope3d.",
   "suffix": "        world.attach_aabb_collider(wheel, 83.16, yope3d.Vec3(0.21, 0.02, 0.06), False)\n\n    def init(self, world, entity, params):\n        yope3d.reg_remove(entity, \"Fixed\")\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.36, 0.16, 0.36)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.28)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.55, yope3d.EASE_QUAD_OUT)\n        if yope3d.input.is_key_pressed(yope3d.KEY_EQUAL):\n",
   "target": "settings",
   "rest": ".get_int(\"hud_root\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0041.py:20:38:MOUSE_MIDDLE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0041.py",
   "line": 20,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth041Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.68},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        res = world.add_model(\"models/prop.glb\")\n        world.attach_box_mesh(other, yope3d.Vec3(2.13, 0.38, 4.78))\n        world.set_shadow_caster(other)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 20.45)\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1.12\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 18.11)\n        hull = yope3d.reg_add(entity, \"ScriptComponent\")\n        hull.script_class = \"player\"\n        hull.params_blob = \"hud_root\"\n        v = yope3d.is_fixed(entity)\n\n    def update(self, world, entity, dt):\n        yope3d.audio.resume_all()\n        world.attach_box_mesh(entity, yope3d.Vec3(3.5, 3.93, 0.95))\n",
   "target": "MOUSE_MIDDLE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:8:35:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 8,
   "col": 35,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.",
   "suffix": "    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n",
   "target": "camera",
   "rest": ".get_forward() * 3.0",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0067.py:39:12:is_fixed",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0067.py",
   "line": 39,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth067Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.66},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, textla, audios in yope3d.view(\"TextLabel3D\", \"AudioSource\"):\n            textla.r = 4.47\n        world.set_ui_focus(entity)\n        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 1.32\n        v = yope3d.settings.keys()\n        cmp = yope3d.reg_add(entity, \"CylinderForm\")\n        cmp.half_height = 0.87\n        cmp.radius = 1.14\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"Transform\")\n        if hull is not None:\n            hull.scale = yope3d.Vec3(1.34, 1.83, 0.71)\n        body = world.add_kinematic_box(yope3d.Vec3(3.89, 4.6, 3.67), yope3d.Vec3(0.15, 0.55, 0.53))\n        target = world.add_kinematic_sphere(yope3d.Vec3(3.36, 3.89, 3.78), 0.88)\n        world.set_mesh_visible(entity, False)\n        world.reset_physics()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.camera.screen_to_ray(4.42, 2.49)\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.angular_damping = 0.25\n        t.mass = 82.7\n        world.attach_capsule_collider(entity, 53.49, 1.9, 0.42)\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1.79\n        v = yope3d.",
   "suffix": "\n    def update(self, world, entity, dt):\n        m = yope3d.reg_get(entity, \"CapsuleForm\")\n        if m is not None:\n            m.radius = 1.02\n        anchor = world.add_point_light(yope3d.Vec3(0.29, 2.53, 3.57))\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.type = 0.29\n        m.position = yope3d.Vec3(0.04, 0.2, 0.64)\n        m.constant = 0.13\n",
   "target": "is_fixed",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0003.py:22:34:BUS_SFX",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0003.py",
   "line": 22,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth003Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        world.attach_box_mesh(entity, yope3d.Vec3(1.2, 2.18, 2.85))\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 1.6\n        n = yope3d.Vec3(1.72, 0, 1.55).normalize()\n        if yope3d.input.is_key_pressed(yope3d.KEY_F4):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def update(self, world, entity, dt):\n        yope3d.audio.resume_all()\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        for ent, textla, aabbfo in yope3d.view(\"TextLabel3D\", \"AABBForm\"):\n            textla.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_add(entity, \"Name\")\n        hull.value = \"ground\"\n        hull = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        hull.local_axis_b = yope3d.Vec3(4.59, 1.99, 0.76)\n        hull.target = entity\n        hull.limit_enabled = False\n        rot = yope3d.Quat.from_euler(0.05, -0.05)\n\n",
   "target": "BUS_SFX",
   "rest": ", 0.54)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0106.py:38:12:get_world_position",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0106.py",
   "line": 38,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth106Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.79},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.reset_physics()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.71, 0, 1.65) - origin).normalize())\n        v = yope3d.attach_script(entity, \"ground\", \"ground\")\n        world.attach_obb_collider(entity, 69.08, yope3d.Vec3(0.36, 0.71, 0.69))\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.swing_limit = 1.23\n        cmp.local_twist_axis_a = yope3d.Vec3(0.56, 0.46, 0.43)\n\n    def update(self, world, entity, dt):\n        e = world.add_aabb(yope3d.Vec3(0.59, 0.25, 0.43), 88.03)\n        yope3d.camera.set_position(yope3d.Vec3(0.21, 4.71, 2.58))\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.hover_r = 2.8\n        m.normal_b = 0.27\n        m.normal_r = 2.37\n        m.hover_g = 1.94\n        v = yope3d.capsule_overlap(yope3d.Vec3(1.29, 0.94, 2.18), 1.92, 1.01)\n        world.set_ui_focus(e)\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.k = 3.86\n        c.rest_length = 0.34\n        c.target = entity\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.capsule_overlap(yope3d.Vec3(4.11, 4.82, 2.45), 1.05, 0.45)\n        v = yope3d.",
   "suffix": "        v = yope3d.window.get_escape_closes()\n        body = world.add_static_aabb(yope3d.Vec3(2.88, 4.18, 3.41), yope3d.Vec3(0.38, 0.01, 0.62))\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.56)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.25, 2.46, yope3d.EASE_QUAD_IN_OUT)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        v = yope3d.settings.save()\n",
   "target": "get_world_position",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0035.py:18:12:find_entity",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0035.py",
   "line": 18,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth035Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.48},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_skybox([])\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.99, 0.28, 0.29)\n        anchor = world.add_text_label_3d(\"Paused\", \"Paused\", yope3d.Vec3(1.22, 2.28, 3.32))\n        for ent, materi, cylind in yope3d.view(\"Material\", \"CylinderForm\"):\n            materi.emissive_map = \"textures/lamp_emissive.png\"\n        v = yope3d.",
   "suffix": "        world.reset_physics()\n        v = yope3d.settings.keys()\n        world.attach_obb_collider(anchor, 47.23, yope3d.Vec3(0.2, 0.78, 0.43))\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(0.31, 0, 1.93)).length()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n",
   "target": "find_entity",
   "rest": "(\"crate_01\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:46:19:get_position",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 46,
   "col": 19,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.",
   "suffix": "\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))\n        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n",
   "target": "get_position",
   "rest": "(entity) or yope3d.Vec3(0, 5, 0)",
   "freq": 3,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0023.py:12:12:save_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0023.py",
   "line": 12,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth023Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.46},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.",
   "suffix": "        v = yope3d.get_position(other)\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.bus = 2\n        prop = world.add_kinematic_capsule(0.23, 0.96, yope3d.Vec3(4.9, 3.67, 2.65))\n        v = yope3d.play_music(\"audios/impact.ogg\")\n        v = yope3d.window.get_tab_pauses()\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.radius = 0.84\n",
   "target": "save_game",
   "rest": "(\"player\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0091.py:25:36:KEY_F12",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0091.py",
   "line": 25,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth091Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.7},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"SphereForm\")\n        m.radius = 0.2\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.b = 0.1\n        rot = yope3d.Quat.from_euler(-0.1, -0.88)\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.quadratic = 0.57\n        world.remove_spring_between(entity, entity)\n\n    def on_ui_press(self, world, entity):\n        h = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 5.66, 0))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 16.77)\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.92\n        t.target = entity\n        t.k = 2.65\n        for ent, script, uiback in yope3d.view(\"ScriptComponent\", \"UIBackground\"):\n            script.script_class = \"player\"\n",
   "target": "KEY_F12",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:12:4:play_sound",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 12,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.",
   "suffix": "    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n",
   "target": "play_sound",
   "rest": "(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0002.py:59:52:EASE_LINEAR",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0002.py",
   "line": 59,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth002Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.87},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.settings.has(\"ground\")\n        hull = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_a = yope3d.Vec3(2.55, 2.96, 4.45)\n        form = yope3d.reg_get(entity, \"TextLabel3D\")\n        if form is not None:\n            form.font = \"fonts/inter_ascii.msdf\"\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 18.09, 0))\n        hull = yope3d.reg_get(entity, \"CompoundCollider\")\n        if hull is not None:\n            hull.loaded = True\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.direction = yope3d.Vec3(0.12, 0.53, 0.93)\n        for ent, materi, audios in yope3d.view(\"Material\", \"AudioSource\"):\n            materi.metal_rough_map = \"textures/crate_mr.png\"\n        hull = yope3d.reg_add(entity, \"PointJointConstraint\")\n        hull.local_anchor_b = yope3d.Vec3(3.62, 4.59, 2.45)\n        hull.local_anchor_a = yope3d.Vec3(4.79, 4.36, 0.26)\n        hull.target = entity\n        if yope3d.input.is_key_down(yope3d.KEY_R):\n            world.apply_impulse(entity, yope3d.Vec3(0, 39.37, 0))\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.28)\n\n    def init(self, world, entity, params):\n        for ent, name, textla in yope3d.view(\"Name\", \"TextLabel3D\"):\n            name.value = \"crate\"\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 11.54)\n        r = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.KEY_F8):\n            world.apply_impulse(entity, yope3d.Vec3(0, 5.64, 0))\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.script_class = \"crate\"\n        cmp.params_blob = \"crate\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        h = world.ui_hovered()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.4)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 1, 2.08, yope3d.EASE_LINEAR)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.83)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.27, 1.98, yope3d.",
   "suffix": "        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.8, 0, 2.79)).length()\n        for ent, parent, textla in yope3d.view(\"Parent\", \"TextLabel3D\"):\n            parent.parent = entity\n        res = world.get_shadow_caster()\n        world.set_wheel_steer(other, 0.2)\n        for ent, sphere, obbfor in yope3d.view(\"SphereForm\", \"OBBForm\"):\n            sphere.radius = 0.61\n\n",
   "target": "EASE_LINEAR",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:313:14:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 313,
   "col": 14,
   "prefix": "        self.aabb_floor(world)\n        self.sphere(world, 1.0, R, (0, 8, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_inside(self, world):\n        # Sphere starts fully inside the box \u2014 must be pushed out smoothly, not\n        # launched across the level.\n        self.plane_floor(world)\n        self.static_box(world, (0, 3, 0), (2.0, 2.0, 2.0), BOX_PURPLE)\n        self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_fast(self, world):\n        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0115.py:37:12:is_sleeping",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0115.py",
   "line": 37,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth115Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.86},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if c is not None:\n            c.local_twist_axis_b = yope3d.Vec3(0.09, 0.85, 0.37)\n        form = yope3d.reg_get(entity, \"Name\")\n        if form is not None:\n            form.value = \"crate\"\n        world.set_shadow_caster(other)\n        if yope3d.input.is_key_down(yope3d.KEY_RIGHT):\n            world.apply_impulse(entity, yope3d.Vec3(0, 26.23, 0))\n        cmp = yope3d.reg_add(entity, \"AudioSource\")\n        cmp.gain = 0.97\n        cmp.autoplay = False\n        cmp.pitch = 0.95\n        cmp.loop = False\n        for ent, uitext, uitext in yope3d.view(\"UIText\", \"UITexturedBackground\"):\n            uitext.text = \"Paused\"\n        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_b = 1.83\n        hull.hover_g = 1.39\n        hull.normal_g = 1.86\n        world.set_skybox([])\n\n    def init(self, world, entity, params):\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -1.26)\n        h = world.add_model(\"models/prop.glb\")\n        v = yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_R):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def update(self, world, entity, dt):\n        t = yope3d.reg_add(entity, \"Name\")\n        t.value = \"crate\"\n        form = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if form is not None:\n            form.tint_g = 0.87\n        form = yope3d.reg_get(entity, \"Transform\")\n",
   "target": "is_sleeping",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:189:8:camera",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 189,
   "col": 8,
   "prefix": "MU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.",
   "suffix": "        yope3d.camera.look_at(tf.position)\n\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.reg_get(e, \"Transform\")\n            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 1.0, 0.0), self.steer_angle)\n                wt.rotation = rot * (steer_q * self._wheel_align)\n",
   "target": "camera",
   "rest": ".set_position(tf.position + self._cam_offset)",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:198:54:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 198,
   "col": 54,
   "prefix": "        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.camera.look_at(tf.position)\n\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.reg_get(e, \"Transform\")\n            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "                wt.rotation = rot * (steer_q * self._wheel_align)\n            else:\n                wt.rotation = rot * self._wheel_align\n\n        for e, offset, local_rot in self._decor:\n            dtf = yope3d.reg_get(e, \"Transform\")\n            dtf.position = tf.position + rot.rotate(offset)\n            dtf.rotation = (rot * local_rot) if local_rot is not None else rot\n",
   "target": "Vec3",
   "rest": "(0.0, 1.0, 0.0), self.steer_angle)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0028.py:40:34:BUS_VOICE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0028.py",
   "line": 40,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth028Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.76},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.half_height = 0.61\n        hull.radius = 0.51\n        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_a = 2\n        hull.hover_r = 4.12\n        hull.hover_g = 4.74\n        hull.normal_b = 1.6\n        prop = world.add_trigger_box(yope3d.Vec3(1.97, 0.37, 4.38), yope3d.Vec3(0.6, 0.43, 0.4))\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.56\n        cmp.tint_r = 0.62\n        cmp.tint_b = 0.73\n        cmp.path = \"audios/impact.ogg\"\n        m = yope3d.reg_get(entity, \"LightSource\")\n        if m is not None:\n            m.color = yope3d.Vec3(2.36, 1.24, 3.89)\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.lower_angle = -0.29\n        if yope3d.input.is_key_down(yope3d.KEY_EQUAL):\n            world.apply_impulse(entity, yope3d.Vec3(0, 9.56, 0))\n\n    def update(self, world, entity, dt):\n        yope3d.set_velocity(entity, yope3d.Vec3(0.88, 0.76, 0.96))\n        if yope3d.input.is_key_pressed(yope3d.KEY_M):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        world.set_ui_group_visible(entity, False)\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_r = 0.22\n        cmp.has_texture = False\n        anchor = world.add_ui_background(yope3d.Vec2(3.81, 3), yope3d.Vec2(3.87, 0.08), yope3d.Vec4(4.33, 2.01, 3.51, 1.33))\n        v = yope3d.settings.save()\n\n    def init(self, world, entity, params):\n        v = yope3d.clamp(1.21, 4.84, 0.28)\n        r = world.get_registry()\n",
   "target": "BUS_VOICE",
   "rest": ", 0.86)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0017.py:34:52:EASE_QUAD_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0017.py",
   "line": 34,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth017Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.51},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"UITexturedBackground\")\n        t.tint_b = 0.4\n        t.path = \"audios/impact.ogg\"\n        t.has_texture = True\n        t.tint_g = 0.83\n        t.tint_r = 0.57\n        world.add_cone_twist_joint(entity, entity, yope3d.Vec3(3.64, 1.18, 1.49), yope3d.Vec3(0.93, 0.31, 0.35), 0.61, 0.32, False)\n        v = yope3d.find_entity(\"ground\")\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.02)\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.omega = yope3d.Vec3(0.95, 1.72, 2.31)\n        t.linear_damping = 0.34\n        v = yope3d.reg_has(entity, \"AABBForm\")\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.14)\n\n    def update(self, world, entity, dt):\n        v = yope3d.input.is_mouse_released(1)\n        h = world.add_vehicle(entity, [])\n        v = yope3d.input.is_key_pressed(3)\n        yope3d.draw_line(yope3d.Vec3(1.69, 1.77, 1.02), yope3d.Vec3(1.96, 1.81, 1.75))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.21)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.36, 1.87, yope3d.",
   "suffix": "        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.75, 0, 1.72) - origin).normalize())\n\n    def on_ui_press(self, world, entity):\n        world.remove_spring_between(entity, entity)\n        v = yope3d.get_world_position(entity)\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 3.13\n        c.r = 4.06\n        c.g = 1.3\n",
   "target": "EASE_QUAD_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0076.py:43:38:MOUSE_MIDDLE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0076.py",
   "line": 43,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth076Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.b = 3.15\n        cmp.r = 0.05\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.speed = 10.06\n        r = world.get_ui_focus()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.17, 0, 4.46) - origin).normalize())\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.55, 2.56, 0.75)\n        m.target = entity\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 25.37)\n        world.make_kinematic(entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 0.84\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.66, 0.29, yope3d.EASE_QUAD_IN)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n\n    def on_collision_enter(self, world, entity, other, contact):\n        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.65, 0.93, 0.15)\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.61)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.81)\n        v = yope3d.input.get_mouse_delta()\n        yope3d.camera.set_clip_planes(1.85, 4.3)\n        v = yope3d.input.is_mouse_down(0)\n\n    def init(self, world, entity, params):\n        r = yope3d.to_radians(163.0)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 1.25)\n",
   "target": "MOUSE_MIDDLE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0024.py:16:12:play_music",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0024.py",
   "line": 16,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth024Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.18},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.half_height = 1.16\n        hull.radius = 0.96\n        yope3d.audio.pause_all()\n        v = yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_5):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"Transform\")\n        m.scale = yope3d.Vec3(0.68, 0.56, 0.4)\n        m.position = yope3d.Vec3(0.06, 0.37, 0.42)\n        form = yope3d.reg_get(entity, \"OBBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.6, 0.68, 0.82)\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.half_height = 0.69\n",
   "target": "play_music",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0083.py:61:12:scene_payload",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0083.py",
   "line": 61,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth083Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.tween_ui_opacity(entity, 0.55, 0.69)\n        world.attach_box_mesh(entity, yope3d.Vec3(1.03, 2.23, 4.65), 1.84, 1.85, 2.82)\n        for ent, obbfor, capsul in yope3d.view(\"OBBForm\", \"CapsuleForm\"):\n            obbfor.extent = yope3d.Vec3(0.05, 0.65, 0.09)\n        v = yope3d.get_world_position(other)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 5.08)\n\n    def init(self, world, entity, params):\n        other = world.add_trigger_box(yope3d.Vec3(2.39, 0.5, 0.35), yope3d.Vec3(0.32, 0.56, 0.07))\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.16, 1.28, 1.11)\n        t.rotation = yope3d.Quat()\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.b = 3.39\n        v = yope3d.reg_add(entity, 3.58)\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.script_class = \"player\"\n        t.params_blob = \"hud_root\"\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(1.73, 1.55, 3.77)\n        e = world.add_trigger_box(yope3d.Vec3(2.63, 2.63, 2.16), yope3d.Vec3(0.06, 0.65, 0.86))\n\n    def update(self, world, entity, dt):\n        wheel = world.add_kinematic_box(yope3d.Vec3(0.04, 4.9, 3.73), yope3d.Vec3(0.27, 0.06, 0.43))\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0, 0, 4.86) - origin).normalize())\n        hull = yope3d.reg_get(entity, \"Material\")\n        if hull is not None:\n            hull.metal_rough_map = \"textures/crate_mr.png\"\n        world.attach_sphere_mesh(wheel, 1.79)\n        r = yope3d.to_radians(66.0)\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.input.is_key_down(2)\n        v = yope3d.play_music(\"models/prop.glb\")\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.reg_add(entity, 4.55)\n        v = yope3d.input.get_scroll_x()\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n        body = world.add_obb(yope3d.Vec3(0.38, 0.23, 0.71), 70.27)\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 0.38\n        world.set_ui_group_opacity(body, 0.98)\n        target = world.add_kinematic_capsule(1.96, 0.54)\n        v = yope3d.",
   "suffix": "\n",
   "target": "scene_payload",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:28:11:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 28,
   "col": 11,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.",
   "suffix": "    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "camera",
   "rest": ".position, yope3d.camera.get_forward()",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:313:42:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 313,
   "col": 42,
   "prefix": "        self.aabb_floor(world)\n        self.sphere(world, 1.0, R, (0, 8, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_inside(self, world):\n        # Sphere starts fully inside the box \u2014 must be pushed out smoothly, not\n        # launched across the level.\n        self.plane_floor(world)\n        self.static_box(world, (0, 3, 0), (2.0, 2.0, 2.0), BOX_PURPLE)\n        self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_fast(self, world):\n        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n",
   "target": "Vec3",
   "rest": "(0, 0, 1), math.radians(20.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0082.py:60:34:BUS_MUSIC",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0082.py",
   "line": 60,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth082Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.74\n        if yope3d.input.is_key_pressed(yope3d.KEY_Z):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_wheel_steer(entity, 1.09)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 21.57)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat()\n        if yope3d.input.is_key_pressed(yope3d.KEY_2):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.camera.set_position(yope3d.Vec3(3.48, 1.05, 4.94))\n        for ent, conetw in yope3d.view(\"ConeTwistJointConstraint\"):\n            conetw.target = entity\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.camera.screen_to_ray(1.39, 0.09)\n        c = yope3d.reg_get(entity, \"CapsuleForm\")\n        if c is not None:\n            c.radius = 1.7\n        m = yope3d.reg_get(entity, \"CylinderForm\")\n        if m is not None:\n            m.radius = 0.74\n        if yope3d.input.is_key_pressed(yope3d.KEY_3):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        hull = yope3d.reg_add(entity, \"OBBForm\")\n        hull.extent = yope3d.Vec3(0.46, 0.63, 0.67)\n\n    def init(self, world, entity, params):\n        for ent, spring, obbfor in yope3d.view(\"SpringConstraint\", \"OBBForm\"):\n            spring.k = 2.87\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.a = 2.22\n        form = yope3d.reg_get(entity, \"AudioSource\")\n        if form is not None:\n            form.bus = 4\n        n = yope3d.Vec3(4.72, 0, 1.99).normalize()\n        for ent, materi, cylind in yope3d.view(\"Material\", \"CylinderForm\"):\n            materi.emissive = (2.22, 0.6, 3.7)\n        r = yope3d.to_radians(98.0)\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.velocity = yope3d.Vec3(0.65, 0.56, 0.37)\n        t.linear_damping = 0.01\n        t.angular_damping = 0.11\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "\n",
   "target": "BUS_MUSIC",
   "rest": ", 0.71)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0010.py:58:39:KEY_EQUAL",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0010.py",
   "line": 58,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth010Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.26},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        t = yope3d.reg_add(entity, \"UIButton\")\n        t.normal_a = 1.32\n        t.normal_r = 0.59\n        t.hover_g = 0.59\n        t.normal_b = 2.73\n        t.hover_r = 3.26\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.autoplay = False\n        r = world.attach_animation(entity, \"textures/tnail.png\")\n        r = world.attach_animation(entity, \"audios/impact.ogg\")\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.metallic = 0.88\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_add(entity, \"AABBForm\")\n        hull.extent = yope3d.Vec3(0.46, 0.29, 0.03)\n        v = yope3d.settings.get_float(\"hud_root\")\n        m = yope3d.reg_get(entity, \"SphereForm\")\n        if m is not None:\n            m.radius = 0.82\n        world.reset_physics()\n        for ent, hingej, animat in yope3d.view(\"HingeJointConstraint\", \"AnimationPlayer\"):\n            hingej.limit_enabled = True\n        rot = yope3d.Quat.from_euler(-0.84, 0.05)\n        m = yope3d.reg_get(entity, \"UIButton\")\n        if m is not None:\n            m.enabled = True\n        t = yope3d.reg_add(entity, \"OBBForm\")\n        t.extent = yope3d.Vec3(0.05, 0.88, 0.74)\n\n    def update(self, world, entity, dt):\n        wheel = world.add_ui_button(yope3d.Vec2(2.73, 4.81), yope3d.Vec2(2.77, 3.99), yope3d.Vec4(4.6, 1.92, 1.31, 3.72), 1)\n        world.remove_spring_between(entity, entity)\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.position = yope3d.Vec3(0.71, 0.4, 0.8)\n        v = yope3d.settings.get_int(\"hud_root\")\n        world.attach_aabb_collider(wheel, 83.16, yope3d.Vec3(0.21, 0.02, 0.06), False)\n\n    def init(self, world, entity, params):\n        yope3d.reg_remove(entity, \"Fixed\")\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.36, 0.16, 0.36)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.28)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.83, 0.55, yope3d.EASE_QUAD_OUT)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_down(yope3d.KEY_C):\n            world.apply_impulse(entity, yope3d.Vec3(0, 34.87, 0))\n\n",
   "target": "KEY_EQUAL",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0083.py:45:39:KEY_CAPS_LOCK",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0083.py",
   "line": 45,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth083Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.tween_ui_opacity(entity, 0.55, 0.69)\n        world.attach_box_mesh(entity, yope3d.Vec3(1.03, 2.23, 4.65), 1.84, 1.85, 2.82)\n        for ent, obbfor, capsul in yope3d.view(\"OBBForm\", \"CapsuleForm\"):\n            obbfor.extent = yope3d.Vec3(0.05, 0.65, 0.09)\n        v = yope3d.get_world_position(other)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 5.08)\n\n    def init(self, world, entity, params):\n        other = world.add_trigger_box(yope3d.Vec3(2.39, 0.5, 0.35), yope3d.Vec3(0.32, 0.56, 0.07))\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.16, 1.28, 1.11)\n        t.rotation = yope3d.Quat()\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.b = 3.39\n        v = yope3d.reg_add(entity, 3.58)\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.script_class = \"player\"\n        t.params_blob = \"hud_root\"\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(1.73, 1.55, 3.77)\n        e = world.add_trigger_box(yope3d.Vec3(2.63, 2.63, 2.16), yope3d.Vec3(0.06, 0.65, 0.86))\n\n    def update(self, world, entity, dt):\n        wheel = world.add_kinematic_box(yope3d.Vec3(0.04, 4.9, 3.73), yope3d.Vec3(0.27, 0.06, 0.43))\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0, 0, 4.86) - origin).normalize())\n        hull = yope3d.reg_get(entity, \"Material\")\n        if hull is not None:\n            hull.metal_rough_map = \"textures/crate_mr.png\"\n        world.attach_sphere_mesh(wheel, 1.79)\n        r = yope3d.to_radians(66.0)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.input.is_key_down(2)\n        v = yope3d.play_music(\"models/prop.glb\")\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.reg_add(entity, 4.55)\n        v = yope3d.input.get_scroll_x()\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.loaded = True\n",
   "target": "KEY_CAPS_LOCK",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0036.py:18:38:MOUSE_RIGHT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0036.py",
   "line": 18,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth036Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.99},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        world.attach_aabb_collider(entity, 37.93, yope3d.Vec3(0.5, 0.3, 0.94))\n        form = yope3d.reg_get(entity, \"LightSource\")\n        if form is not None:\n            form.type = 0.25\n        if yope3d.input.is_key_pressed(yope3d.KEY_CAPS_LOCK):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 17.59)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_path(\"audios/impact.ogg\")\n        v = yope3d.clamp(3.58, 2.42, 1.24)\n        world.set_wheel_steer(entity, -0.54)\n        v = yope3d.capsule_cast(yope3d.Vec3(4.35, 2.7, 1.31), 1.48, 0.31, yope3d.Vec3(3.61, 1.03, 1.78), 1)\n        v = yope3d.settings.get_str(\"ground\")\n        body = world.add_ui_text(\"Paused\", \"Press E to interact\", yope3d.Vec2(0.76, 3.7), yope3d.Vec2(2.54, 0.25), 1)\n",
   "target": "MOUSE_RIGHT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0045.py:12:39:KEY_B",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0045.py",
   "line": 12,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth045Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.48},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.params_blob = \"player\"\n        v = yope3d.window.get_tab_pauses()\n        yope3d.reg_remove(entity, \"OBBForm\")\n        world.attach_obb_collider(other, 61.98, yope3d.Vec3(0.21, 0.32, 0.41))\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.normal_map = \"textures/crate_normal.png\"\n        v = yope3d.reg_get(other, 2.69)\n",
   "target": "KEY_B",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0064.py:81:12:find_entity",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0064.py",
   "line": 81,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth064Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.2},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.59)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.92, 1.07, yope3d.EASE_QUAD_OUT)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.02)\n        form = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if form is not None:\n            form.local_anchor_a = yope3d.Vec3(3.66, 3.2, 3.34)\n        for ent, hull in yope3d.view(\"Hull\"):\n            hull.is_trigger = False\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.a = 1.45\n        cmp.r = 2.82\n        cmp.g = 1.15\n        cmp.b = 4.85\n        cmp.text = \"Paused\"\n        t = yope3d.reg_add(entity, \"TextLabel3D\")\n        t.b = 1.56\n        t.g = 4.56\n        hull = yope3d.reg_get(entity, \"TextLabel3D\")\n        if hull is not None:\n            hull.b = 4.33\n        world.set_ui_focus(entity)\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (1.74, 3.92, 1.35, 0.08)\n        hull.albedo_map = \"textures/metal_basecolor.png\"\n        hull.normal_map = \"textures/crate_normal.png\"\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        cmp = yope3d.reg_add(entity, \"Name\")\n        cmp.value = \"player\"\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.tint_b = 0.52\n        hull.tint_g = 0.45\n        hull.has_texture = False\n        hull.tint_a = 0.63\n        hull.path = \"textures/tnail.png\"\n        m = yope3d.reg_add(entity, \"UITexturedBackground\")\n        m.has_texture = False\n        m.tint_b = 0.82\n        m.tint_g = 0.26\n        for ent, spring, sphere in yope3d.view(\"SpringConstraint\", \"SphereForm\"):\n            spring.rest_length = 0\n        for ent, sphere, capsul in yope3d.view(\"SphereForm\", \"CapsuleForm\"):\n            sphere.radius = 0.17\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(0.15, 0.87, 3.02), yope3d.Vec3(4.04, 1.39, 0.16))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.loop = False\n        hull = yope3d.reg_add(entity, \"SphereForm\")\n        hull.radius = 0.96\n        v = yope3d.window.get_cursor_pos()\n        v = yope3d.is_fixed(entity)\n        t = yope3d.reg_add(entity, \"AABBForm\")\n        t.extent = yope3d.Vec3(0.54, 0.85, 0.66)\n        r = yope3d.to_radians(37.0)\n        v = yope3d.lerp(4.85, 4.48, 0.56)\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if m is not None:\n            m.tint_a = 0.96\n        body = world.build_sphere_compound([])\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.normal_a = 4.7\n        v = yope3d.reg_has(entity, \"Material\")\n        v = yope3d.",
   "suffix": "        m = yope3d.reg_add(entity, \"UIButton\")\n        m.normal_a = 0.13\n        m.hover_g = 2.5\n        m.normal_r = 3.74\n\n",
   "target": "find_entity",
   "rest": "(\"player\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0099.py:58:39:KEY_0",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0099.py",
   "line": 58,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth099Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.76)\n        yope3d.observe_collisions(4, 3.23)\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 0.11\n        t.font = \"fonts/inter_ascii.msdf\"\n        t.text = \"Paused\"\n        t.g = 2.49\n        prop = yope3d.spawn(\"textures/tnail.png\")\n        h = world.get_registry()\n        res = world.ui_consumed_click()\n        world.set_ui_focus(entity)\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n        if hull is not None:\n            hull.params_blob = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        world.attach_cylinder_mesh(entity, 0.44, 0.43, 2.54, 4.18, 3.81)\n        v = yope3d.window.is_cursor_locked()\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.autoplay = True\n        yope3d.set_text(entity, \"Score: 0\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.63)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.55)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.55, 1.74, yope3d.EASE_CUBIC_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_sphere_mesh(entity, 0.24, 1.19, 1.67, 3.16, 2)\n        form = yope3d.reg_get(entity, \"UIButton\")\n        if form is not None:\n            form.hover_b = 2.96\n        world.attach_cylinder_mesh(entity, 1.05, 0.55, 3.14, 2.57, 4.96)\n        anchor = world.add_sphere(55.02, 0.6)\n        c = yope3d.reg_get(entity, \"OBBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.13, 0.95, 0.78)\n        c = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if c is not None:\n            c.local_axis_a = yope3d.Vec3(0.1, 4.93, 0.99)\n        m = yope3d.reg_get(entity, \"Material\")\n        if m is not None:\n            m.emissive_map = \"textures/lamp_emissive.png\"\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.to_degrees(3.05)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.67)\n        hull = yope3d.reg_add(entity, \"ScriptComponent\")\n        hull.script_class = \"crate\"\n        hull.params_blob = \"ground\"\n        yope3d.camera.set_clip_planes(2.65, 3.19)\n        anchor = world.add_ui_background(yope3d.Vec2(3.79, 0.87), yope3d.Vec2(4.45, 3.1), yope3d.Vec4(4.4, 1.3, 1.21, 1.65))\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"hud_root\"\n",
   "target": "KEY_0",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:117:56:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 117,
   "col": 56,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n",
   "target": "Vec3",
   "rest": "(0.0, 0.0, 1.0), math.pi * 0.5)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0119.py:50:52:EASE_QUAD_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0119.py",
   "line": 50,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth119Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_get(entity, \"OBBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.51, 0.38, 0.97)\n        world.attach_capsule_collider(entity, 74.56, 1.81, 0.75, True)\n        v = yope3d.settings.get_str(\"player\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.33)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.46)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.94)\n        body = world.add_capsule(0.67, 0.59, 59.05, yope3d.Vec3(1.98, 2.56, 1.11))\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITransform\")\n        if hull is not None:\n            hull.min_x = 0.95\n        anchor = world.add_audio_source_entity()\n        hull = yope3d.reg_get(entity, \"SphereForm\")\n        if hull is not None:\n            hull.radius = 1.51\n        body = world.add_trigger_box(yope3d.Vec3(4.42, 3.36, 3.35), yope3d.Vec3(0.08, 0.37, 0.32))\n        wheel = world.add_ui_background(yope3d.Vec2(4.79, 4.73), yope3d.Vec2(3.46, 1.44), yope3d.Vec4(3.99, 0.64, 0.23, 2.51))\n        t = yope3d.reg_add(entity, \"AnimationPlayer\")\n        t.loop = 0\n        t.clip = \"hud_root\"\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0\n        t.k = 3.42\n        t.target = entity\n        t = yope3d.reg_add(entity, \"UIBackground\")\n        t.b = 4.51\n        t.g = 3.71\n        t.a = 4.29\n        t.r = 2.02\n        world.reset_physics()\n        wheel = world.add_static_aabb(yope3d.Vec3(1.39, 4.97, 2.92), yope3d.Vec3(0.26, 0.14, 0.63))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.5)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.51, 1.99, yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"Transform\")\n        c.position = yope3d.Vec3(0.47, 0.07, 0.31)\n        c.rotation = yope3d.Quat()\n        c.scale = yope3d.Vec3(0.97, 1.96, 1.91)\n        cmp = yope3d.reg_add(entity, \"Material\")\n        cmp.normal_map = \"textures/crate_normal.png\"\n        cmp.occlusion_map = \"textures/crate_ao.png\"\n        cmp.albedo_map = \"textures/metal_basecolor.png\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n",
   "target": "EASE_QUAD_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:326:14:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 326,
   "col": 14,
   "prefix": "        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(30.0))\n        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.",
   "suffix": "        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_headon(self, world):\n        self.plane_floor(world)\n        ra = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(20.0))\n        rb = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), math.radians(-20.0))\n        self.obb(world, H, 1.0, (-5, 3, 0), OBB_A, vel=(4.0, 0, 0), rot=ra)\n        self.obb(world, H, 1.0, (5, 3, 0), OBB_B, vel=(-4.0, 0, 0), rot=rb)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "typings/yope3d/__init__.pyi:14:40:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 14,
   "col": 40,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.",
   "suffix": "    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n",
   "target": "Vec3",
   "rest": "(0, 1, 0), yope3d.Vec3(2, 0.2, 2))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "typings/yope3d/__init__.pyi:9:35:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 9,
   "col": 35,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.",
   "suffix": "    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n",
   "target": "camera",
   "rest": ".get_forward()",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0039.py:52:39:KEY_I",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0039.py",
   "line": 52,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth039Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.12},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Score: 0\"\n        wheel = world.add_kinematic_capsule(1.54, 0.54)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"crate\"\n        res = world.get_registry()\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.4, 2.68, 1.73)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.03, 2.91, 3.91)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.58, 1.34), yope3d.Vec2(1.62, 0.21), yope3d.Vec4(0.69, 0.66, 0.06, 0.99), \"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.46\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"player\"\n        t.script_class = \"hud_root\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_RIGHT_BRACKET):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 23.33, 0))\n        for ent, lights, conetw in yope3d.view(\"LightSource\", \"ConeTwistJointConstraint\"):\n            lights.color = yope3d.Vec3(0.02, 0.24, 1.28)\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.71)\n        body = world.add_capsule(0.5, 0.32, 13.11)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.52, 0.8, 1.61)\n        t.rotation = yope3d.Quat.from_euler(0.0, -0.97)\n        t.position = yope3d.Vec3(0.43, 0.38, 0.93)\n        h = world.get_pair_count()\n        res = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, uibutt, hingej in yope3d.view(\"UIButton\", \"HingeJointConstraint\"):\n            uibutt.normal_a = 0.27\n        if yope3d.input.is_key_down(yope3d.KEY_SLASH):\n            world.apply_impulse(entity, yope3d.Vec3(0, 9.66, 0))\n        anchor = world.add_capsule(1.24, 0.3, 14.4)\n        prop = world.add_ui_button(yope3d.Vec2(2.22, 2.41), yope3d.Vec2(0.95, 1.55), yope3d.Vec4(0.19, 3.54, 2.11, 4.2))\n        if yope3d.input.is_key_pressed(yope3d.KEY_UP):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n",
   "target": "KEY_I",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0117.py:29:34:BUS_VOICE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0117.py",
   "line": 29,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth117Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.35},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        h = world.add_model(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"AnimationPlayer\")\n        cmp.loop = 1\n        cmp.clip = \"player\"\n        cmp.speed = 9.14\n        cmp.time = 0.75\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 0.81\n        c.a = 0.4\n        c.g = 1.02\n        form = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if form is not None:\n            form.local_axis_a = yope3d.Vec3(1.08, 1.2, 0.94)\n\n    def init(self, world, entity, params):\n        anchor = world.add_capsule(1.54, 0.42, 82.6)\n        v = yope3d.window.get_escape_closes()\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        world.set_ui_group_opacity(entity, 0.05)\n        for ent, sphere, audios in yope3d.view(\"SphereForm\", \"AudioSource\"):\n            sphere.radius = 0.51\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.87)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.36, 2.62, yope3d.EASE_QUAD_IN_OUT)\n\n    def on_ui_press(self, world, entity):\n        world.clear_shadow_caster()\n        v = yope3d.attach_script(entity, \"hud_root\", \"spawn_point\")\n",
   "target": "BUS_VOICE",
   "rest": ", 0.51)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0092.py:33:36:KEY_Q",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0092.py",
   "line": 33,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth092Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.22},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if hull is not None:\n            hull.upper_angle = 1.21\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.g = 1.03\n        hull.a = 3.5\n        hull.b = 0.12\n        hull.r = 4.28\n        for ent, transf, name in yope3d.view(\"Transform\", \"Name\"):\n            transf.position = yope3d.Vec3(0.4, 0.55, 0.38)\n        world.remove_spring_between(entity, entity)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.draw_line(yope3d.Vec3(4.38, 2.28, 3.85), yope3d.Vec3(4.89, 2.32, 1.59))\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -1.28)\n        c = yope3d.reg_add(entity, \"UITransform\")\n        c.depth = 0\n        c.min_x = 3.2\n        c.max_x = 2.77\n        c.visible = True\n        c.max_y = 1.39\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 37.46, 0))\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"Parent\")\n        if form is not None:\n            form.parent = entity\n        v = yope3d.settings.has(\"hud_root\")\n        v = yope3d.is_fixed(entity)\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n",
   "target": "KEY_Q",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0039.py:42:34:BUS_MUSIC",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0039.py",
   "line": 42,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth039Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.12},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.text = \"Score: 0\"\n        wheel = world.add_kinematic_capsule(1.54, 0.54)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"crate\"\n        res = world.get_registry()\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.4, 2.68, 1.73)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.03, 2.91, 3.91)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        anchor = world.add_ui_textured_background(yope3d.Vec2(4.58, 1.34), yope3d.Vec2(1.62, 0.21), yope3d.Vec4(0.69, 0.66, 0.06, 0.99), \"models/prop.glb\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.46\n        t = yope3d.reg_add(entity, \"ScriptComponent\")\n        t.params_blob = \"player\"\n        t.script_class = \"hud_root\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_RIGHT_BRACKET):\n            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_F6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 23.33, 0))\n        for ent, lights, conetw in yope3d.view(\"LightSource\", \"ConeTwistJointConstraint\"):\n            lights.color = yope3d.Vec3(0.02, 0.24, 1.28)\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        body = world.add_capsule(0.5, 0.32, 13.11)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(1.52, 0.8, 1.61)\n        t.rotation = yope3d.Quat.from_euler(0.0, -0.97)\n        t.position = yope3d.Vec3(0.43, 0.38, 0.93)\n        h = world.get_pair_count()\n        res = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_I):\n",
   "target": "BUS_MUSIC",
   "rest": ", 0.71)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0023.py:26:8:load_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0023.py",
   "line": 26,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth023Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.46},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.save_game(\"player\")\n        v = yope3d.get_position(other)\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.bus = 2\n        prop = world.add_kinematic_capsule(0.23, 0.96, yope3d.Vec3(4.9, 3.67, 2.65))\n        v = yope3d.play_music(\"audios/impact.ogg\")\n        v = yope3d.window.get_tab_pauses()\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.radius = 0.84\n        hull.half_height = 0.41\n        v = yope3d.window.get_cursor_pos()\n        wheel = world.add_ui_background(yope3d.Vec2(3.86, 4.1), yope3d.Vec2(0, 0.49), yope3d.Vec4(4.69, 4.14, 4.32, 4.91))\n        yope3d.",
   "suffix": "        res = world.get_contact_count()\n\n    def on_ui_press(self, world, entity):\n        h = world.ui_hovered()\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.77)\n        yope3d.set_position(entity, yope3d.Vec3(2.34, 4.69, 0.78))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.29)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.54, 0.39, yope3d.EASE_QUAD_IN)\n        m = yope3d.reg_add(entity, \"AudioSource\")\n",
   "target": "load_game",
   "rest": "(\"ground\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:49:55:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 49,
   "col": 55,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.Vec3(0, 5, 0)\n\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.",
   "suffix": "        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n        self.spawned += 1\n\n        x = self.pos.x + (self.spawned % 3 - 1) * 1.5\n",
   "target": "Vec3",
   "rest": "(4, 0.5, 4))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0082.py:18:38:MOUSE_RIGHT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0082.py",
   "line": 18,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth082Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_r = 0.74\n        if yope3d.input.is_key_pressed(yope3d.KEY_Z):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_wheel_steer(entity, 1.09)\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 21.57)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, transf in yope3d.view(\"Transform\"):\n            transf.rotation = yope3d.Quat()\n        if yope3d.input.is_key_pressed(yope3d.KEY_2):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.camera.set_position(yope3d.Vec3(3.48, 1.05, 4.94))\n        for ent, conetw in yope3d.view(\"ConeTwistJointConstraint\"):\n",
   "target": "MOUSE_RIGHT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0086.py:40:12:clamp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0086.py",
   "line": 40,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth086Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.37},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.15, 0, 1.09) - origin).normalize())\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.time = 0.74\n        m = yope3d.reg_get(entity, \"AudioSource\")\n        if m is not None:\n            m.loop = False\n        world.set_mesh_visible(entity, False)\n\n    def init(self, world, entity, params):\n        r = yope3d.to_radians(58.0)\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.hover_g = 1.42\n        m = yope3d.reg_get(entity, \"SphereForm\")\n        if m is not None:\n            m.radius = 1.02\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 27.45)\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(139.0)\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.constant = 0.32\n        m.direction = yope3d.Vec3(0.26, 0.18, 0.66)\n        world.add_spring_with_proxies(entity, entity, 4.36, 0.36, 4, 1.83)\n        v = yope3d.",
   "suffix": "        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.autoplay = False\n        yope3d.camera.set_fov(75.18)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_ui_parent(entity, entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.half_height = 1.05\n        for ent, sphere, name in yope3d.view(\"SphereForm\", \"Name\"):\n            sphere.radius = 1.08\n",
   "target": "clamp",
   "rest": "(2.23, 4.6, 3.13)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:88:42:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 88,
   "col": 42,
   "prefix": "\"\"\"Physics gallery \u2014 the 39 validation scenes from the pre-scripting engine.\n\nPorted from the scene switcher that lived in Engine::loadScene at commit b0b630a\n(May 2026), before scripting existed. Each scene isolates one narrowphase pair or\none solver property (resting stability, deflection, stacking, spring rest length),\nso a regression shows up as a body that jitters, sinks, spins wrong, or passes\nthrough something it shouldn't.\n\n    0-4    Sphere vs floor          \u2014 drop, high-speed drop, shallow approach, rest, roll\n    5-8    Sphere vs floating panel \u2014 center hit, edge approach, edge miss, rest\n    9-13   Sphere vs sphere         \u2014 head-on, glancing, one fixed, resting stack, high-speed\n    14-18  Sphere vs AABB           \u2014 drop on top, hit side, floor only, start inside, high-speed\n    19-22  Springs                  \u2014 two free, one anchored, compressed, stretched\n    23-26  AABB vs floor            \u2014 drop, high-speed drop, rest, 45-degree ramp\n    27-29  AABB vs floating panel   \u2014 center hit, edge miss, rest\n    30-32  AABB vs AABB             \u2014 drop on static, two dynamic, stacking\n    33     Sphere on the 45-degree ramp (compare against scene 26)\n    34-38  OBB                      \u2014 drop, sphere vs OBB, drop on static AABB, head-on, rest\n\nPORTING NOTE \u2014 the originals were built on physics::Barrier / BoundedBarrier (an\ninfinite plane and a finite one) plus the CCD collider, all deleted in M12 when\nphysics went ECS-native. Here the infinite floor is a 100x100x1 static AABB, the\nbounded panel is a thin static AABB, and the angled barrier is a fixed rotated OBB\n\u2014 same surfaces at the same heights, so every drop height and rest position below\nis unchanged. Two consequences worth knowing when you read the results:\n\n  * The \"no tunneling\" scenes (1, 13, 18, 24) were CCD tests. Nothing sweeps the\n    swept volume now, so a fast enough body CAN tunnel \u2014 these are honest discrete-\n    solver checks today, not guarantees.\n  * The \"edge miss\" scenes (7, 28) dropped a body just outside a *point-tested*\n    panel boundary so it fell straight through. A real AABB panel has corners, so\n    the body now clips the corner and deflects instead of passing cleanly by.\n\nAttach to any entity in a scene file (assets/scenes/physicsGallery.yscene does it).\n\nControls: LEFT/RIGHT switch scene, UP/DOWN pick spawn shape, LMB fire it,\nWASD+mouse fly, P collider overlay, H hide HUD.\n\"\"\"\nimport math\n\nimport yope3d\n\nfrom behaviors._gallery import GalleryBase\n\nFLOOR = (0.35, 0.30, 0.25)\nPANEL = (0.5, 0.7, 0.4)\nPROP = (0.7, 0.5, 0.3)\nBOX_PURPLE = (0.7, 0.3, 0.7)\n\nBALL = (0.2, 0.6, 1.0)\nBALL_HOT = (1.0, 0.4, 0.2)\nBALL_FAST = (1.0, 0.2, 0.2)\nBALL_MISS = (1.0, 0.3, 0.3)\nFIXED = (0.8, 0.8, 0.2)\n\nCUBE = (0.3, 0.7, 0.4)\nCUBE_FAST = (1.0, 0.3, 0.2)\nCUBE_MISS = (1.0, 0.3, 0.3)\nOBB_A = (0.8, 0.4, 0.1)\nOBB_B = (0.2, 0.5, 0.9)\n\nR = 0.5          # sphere radius used by every scene\nH = (0.5, 0.5, 0.5)   # standard box half-extents\n\n\nclass PhysicsGallery(GalleryBase):\n\n    # ---- Terrain helpers -------------------------------------------------\n\n    def plane_floor(self, world):\n        \"\"\"Top face at y=0 \u2014 a sphere of radius 0.5 rests at y=0.5.\"\"\"\n        self.static_box(world, (0.0, -0.5, 0.0), (50.0, 0.5, 50.0), FLOOR)\n\n    def aabb_floor(self, world):\n        \"\"\"Top face at y=0.5 \u2014 a sphere of radius 0.5 rests at y=1.0.\"\"\"\n        self.static_box(world, (0.0, 0.0, 0.0), (50.0, 0.5, 50.0), FLOOR)\n\n    def panel(self, world, y, s):\n        \"\"\"Floating slab, half-extent s, top face exactly at y.\"\"\"\n        self.static_box(world, (0.0, y - 0.05, 0.0), (s, 0.05, s), PANEL)\n\n    def ramp(self, world):\n        \"\"\"45-degree slope through the origin (surface normal (1,1,0)/sqrt2).\n\n        Offset half a thickness down the normal so the slab's TOP face \u2014 not its\n        center \u2014 passes through the origin, matching the old infinite barrier plane.\n        \"\"\"\n        rot = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self.obb(world, (30.0, 0.5, 30.0), 1.0, (-0.354, -0.354, 0.0), FLOOR,\n                 rot=rot, fixed=True)\n\n    # ---- Sphere vs floor -------------------------------------------------\n\n    def s_drop(self, world):\n        self.plane_floor(world)\n        self.sphere(world, 1.0, R, (0, 8, 0), BALL)\n        self.set_camera((0.0, 4.0, 16.0), pitch=-0.1)\n\n",
   "target": "Vec3",
   "rest": "(0, 0, 1), math.radians(-45.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0072.py:35:8:observe_collisions",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0072.py",
   "line": 35,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth072Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.98},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        world.set_paused(False)\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"player\"\n        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.61, 0.73, 0.72)\n        yope3d.settings.remove(\"crate\")\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n        hull.font = \"fonts/inter_ascii.msdf\"\n        hull.r = 1.14\n        hull.a = 4.99\n        cmp = yope3d.reg_add(entity, \"CapsuleForm\")\n        cmp.radius = 1.49\n        cmp.half_height = 1.03\n\n    def init(self, world, entity, params):\n        for ent, uibutt, name in yope3d.view(\"UIButton\", \"Name\"):\n            uibutt.disabled_a = 0.91\n        body = world.add_kinematic_box(yope3d.Vec3(4.07, 3.38, 3.39), yope3d.Vec3(0.98, 0.89, 0.41))\n        for ent, hingej, materi in yope3d.view(\"HingeJointConstraint\", \"Material\"):\n            hingej.local_anchor_b = yope3d.Vec3(3.21, 3.19, 4.37)\n        world.clear_shadow_caster()\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.05)\n        world.add_cone_twist_joint(body, body, yope3d.Vec3(4.48, 1.37, 4.12), yope3d.Vec3(0.54, 0.3, 0.1))\n        yope3d.",
   "suffix": "        h = world.ui_consumed_click()\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"Name\")\n        m.value = \"player\"\n        hull = yope3d.reg_add(entity, \"Transform\")\n        hull.rotation = yope3d.Quat()\n        hull.scale = yope3d.Vec3(1.21, 0.43, 0.44)\n        hull.position = yope3d.Vec3(0.28, 0.02, 0.85)\n        target = yope3d.spawn(\"models/prop.glb\")\n",
   "target": "observe_collisions",
   "rest": "(1, 2.62)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0099.py:60:12:to_degrees",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0099.py",
   "line": 60,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth099Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.76)\n        yope3d.observe_collisions(4, 3.23)\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 0.11\n        t.font = \"fonts/inter_ascii.msdf\"\n        t.text = \"Paused\"\n        t.g = 2.49\n        prop = yope3d.spawn(\"textures/tnail.png\")\n        h = world.get_registry()\n        res = world.ui_consumed_click()\n        world.set_ui_focus(entity)\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n        if hull is not None:\n            hull.params_blob = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        world.attach_cylinder_mesh(entity, 0.44, 0.43, 2.54, 4.18, 3.81)\n        v = yope3d.window.is_cursor_locked()\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.autoplay = True\n        yope3d.set_text(entity, \"Score: 0\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.63)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.55)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.55, 1.74, yope3d.EASE_CUBIC_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_sphere_mesh(entity, 0.24, 1.19, 1.67, 3.16, 2)\n        form = yope3d.reg_get(entity, \"UIButton\")\n        if form is not None:\n            form.hover_b = 2.96\n        world.attach_cylinder_mesh(entity, 1.05, 0.55, 3.14, 2.57, 4.96)\n        anchor = world.add_sphere(55.02, 0.6)\n        c = yope3d.reg_get(entity, \"OBBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.13, 0.95, 0.78)\n        c = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if c is not None:\n            c.local_axis_a = yope3d.Vec3(0.1, 4.93, 0.99)\n        m = yope3d.reg_get(entity, \"Material\")\n        if m is not None:\n            m.emissive_map = \"textures/lamp_emissive.png\"\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_0):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.",
   "suffix": "        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.67)\n        hull = yope3d.reg_add(entity, \"ScriptComponent\")\n        hull.script_class = \"crate\"\n        hull.params_blob = \"ground\"\n        yope3d.camera.set_clip_planes(2.65, 3.19)\n        anchor = world.add_ui_background(yope3d.Vec2(3.79, 0.87), yope3d.Vec2(4.45, 3.1), yope3d.Vec4(4.4, 1.3, 1.21, 1.65))\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"hud_root\"\n\n",
   "target": "to_degrees",
   "rest": "(3.05)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:22:29:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 22,
   "col": 29,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.",
   "suffix": "    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n",
   "target": "Vec3",
   "rest": "(3, 0, 0), 1.0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0048.py:12:8:load_game",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0048.py",
   "line": 12,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth048Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.55},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.",
   "suffix": "        cmp = yope3d.reg_add(entity, \"Parent\")\n        cmp.parent = entity\n        world.set_wheel_steer(entity, -0.33)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"hud_root\"\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.local_anchor_a = yope3d.Vec3(4.15, 1.25, 3.25)\n        prop = world.add_capsule(0.4, 0.29, 82.35, yope3d.Vec3(0.64, 3.8, 4.69))\n",
   "target": "load_game",
   "rest": "(\"player\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:195:17:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 195,
   "col": 17,
   "prefix": "\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.camera.look_at(tf.position)\n\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.",
   "suffix": "            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 1.0, 0.0), self.steer_angle)\n                wt.rotation = rot * (steer_q * self._wheel_align)\n            else:\n                wt.rotation = rot * self._wheel_align\n\n        for e, offset, local_rot in self._decor:\n            dtf = yope3d.reg_get(e, \"Transform\")\n            dtf.position = tf.position + rot.rotate(offset)\n",
   "target": "reg_get",
   "rest": "(e, \"Transform\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0098.py:20:8:load_scene",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0098.py",
   "line": 20,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth098Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.78},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.path = \"audios/impact.ogg\"\n        c.autoplay = False\n        c.pitch = 1.06\n        c.gain = 0.36\n        c.loop = True\n        target = world.add_audio_source_entity(yope3d.Vec3(3.17, 4.96, 3.56))\n        yope3d.audio.stop_all()\n        yope3d.",
   "suffix": "        yope3d.draw_ray(yope3d.Vec3(1.86, 3.24, 0.21), yope3d.Vec3(0.78, 1.16, 2.35))\n        world.apply_impulse(target, yope3d.Vec3(24.27, 8.56, 14.78))\n\n    def init(self, world, entity, params):\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(2.17, 0, 2.99)).length()\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        v = yope3d.reg_has(entity, \"Transform\")\n        form = yope3d.reg_get(entity, \"Transform\")\n        if form is not None:\n",
   "target": "load_scene",
   "rest": "(\"scenes/level_01.yscene\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0049.py:22:36:KEY_F2",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0049.py",
   "line": 22,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth049Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.73},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.fade_gain(entity, 0.82, 1.59)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.26, 0.64, yope3d.EASE_QUAD_IN_OUT)\n        yope3d.audio.set_bus_gain(1, 0.47)\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 3.1\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.77, 0, 4.51) - origin).normalize())\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 4.76, 0))\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_get(entity, \"SpringConstraint\")\n        if c is not None:\n            c.rest_length = 0.3\n        world.apply_impulse(entity, yope3d.Vec3(29.62, 1.29, 20.19))\n        r = yope3d.to_radians(6.0)\n        rot = yope3d.Quat.from_euler(0.23, -0.88)\n        p = yope3d.get_position(entity)\n",
   "target": "KEY_F2",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0066.py:41:36:KEY_F6",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0066.py",
   "line": 41,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth066Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        c = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if c is not None:\n            c.target = entity\n        if yope3d.input.is_key_pressed(yope3d.KEY_Q):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.35)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.63, 0.68, yope3d.EASE_QUAD_OUT)\n        world.set_paused(False)\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.text = \"Score: 0\"\n        c.font = \"fonts/inter_ascii.msdf\"\n        h = world.add_vehicle(entity, [])\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.68)\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(3.94, 1.39, 3.61), yope3d.Vec3(0.88, 4.06, 1.25), True, -0.74, -1.17, True)\n        for ent, hingej, uibutt in yope3d.view(\"HingeJointConstraint\", \"UIButton\"):\n            hingej.local_anchor_a = yope3d.Vec3(3.04, 4.55, 0.33)\n        world.clear_shadow_caster()\n\n    def update(self, world, entity, dt):\n        yope3d.set_profile_scene(\"scenes/level_01.yscene\")\n        m = yope3d.reg_add(entity, \"AABBForm\")\n        m.extent = yope3d.Vec3(0.96, 0.12, 0.91)\n        e = world.add_kinematic_box(yope3d.Vec3(0.44, 4.74, 4.91), yope3d.Vec3(0.02, 0.81, 0.79))\n        res = world.get_contact_point_count()\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 32.19, 0))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.15)\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.normal_a = 1.95\n        m.normal_r = 1.95\n        target = world.add_point_light(yope3d.Vec3(4.68, 4.22, 2.91))\n\n    def on_collision_enter(self, world, entity, other, contact):\n",
   "target": "KEY_F6",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0079.py:54:8:reg_remove",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0079.py",
   "line": 54,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth079Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.83},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_add(entity, \"Material\")\n        c.albedo_map = \"textures/metal_basecolor.png\"\n        c.metal_rough_map = \"textures/crate_mr.png\"\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.occlusion_map = \"textures/crate_ao.png\"\n        c.albedo = (0.02, 2.44, 3.27, 0.99)\n        yope3d.settings.remove(\"ground\")\n        world.attach_aabb_collider(entity, 70.19, yope3d.Vec3(0.7, 0.11, 0.19), True)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.45, 1.24, yope3d.EASE_QUAD_OUT)\n        cmp = yope3d.reg_add(entity, \"UIText\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.a = 1.31\n        world.wake(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_W):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.82, 0, 1.91) - origin).normalize())\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.fix_entity(other)\n        r = world.get_island_count()\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 2.59\n        t.text = \"Score: 0\"\n        t.g = 2.73\n        t.a = 0.14\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.34, 0, 3.35) - origin).normalize())\n        v = yope3d.reg_valid(other)\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.loop = True\n        c.bus = 4\n        if yope3d.input.is_key_pressed(yope3d.KEY_F9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.target = entity\n        c.rest_length = 0.81\n        c.k = 3.03\n\n    def update(self, world, entity, dt):\n        yope3d.",
   "suffix": "        world.set_mesh_color(entity, 4, 2.2, 1.32)\n        yope3d.clear_collision_observers()\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.mass = 82.59\n        c.angular_damping = 0.16\n        c.velocity = yope3d.Vec3(0.38, 0.87, 0.03)\n        if yope3d.input.is_key_down(yope3d.KEY_6):\n            world.apply_impulse(entity, yope3d.Vec3(0, 8.13, 0))\n\n    def init(self, world, entity, params):\n",
   "target": "reg_remove",
   "rest": "(entity, \"CapsuleForm\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0030.py:59:39:KEY_CAPS_LOCK",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0030.py",
   "line": 59,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth030Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.43},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.09)\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.script_class = \"crate\"\n        for ent, sphere, cylind in yope3d.view(\"SphereForm\", \"CylinderForm\"):\n            sphere.radius = 1.75\n        target = world.add_kinematic_capsule(0.77, 0.22, yope3d.Vec3(2.18, 0.28, 4.7))\n        v = yope3d.is_sleeping(target)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.65)\n        world.wake(target)\n        yope3d.set_velocity(target, yope3d.Vec3(0.7, 0.93, 0.35))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 1.31\n        m.half_height = 1.03\n        world.apply_impulse(other, yope3d.Vec3(8.76, 17.61, 27.87))\n        anchor = world.add_ui_curved_background(yope3d.Vec2(3.99, 3.35), yope3d.Vec2(2.14, 2.13), yope3d.Vec4(0.91, 4.73, 4.99, 2.29))\n        form = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if form is not None:\n            form.tint_a = 0.17\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo_map = \"textures/crate_albedo.png\"\n        hull.albedo = (3.37, 4.67, 3.65, 3.32)\n        hull.normal_map = \"textures/crate_normal.png\"\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        if yope3d.input.is_key_pressed(yope3d.KEY_J):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.path = \"textures/tnail.png\"\n        for ent, spring in yope3d.view(\"SpringConstraint\"):\n            spring.k = 3.3\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"SpringConstraint\")\n        m.target = entity\n        m.k = 1.31\n        m.rest_length = 0.23\n        for ent, uiback in yope3d.view(\"UIBackground\"):\n            uiback.r = 4.83\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.albedo = (1.11, 0.69, 2.3, 1.03)\n        hull.occlusion_map = \"textures/crate_ao.png\"\n        hull.albedo_map = \"textures/metal_basecolor.png\"\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        world.set_skybox([])\n        hull = yope3d.reg_get(entity, \"SpringConstraint\")\n        if hull is not None:\n            hull.rest_length = 0.81\n\n    def on_ui_press(self, world, entity):\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.26)\n        world.clear_shadow_caster()\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n",
   "target": "KEY_CAPS_LOCK",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0119.py:17:34:BUS_VOICE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0119.py",
   "line": 17,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth119Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_get(entity, \"OBBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.51, 0.38, 0.97)\n        world.attach_capsule_collider(entity, 74.56, 1.81, 0.75, True)\n        v = yope3d.settings.get_str(\"player\")\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.46)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.94)\n        body = world.add_capsule(0.67, 0.59, 59.05, yope3d.Vec3(1.98, 2.56, 1.11))\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UITransform\")\n        if hull is not None:\n            hull.min_x = 0.95\n        anchor = world.add_audio_source_entity()\n        hull = yope3d.reg_get(entity, \"SphereForm\")\n",
   "target": "BUS_VOICE",
   "rest": ", 0.33)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0040.py:65:36:KEY_GRAVE_ACCENT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0040.py",
   "line": 65,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth040Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.54},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"Material\")\n        hull.emissive_map = \"textures/lamp_emissive.png\"\n        hull.metal_rough_map = \"textures/crate_mr.png\"\n        hull.albedo = (0.42, 1.57, 1.88, 4.48)\n        world.add_spring_with_proxies(entity, entity, 2.08, 0.73, 3, 1.11)\n        yope3d.draw_line(yope3d.Vec3(3.65, 2.05, 4.92), yope3d.Vec3(2.88, 2.44, 3.38))\n        yope3d.set_velocity(entity, yope3d.Vec3(0.7, 0.95, 0.41))\n        v = yope3d.input.is_rmb_down()\n        form = yope3d.reg_get(entity, \"OBBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.74, 0.38, 0.83)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.params_blob = \"crate\"\n        c = yope3d.reg_add(entity, \"CapsuleForm\")\n        c.radius = 1.12\n        c.half_height = 0.45\n\n    def init(self, world, entity, params):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.79, 2.71, yope3d.EASE_CUBIC_IN_OUT)\n        t = yope3d.reg_add(entity, \"UITexturedBackground\")\n        t.tint_g = 0.22\n        t.has_texture = False\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.intensity = 6.46\n        m.constant = 0.31\n        m.direction = yope3d.Vec3(0.87, 0.34, 0.48)\n        m.position = yope3d.Vec3(0.46, 0.65, 0.72)\n        m.color = yope3d.Vec3(4.53, 3.71, 1.86)\n        c = yope3d.reg_add(entity, \"Parent\")\n        c.parent = entity\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.9)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.37, 1.9, yope3d.EASE_QUAD_IN)\n        world.remove_entity(entity)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.constant = 0.8\n        t.position = yope3d.Vec3(0.83, 0.12, 0.94)\n        t.type = 0.91\n        t.color = yope3d.Vec3(2.37, 0.55, 3.98)\n        t.direction = yope3d.Vec3(0.16, 0.67, 0.87)\n\n    def update(self, world, entity, dt):\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.99)\n        yope3d.load_game(\"crate_01\")\n        wheel = world.add_ui_background(yope3d.Vec2(1.97, 2.3), yope3d.Vec2(1.99, 2.72), yope3d.Vec4(2.29, 2.28, 3.24, 4.34))\n        yope3d.audio.pause_all()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        r = world.add_vehicle(other, [])\n        world.add_cone_twist_joint(entity, entity, yope3d.Vec3(0.89, 4.66, 3.43), yope3d.Vec3(0.34, 0.07, 0.7))\n        yope3d.set_text(other, \"Paused\")\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 22.18, 0))\n        target = world.add_kinematic_sphere(yope3d.Vec3(2.35, 4.74, 1.54), 0.18)\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.casts_shadow = False\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.6\n        t.k = 0.9\n\n",
   "target": "KEY_GRAVE_ACCENT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0110.py:51:8:observe_collisions",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0110.py",
   "line": 51,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth110Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.38},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_key_pressed(yope3d.KEY_MINUS):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, cylind, name in yope3d.view(\"CylinderForm\", \"Name\"):\n            cylind.radius = 1.68\n        e = world.add_text_label_3d(\"Score: 0\", \"Press E to interact\", yope3d.Vec3(4.47, 1.12, 4.65))\n        world.apply_impulse_at(other, yope3d.Vec3(23.35, 2.57, 36.97), yope3d.Vec3(0.42, 0.56, 0.55))\n        if yope3d.input.is_key_down(yope3d.KEY_TAB):\n            world.apply_impulse(entity, yope3d.Vec3(0, 21.37, 0))\n        c = yope3d.reg_get(entity, \"TextLabel3D\")\n        if c is not None:\n            c.b = 0.95\n        form = yope3d.reg_get(entity, \"SpringConstraint\")\n        if form is not None:\n            form.rest_length = 0.61\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"Name\")\n        m.value = \"player\"\n        c = yope3d.reg_get(entity, \"Parent\")\n        if c is not None:\n            c.parent = entity\n        if yope3d.input.is_key_pressed(yope3d.KEY_UP):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        hull = yope3d.reg_add(entity, \"Name\")\n        hull.value = \"player\"\n        v = yope3d.reg_has(entity, \"Material\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.43, 0, 4.42) - origin).normalize())\n\n    def init(self, world, entity, params):\n        target = world.add_kinematic_sphere(yope3d.Vec3(3.08, 1.08, 1.45), 1.12)\n        world.attach_cylinder_mesh(target, 0.28, 0.88)\n        for ent, hull, uitran in yope3d.view(\"Hull\", \"UITransform\"):\n            hull.velocity = yope3d.Vec3(0.5, 0.56, 0.14)\n        prop = world.add_trigger_sphere(yope3d.Vec3(2.17, 0.53, 2.2), 0.55)\n        yope3d.settings.set_bool(\"hud_root\", True)\n        wheel = world.add_trigger_box(yope3d.Vec3(4.82, 0.23, 1.78), yope3d.Vec3(0.69, 0.17, 0.1))\n\n    def update(self, world, entity, dt):\n        yope3d.",
   "suffix": "        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_r = 2.72\n        hull.hover_g = 1.32\n        hull.hover_r = 3.22\n        hull.normal_g = 0\n        form = yope3d.reg_get(entity, \"ScriptComponent\")\n        if form is not None:\n            form.params_blob = \"ground\"\n        for ent, textla, aabbfo in yope3d.view(\"TextLabel3D\", \"AABBForm\"):\n            textla.billboard = 2\n",
   "target": "observe_collisions",
   "rest": "(4, 0.93)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0054.py:55:39:KEY_2",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0054.py",
   "line": 55,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth054Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.1},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.quadratic = 0.06\n        c = yope3d.reg_get(entity, \"CapsuleForm\")\n        if c is not None:\n            c.radius = 1.1\n        cmp = yope3d.reg_add(entity, \"Parent\")\n        cmp.parent = entity\n        world.set_paused(False)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 7.61)\n\n    def on_ui_press(self, world, entity):\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.12)\n        world.tween_ui_opacity(entity, 0.7, 0.54, 2)\n        world.set_paused(False)\n        world.attach_cylinder_collider(entity, 78.35, 1.66, 1.06)\n        world.set_skybox([])\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_3):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        hull = yope3d.reg_get(entity, \"Name\")\n        if hull is not None:\n            hull.value = \"hud_root\"\n        world.add_spring_with_proxies(entity, entity, 3.87, 0.63, 4, 1.59)\n        v = yope3d.save_path(\"models/prop.glb\")\n        r = world.ui_hovered()\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"UIButton\")\n        m.hover_r = 3.74\n        m.hover_g = 0.37\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 18.82)\n        for ent, materi, aabbfo in yope3d.view(\"Material\", \"AABBForm\"):\n            materi.occlusion_map = \"textures/crate_ao.png\"\n        for ent, pointj, aabbfo in yope3d.view(\"PointJointConstraint\", \"AABBForm\"):\n            pointj.target = entity\n        world.set_ui_parent(entity, entity)\n        wheel = world.add_trigger_sphere(yope3d.Vec3(3.37, 3.76, 4.17), 1.68)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n",
   "target": "KEY_2",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0047.py:18:52:EASE_QUAD_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0047.py",
   "line": 18,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth047Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.21},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.max_x = 3.36\n        m.max_y = 0.37\n        v = yope3d.reg_add(entity, 1.56)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.53)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.95, 0.67, yope3d.",
   "suffix": "        n = yope3d.Vec3(2.47, 0, 0.57).normalize()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 20.62)\n        c = yope3d.reg_add(entity, \"Material\")\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.albedo_map = \"textures/crate_albedo.png\"\n        c.albedo = (4.36, 2.36, 0.81, 4.49)\n",
   "target": "EASE_QUAD_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0031.py:51:12:is_fixed",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0031.py",
   "line": 51,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth031Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_sphere_collider(entity, 88.72, 0.66)\n        v = yope3d.get_behavior(entity)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.scale = yope3d.Vec3(0.48, 1.66, 0.66)\n        t.rotation = yope3d.Quat()\n        t.position = yope3d.Vec3(0.46, 0.44, 0.61)\n        for ent, sphere, uibutt in yope3d.view(\"SphereForm\", \"UIButton\"):\n            sphere.radius = 0.68\n        world.remove_spring_between(other, entity)\n        cmp = yope3d.reg_add(entity, \"Name\")\n        cmp.value = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.window.get_f11_fullscreen()\n        wheel = world.add_audio_source_entity()\n        world.attach_sphere_mesh(entity, 0.66, 3.83, 2.63, 1.47, 0)\n        for ent, uiback, capsul in yope3d.view(\"UIBackground\", \"CapsuleForm\"):\n            uiback.a = 2.51\n\n    def init(self, world, entity, params):\n        r = world.get_contact_count()\n        if yope3d.input.is_key_pressed(yope3d.KEY_F):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_pressed(yope3d.KEY_9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"AudioSource\")\n        m.gain = 0.51\n        m.path = \"models/prop.glb\"\n        m.autoplay = True\n        m.pitch = 1.11\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.45\n        t.k = 3.1\n        t.target = entity\n\n    def update(self, world, entity, dt):\n        yope3d.camera.look_at(yope3d.Vec3(0.61, 0.29, 0.37))\n        prop = world.add_kinematic_capsule(1.5, 0.56)\n        v = yope3d.window.get_f11_fullscreen()\n        v = yope3d.",
   "suffix": "        target = world.add_cylinder(1.07, 0.49, 74.37)\n        v = yope3d.reg_has(entity, \"Material\")\n        c = yope3d.reg_add(entity, \"AnimationPlayer\")\n        c.clip = \"hud_root\"\n        c.speed = 4.12\n        c.time = 0.66\n        c.loop = 0\n        world.attach_box_mesh(entity, yope3d.Vec3(4.28, 0.62, 2.95))\n\n",
   "target": "is_fixed",
   "rest": "(prop)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0017.py:56:12:to_radians",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0017.py",
   "line": 56,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth017Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.51},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"UITexturedBackground\")\n        t.tint_b = 0.4\n        t.path = \"audios/impact.ogg\"\n        t.has_texture = True\n        t.tint_g = 0.83\n        t.tint_r = 0.57\n        world.add_cone_twist_joint(entity, entity, yope3d.Vec3(3.64, 1.18, 1.49), yope3d.Vec3(0.93, 0.31, 0.35), 0.61, 0.32, False)\n        v = yope3d.find_entity(\"ground\")\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.02)\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.omega = yope3d.Vec3(0.95, 1.72, 2.31)\n        t.linear_damping = 0.34\n        v = yope3d.reg_has(entity, \"AABBForm\")\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.14)\n\n    def update(self, world, entity, dt):\n        v = yope3d.input.is_mouse_released(1)\n        h = world.add_vehicle(entity, [])\n        v = yope3d.input.is_key_pressed(3)\n        yope3d.draw_line(yope3d.Vec3(1.69, 1.77, 1.02), yope3d.Vec3(1.96, 1.81, 1.75))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.21)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.36, 1.87, yope3d.EASE_QUAD_OUT)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.75, 0, 1.72) - origin).normalize())\n\n    def on_ui_press(self, world, entity):\n        world.remove_spring_between(entity, entity)\n        v = yope3d.get_world_position(entity)\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 3.13\n        c.r = 4.06\n        c.g = 1.3\n        prop = world.add_ui_background(yope3d.Vec2(3.85, 0.69), yope3d.Vec2(2.68, 3.2), yope3d.Vec4(0.61, 5, 4.24, 2.78), 3)\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.velocity = yope3d.Vec3(0.11, 0.99, 0.19)\n        c.omega = yope3d.Vec3(0.55, 0.64, 1.72)\n        c.linear_damping = 0.14\n        c.mass = 36.78\n\n    def on_collision_enter(self, world, entity, other, contact):\n        cmp = yope3d.reg_add(entity, \"CylinderForm\")\n        cmp.radius = 1.02\n        cmp.half_height = 0.59\n        r = yope3d.",
   "suffix": "        world.apply_impulse_at(entity, yope3d.Vec3(25.35, 3.1, 23.87), yope3d.Vec3(0.9, 0.84, 0.2))\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.local_axis_b = yope3d.Vec3(2.2, 4.93, 2.52)\n        t = yope3d.reg_add(entity, \"AABBForm\")\n        t.extent = yope3d.Vec3(0.99, 0.11, 0.75)\n\n",
   "target": "to_radians",
   "rest": "(21.0)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0114.py:62:12:scene_payload",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0114.py",
   "line": 62,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth114Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.74},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.63, 2.47, yope3d.EASE_CUBIC_IN)\n        other = world.add_aabb(yope3d.Vec3(0.58, 0.36, 0.83), 79.89, yope3d.Vec3(3.87, 1.89, 0.58))\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.local_anchor_b = yope3d.Vec3(2.12, 3.41, 0.92)\n        v = yope3d.reg_add(entity, 0.08)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.attach_script(entity, \"ground\", \"player\")\n        hull = yope3d.reg_add(entity, \"OBBForm\")\n        hull.extent = yope3d.Vec3(0.21, 0.49, 0.79)\n        for ent, hingej in yope3d.view(\"HingeJointConstraint\"):\n            hingej.local_anchor_b = yope3d.Vec3(3.1, 1.9, 4.78)\n        v = yope3d.window.get_f11_fullscreen()\n        h = world.get_ui_focus()\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.intensity = 2.43\n        yope3d.window.set_escape_closes(True)\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_down(yope3d.KEY_R):\n            world.apply_impulse(entity, yope3d.Vec3(0, 15.27, 0))\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"ground\"\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 3.11)\n        body = world.add_cylinder(0.96, 0.54, 87.43, yope3d.Vec3(4.31, 4.93, 1.23))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.6)\n        for ent, cylind in yope3d.view(\"CylinderForm\"):\n            cylind.radius = 1.74\n        yope3d.window.set_tab_pauses(False)\n\n    def init(self, world, entity, params):\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.path = \"textures/tnail.png\"\n        yope3d.scene_manager.load_scene(\"scenes/level_01.yscene\")\n        for ent, materi, uibutt in yope3d.view(\"Material\", \"UIButton\"):\n            materi.albedo_map = \"textures/crate_albedo.png\"\n        c = yope3d.reg_get(entity, \"LightSource\")\n        if c is not None:\n            c.direction = yope3d.Vec3(0.22, 0.09, 0.65)\n        other = world.add_obb(yope3d.Vec3(0.44, 0.31, 0.63), 23.94, yope3d.Vec3(4.5, 1.62, 4))\n        world.apply_impulse_at(entity, yope3d.Vec3(39.32, 19.39, 25.12), yope3d.Vec3(0.78, 0.64, 0.02))\n        form = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if form is not None:\n            form.local_anchor_a = yope3d.Vec3(3.01, 1.22, 0.42)\n        v = yope3d.",
   "suffix": "\n",
   "target": "scene_payload",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:88:14:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 88,
   "col": 14,
   "prefix": "\"\"\"Physics gallery \u2014 the 39 validation scenes from the pre-scripting engine.\n\nPorted from the scene switcher that lived in Engine::loadScene at commit b0b630a\n(May 2026), before scripting existed. Each scene isolates one narrowphase pair or\none solver property (resting stability, deflection, stacking, spring rest length),\nso a regression shows up as a body that jitters, sinks, spins wrong, or passes\nthrough something it shouldn't.\n\n    0-4    Sphere vs floor          \u2014 drop, high-speed drop, shallow approach, rest, roll\n    5-8    Sphere vs floating panel \u2014 center hit, edge approach, edge miss, rest\n    9-13   Sphere vs sphere         \u2014 head-on, glancing, one fixed, resting stack, high-speed\n    14-18  Sphere vs AABB           \u2014 drop on top, hit side, floor only, start inside, high-speed\n    19-22  Springs                  \u2014 two free, one anchored, compressed, stretched\n    23-26  AABB vs floor            \u2014 drop, high-speed drop, rest, 45-degree ramp\n    27-29  AABB vs floating panel   \u2014 center hit, edge miss, rest\n    30-32  AABB vs AABB             \u2014 drop on static, two dynamic, stacking\n    33     Sphere on the 45-degree ramp (compare against scene 26)\n    34-38  OBB                      \u2014 drop, sphere vs OBB, drop on static AABB, head-on, rest\n\nPORTING NOTE \u2014 the originals were built on physics::Barrier / BoundedBarrier (an\ninfinite plane and a finite one) plus the CCD collider, all deleted in M12 when\nphysics went ECS-native. Here the infinite floor is a 100x100x1 static AABB, the\nbounded panel is a thin static AABB, and the angled barrier is a fixed rotated OBB\n\u2014 same surfaces at the same heights, so every drop height and rest position below\nis unchanged. Two consequences worth knowing when you read the results:\n\n  * The \"no tunneling\" scenes (1, 13, 18, 24) were CCD tests. Nothing sweeps the\n    swept volume now, so a fast enough body CAN tunnel \u2014 these are honest discrete-\n    solver checks today, not guarantees.\n  * The \"edge miss\" scenes (7, 28) dropped a body just outside a *point-tested*\n    panel boundary so it fell straight through. A real AABB panel has corners, so\n    the body now clips the corner and deflects instead of passing cleanly by.\n\nAttach to any entity in a scene file (assets/scenes/physicsGallery.yscene does it).\n\nControls: LEFT/RIGHT switch scene, UP/DOWN pick spawn shape, LMB fire it,\nWASD+mouse fly, P collider overlay, H hide HUD.\n\"\"\"\nimport math\n\nimport yope3d\n\nfrom behaviors._gallery import GalleryBase\n\nFLOOR = (0.35, 0.30, 0.25)\nPANEL = (0.5, 0.7, 0.4)\nPROP = (0.7, 0.5, 0.3)\nBOX_PURPLE = (0.7, 0.3, 0.7)\n\nBALL = (0.2, 0.6, 1.0)\nBALL_HOT = (1.0, 0.4, 0.2)\nBALL_FAST = (1.0, 0.2, 0.2)\nBALL_MISS = (1.0, 0.3, 0.3)\nFIXED = (0.8, 0.8, 0.2)\n\nCUBE = (0.3, 0.7, 0.4)\nCUBE_FAST = (1.0, 0.3, 0.2)\nCUBE_MISS = (1.0, 0.3, 0.3)\nOBB_A = (0.8, 0.4, 0.1)\nOBB_B = (0.2, 0.5, 0.9)\n\nR = 0.5          # sphere radius used by every scene\nH = (0.5, 0.5, 0.5)   # standard box half-extents\n\n\nclass PhysicsGallery(GalleryBase):\n\n    # ---- Terrain helpers -------------------------------------------------\n\n    def plane_floor(self, world):\n        \"\"\"Top face at y=0 \u2014 a sphere of radius 0.5 rests at y=0.5.\"\"\"\n        self.static_box(world, (0.0, -0.5, 0.0), (50.0, 0.5, 50.0), FLOOR)\n\n    def aabb_floor(self, world):\n        \"\"\"Top face at y=0.5 \u2014 a sphere of radius 0.5 rests at y=1.0.\"\"\"\n        self.static_box(world, (0.0, 0.0, 0.0), (50.0, 0.5, 50.0), FLOOR)\n\n    def panel(self, world, y, s):\n        \"\"\"Floating slab, half-extent s, top face exactly at y.\"\"\"\n        self.static_box(world, (0.0, y - 0.05, 0.0), (s, 0.05, s), PANEL)\n\n    def ramp(self, world):\n        \"\"\"45-degree slope through the origin (surface normal (1,1,0)/sqrt2).\n\n        Offset half a thickness down the normal so the slab's TOP face \u2014 not its\n        center \u2014 passes through the origin, matching the old infinite barrier plane.\n        \"\"\"\n        rot = yope3d.",
   "suffix": "        self.obb(world, (30.0, 0.5, 30.0), 1.0, (-0.354, -0.354, 0.0), FLOOR,\n                 rot=rot, fixed=True)\n\n    # ---- Sphere vs floor -------------------------------------------------\n\n    def s_drop(self, world):\n        self.plane_floor(world)\n        self.sphere(world, 1.0, R, (0, 8, 0), BALL)\n        self.set_camera((0.0, 4.0, 16.0), pitch=-0.1)\n\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(-45.0))",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0012.py:18:12:clamp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0012.py",
   "line": 18,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth012Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.3},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(90.0)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.57, 0, 1.87)).length()\n        world.attach_sphere_mesh(entity, 0.21)\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.69, 0.32, 0.67)\n        v = yope3d.",
   "suffix": "        m = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if m is not None:\n            m.tint_g = 0.7\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.11)\n        if yope3d.input.is_key_down(yope3d.KEY_RIGHT_ALT):\n            world.apply_impulse(entity, yope3d.Vec3(0, 2.17, 0))\n\n    def init(self, world, entity, params):\n",
   "target": "clamp",
   "rest": "(3.34, 0.14, 0.12)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0101.py:44:39:KEY_F8",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0101.py",
   "line": 44,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth101Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.23},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.39, 0.04, 0.39)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_a = 0.4\n        c.normal_r = 3.38\n        c.hover_g = 0.11\n        c.hover_r = 0.74\n        c.normal_b = 4.16\n        v = yope3d.reg_add(entity, 0.07)\n        c = yope3d.reg_get(entity, \"Hull\")\n        if c is not None:\n            c.gravity = False\n        world.apply_impulse(entity, yope3d.Vec3(33.96, 31.06, 32.55))\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.asset_path = \"audios/impact.ogg\"\n        m.loaded = False\n\n    def update(self, world, entity, dt):\n        v = yope3d.camera.screen_to_ray(2.94, 3.83)\n        world.attach_obb_collider(entity, 71.39, yope3d.Vec3(0.73, 0.9, 0.47))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.23)\n        t = yope3d.clamp(dt * 4.59, 0.0, 1.0)\n        v = yope3d.lerp(3.29, 2.5, t)\n        body = world.add_obb(yope3d.Vec3(0.03, 0.98, 0.52), 48.15)\n\n    def on_ui_press(self, world, entity):\n        world.attach_obb_collider(entity, 84.17, yope3d.Vec3(0.37, 0.03, 0.39), True)\n        r = yope3d.to_radians(116.0)\n        yope3d.set_position(entity, yope3d.Vec3(3.81, 3.95, 4.06))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.97)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.29, 2.47, yope3d.EASE_CUBIC_OUT)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_capsule_collider(other, 65.56, 0.19, 0.39)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(2.49, 0, 2.13)).length()\n        r = yope3d.to_radians(77.0)\n        for ent, conetw, script in yope3d.view(\"ConeTwistJointConstraint\", \"ScriptComponent\"):\n            conetw.local_twist_axis_a = yope3d.Vec3(0.99, 0.78, 0.58)\n        world.attach_cylinder_mesh(entity, 1.66, 0.51, 3.22, 2.62, 0.23)\n",
   "target": "KEY_F8",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0059.py:35:39:KEY_GRAVE_ACCENT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0059.py",
   "line": 35,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth059Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        v = yope3d.reg_get(entity, 4.56)\n        if yope3d.input.is_key_pressed(yope3d.KEY_INSERT):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_a = yope3d.Vec3(3.24, 0.87, 4.06)\n        m.target = entity\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 0.43\n        hull.half_height = 0.58\n        for ent, pointj, conetw in yope3d.view(\"PointJointConstraint\", \"ConeTwistJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(1.51, 2.65, 3.74)\n        v = yope3d.to_radians(1.56)\n\n    def on_ui_press(self, world, entity):\n        world.set_skybox([])\n        yope3d.window.set_escape_closes(True)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"ground\"\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.position = yope3d.Vec3(0.75, 0.49, 0.84)\n        m.constant = 0.46\n        m.color = yope3d.Vec3(2.96, 1.16, 2.91)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        v = yope3d.capsule_overlap(yope3d.Vec3(2.14, 4.84, 2.47), 1.08, 0.34)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.set_master_gain(0.58)\n        res = world.get_registry()\n        c = yope3d.reg_get(entity, \"CompoundCollider\")\n        if c is not None:\n            c.asset_path = \"audios/impact.ogg\"\n        n = yope3d.Vec3(0.83, 0, 1).normalize()\n",
   "target": "KEY_GRAVE_ACCENT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:139:27:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 139,
   "col": 27,
   "prefix": "brake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.",
   "suffix": "        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n",
   "target": "Vec3",
   "rest": "(0.0, 5.0, -9.0)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "typings/yope3d/__init__.pyi:33:8:spawn",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 33,
   "col": 8,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.",
   "suffix": "",
   "target": "spawn",
   "rest": "(\"prefabs/enemy.ytemplated\", spawn_point.position)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0109.py:32:39:KEY_8",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0109.py",
   "line": 32,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth109Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.31},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        yope3d.load_scene(\"scenes/menu.yscene\")\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.normal_scale = 1.84\n        for ent, cylind, aabbfo in yope3d.view(\"CylinderForm\", \"AABBForm\"):\n            cylind.half_height = 1.1\n        for ent, spring, audios in yope3d.view(\"SpringConstraint\", \"AudioSource\"):\n            spring.rest_length = 0.24\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.lower_angle = -1.11\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.94)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.57, 1.05, yope3d.EASE_CUBIC_IN_OUT)\n        for ent, name in yope3d.view(\"Name\"):\n            name.value = \"hud_root\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.r = 0.24\n        c.b = 2.73\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"Hull\")\n        m.velocity = yope3d.Vec3(0.97, 0.06, 0.34)\n        m.friction = 0.72\n        m.omega = yope3d.Vec3(0.33, 3.39, 1.36)\n        v = yope3d.window.is_cursor_locked()\n        if yope3d.input.is_key_pressed(yope3d.KEY_BACKSPACE):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        rot = yope3d.Quat.from_euler(0.68, 1.32)\n        prop = world.add_cylinder(0.32, 0.25, 34.47, yope3d.Vec3(2.29, 4.24, 3.86))\n",
   "target": "KEY_8",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:275:12:input",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 275,
   "col": 12,
   "prefix": "        grid = [[None] * GRID_N for _ in range(GRID_N)]\n        half = (NODE_HALF, NODE_HALF, NODE_HALF)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N):\n                if horizontal:\n                    pos = (-half_w + i * GRID_STEP, top_y, -half_w + j * GRID_STEP)\n                else:\n                    pos = (-half_w + i * GRID_STEP, top_y - j * GRID_STEP, 0.0)\n\n                fi = i / (GRID_N - 1)\n                fj = j / (GRID_N - 1)\n\n                if shape == 0:\n                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.audio.load_sound(\"audios/test.ogg\")\n        if buf is not None:\n            self.doppler_src = yope3d.audio.create_source(buf)\n            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n        self.self_collide = bool(params.get(\"cloth_self_collision\", False))\n        self.cloth_nodes = []\n        self.prev_v = False\n        GalleryBase.init(self, world, entity, params)\n\n    def on_update(self, world, dt):\n        v = yope3d.",
   "suffix": "        if v and not self.prev_v and self.cloth_nodes:\n            self.self_collide = not self.self_collide\n            self.apply_cloth_filter(world)\n            self.refresh_stats(world)\n        self.prev_v = v\n\n        # The audio source is not an entity, so it needs a manual per-frame sync of\n        # position + velocity (velocity is what OpenAL derives the Doppler shift from).\n        src = getattr(self, \"doppler_src\", None)\n        ent = getattr(self, \"doppler_ent\", None)\n",
   "target": "input",
   "rest": ".is_key_down(yope3d.KEY_V)",
   "freq": 15,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0110.py:12:39:KEY_MINUS",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0110.py",
   "line": 12,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth110Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.38},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, cylind, name in yope3d.view(\"CylinderForm\", \"Name\"):\n            cylind.radius = 1.68\n        e = world.add_text_label_3d(\"Score: 0\", \"Press E to interact\", yope3d.Vec3(4.47, 1.12, 4.65))\n        world.apply_impulse_at(other, yope3d.Vec3(23.35, 2.57, 36.97), yope3d.Vec3(0.42, 0.56, 0.55))\n        if yope3d.input.is_key_down(yope3d.KEY_TAB):\n            world.apply_impulse(entity, yope3d.Vec3(0, 21.37, 0))\n        c = yope3d.reg_get(entity, \"TextLabel3D\")\n        if c is not None:\n            c.b = 0.95\n",
   "target": "KEY_MINUS",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0049.py:55:52:EASE_CUBIC_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0049.py",
   "line": 55,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth049Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.73},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.fade_gain(entity, 0.82, 1.59)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.26, 0.64, yope3d.EASE_QUAD_IN_OUT)\n        yope3d.audio.set_bus_gain(1, 0.47)\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 3.1\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.77, 0, 4.51) - origin).normalize())\n        if yope3d.input.is_key_down(yope3d.KEY_F2):\n            world.apply_impulse(entity, yope3d.Vec3(0, 4.76, 0))\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_get(entity, \"SpringConstraint\")\n        if c is not None:\n            c.rest_length = 0.3\n        world.apply_impulse(entity, yope3d.Vec3(29.62, 1.29, 20.19))\n        r = yope3d.to_radians(6.0)\n        rot = yope3d.Quat.from_euler(0.23, -0.88)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(1.25, 0, 3.52)).length()\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        v = yope3d.capsule_cast(yope3d.Vec3(1.11, 3.61, 0.69), 0.87, 0.69, yope3d.Vec3(3.42, 0.19, 0.76), 0.82)\n        other = world.add_static_aabb(yope3d.Vec3(2.23, 1.32, 4.94), yope3d.Vec3(0.31, 0.51, 0.21))\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.loop = 3\n        wheel = world.add_sphere(20.84, 1.87, yope3d.Vec3(0.12, 1.38, 1.48))\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"crate\"\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.44, 0, 0.34) - origin).normalize())\n        yope3d.audio.set_master_gain(0.91)\n        rot = yope3d.Quat.from_euler(-0.48, -0.18)\n        v = yope3d.get_world_position(entity)\n\n    def update(self, world, entity, dt):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.35)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.22, 1.35, yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_TAB):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        m = yope3d.reg_add(entity, \"UITexturedBackground\")\n        m.has_texture = True\n        m.tint_b = 0.13\n        m.tint_a = 0.29\n        m.tint_r = 0.13\n        m.tint_g = 0.02\n        v = yope3d.input.is_key_released(2)\n        world.set_wheel_steer(entity, 1.27)\n",
   "target": "EASE_CUBIC_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0104.py:54:36:KEY_G",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0104.py",
   "line": 54,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth104Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.08},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        world.tween_ui_opacity(entity, 0.78, 0.41, 4)\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.42, 0.71, 0.02)\n        world.add_spring(entity, entity, 4.93, 0.16)\n        n = yope3d.Vec3(1.5, 0, 2.02).normalize()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.23)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.7, 2.48, yope3d.EASE_QUAD_IN)\n        if yope3d.input.is_key_down(yope3d.KEY_F8):\n            world.apply_impulse(entity, yope3d.Vec3(0, 22.26, 0))\n        yope3d.set_position(entity, yope3d.Vec3(4.49, 4.35, 4.47))\n\n    def on_ui_press(self, world, entity):\n        rot = yope3d.Quat.from_euler(0.38, 1.39)\n        anchor = world.add_cylinder(0.26, 0.21, 76.82)\n        for ent, conetw, capsul in yope3d.view(\"ConeTwistJointConstraint\", \"CapsuleForm\"):\n            conetw.swing_limit = 1.14\n        target = world.add_text_label_3d(\"Paused\", \"Press E to interact\", yope3d.Vec3(2.56, 2.54, 1.78))\n        n = yope3d.Vec3(1.86, 0, 3.12).normalize()\n        if yope3d.input.is_key_pressed(yope3d.KEY_F4):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.path = \"audios/impact.ogg\"\n        t.gain = 0.38\n        t.loop = False\n        t.bus = 0\n        yope3d.settings.set_bool(\"player\", True)\n\n    def update(self, world, entity, dt):\n        v = yope3d.to_degrees(1.36)\n        world.set_ui_group_visible(entity, False)\n        cmp = yope3d.reg_add(entity, \"Hull\")\n        cmp.linear_damping = 0.06\n        cmp.friction = 0.58\n        world.detach_physics_body(entity)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.window.get_escape_closes()\n        n = yope3d.Vec3(4.67, 0, 0.65).normalize()\n        world.attach_cylinder_collider(other, 48.56, 0.55, 0.36)\n        v = yope3d.reg_get(other, 0.14)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 6.14, 0))\n\n",
   "target": "KEY_G",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:30:8:reg_add",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 30,
   "col": 8,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.",
   "suffix": "    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "reg_add",
   "rest": "(e, \"Material\")",
   "freq": 3,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0118.py:15:8:load_scene",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0118.py",
   "line": 15,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth118Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.96},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if hull is not None:\n            hull.target = entity\n        yope3d.",
   "suffix": "        v = yope3d.input.get_mouse_delta()\n        v = yope3d.input.is_mouse_down(4)\n        v = yope3d.window.get_width()\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.78\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.params_blob = \"player\"\n        cmp.script_class = \"player\"\n\n    def init(self, world, entity, params):\n",
   "target": "load_scene",
   "rest": "(\"scenes/level_01.yscene\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0072.py:45:17:spawn",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0072.py",
   "line": 45,
   "col": 17,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth072Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.98},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        world.set_paused(False)\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"player\"\n        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.61, 0.73, 0.72)\n        yope3d.settings.remove(\"crate\")\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n        hull.font = \"fonts/inter_ascii.msdf\"\n        hull.r = 1.14\n        hull.a = 4.99\n        cmp = yope3d.reg_add(entity, \"CapsuleForm\")\n        cmp.radius = 1.49\n        cmp.half_height = 1.03\n\n    def init(self, world, entity, params):\n        for ent, uibutt, name in yope3d.view(\"UIButton\", \"Name\"):\n            uibutt.disabled_a = 0.91\n        body = world.add_kinematic_box(yope3d.Vec3(4.07, 3.38, 3.39), yope3d.Vec3(0.98, 0.89, 0.41))\n        for ent, hingej, materi in yope3d.view(\"HingeJointConstraint\", \"Material\"):\n            hingej.local_anchor_b = yope3d.Vec3(3.21, 3.19, 4.37)\n        world.clear_shadow_caster()\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.05)\n        world.add_cone_twist_joint(body, body, yope3d.Vec3(4.48, 1.37, 4.12), yope3d.Vec3(0.54, 0.3, 0.1))\n        yope3d.observe_collisions(1, 2.62)\n        h = world.ui_consumed_click()\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"Name\")\n        m.value = \"player\"\n        hull = yope3d.reg_add(entity, \"Transform\")\n        hull.rotation = yope3d.Quat()\n        hull.scale = yope3d.Vec3(1.21, 0.43, 0.44)\n        hull.position = yope3d.Vec3(0.28, 0.02, 0.85)\n        target = yope3d.",
   "suffix": "        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 0.62\n        m.half_height = 0.86\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.asset_path = \"models/prop.glb\"\n        t.loaded = False\n        for ent, hull in yope3d.view(\"Hull\"):\n            hull.sleeping_enabled = True\n\n    def on_collision_enter(self, world, entity, other, contact):\n",
   "target": "spawn",
   "rest": "(\"models/prop.glb\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0052.py:29:12:settings",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0052.py",
   "line": 29,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth052Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.53},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        for ent, materi, capsul in yope3d.view(\"Material\", \"CapsuleForm\"):\n            materi.albedo_map = \"textures/crate_albedo.png\"\n        v = yope3d.clamp(1.41, 3.98, 0.36)\n        for ent, textla, conetw in yope3d.view(\"TextLabel3D\", \"ConeTwistJointConstraint\"):\n            textla.size_meters = 0.54\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n        if hull is not None:\n            hull.script_class = \"crate\"\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.loaded = True\n        t.asset_path = \"audios/impact.ogg\"\n        for ent, name, conetw in yope3d.view(\"Name\", \"ConeTwistJointConstraint\"):\n            name.value = \"ground\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.get_world_position(entity)\n        yope3d.audio.stop_all()\n        v = yope3d.",
   "suffix": "        world.add_cone_twist_joint(other, entity, yope3d.Vec3(1.32, 3.78, 1.76), yope3d.Vec3(0.27, 0.29, 0.26), 1.45, 0.38, True)\n        prop = world.add_aabb(yope3d.Vec3(0.38, 0.51, 0.46), 24.77, yope3d.Vec3(4.21, 1.08, 4.54))\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.rest_length = 0.78\n        hull.k = 2.87\n\n    def init(self, world, entity, params):\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.display_px = 3\n        yope3d.camera.set_rotation(yope3d.Vec3(2.41, 3.95, 4.85))\n",
   "target": "settings",
   "rest": ".save()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0099.py:21:15:spawn",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0099.py",
   "line": 21,
   "col": 15,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth099Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.76)\n        yope3d.observe_collisions(4, 3.23)\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 0.11\n        t.font = \"fonts/inter_ascii.msdf\"\n        t.text = \"Paused\"\n        t.g = 2.49\n        prop = yope3d.",
   "suffix": "        h = world.get_registry()\n        res = world.ui_consumed_click()\n        world.set_ui_focus(entity)\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n        if hull is not None:\n            hull.params_blob = \"hud_root\"\n\n    def on_ui_press(self, world, entity):\n        world.attach_cylinder_mesh(entity, 0.44, 0.43, 2.54, 4.18, 3.81)\n        v = yope3d.window.is_cursor_locked()\n",
   "target": "spawn",
   "rest": "(\"textures/tnail.png\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0004.py:47:12:look_at",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0004.py",
   "line": 47,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth004Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.77},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(98.0)\n        v = yope3d.attach_script(entity, \"hud_root\", \"player\")\n        form = yope3d.reg_get(entity, \"Name\")\n        if form is not None:\n            form.value = \"player\"\n        n = yope3d.Vec3(2.12, 0, 3.27).normalize()\n        h = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        rot = yope3d.Quat.from_euler(1.25, -0.43)\n        m = yope3d.reg_get(entity, \"SpringConstraint\")\n        if m is not None:\n            m.k = 1.26\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"player\"\n        hull = yope3d.reg_get(entity, \"TextLabel3D\")\n        if hull is not None:\n            hull.billboard = 0\n\n    def init(self, world, entity, params):\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.params_blob = \"crate\"\n        world.attach_sphere_collider(entity, 23.6, 1.85, True)\n        rot = yope3d.Quat.from_euler(-0.37, 0.91)\n        body = world.add_text_label_3d(\"Press E to interact\", \"Press E to interact\", yope3d.Vec3(2.14, 1.59, 0.79))\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.params_blob = \"crate\"\n        c.script_class = \"hud_root\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.23)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.7, 1.45, yope3d.EASE_QUAD_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.to_radians(2.05)\n        origin = yope3d.get_position(entity)\n        q = yope3d.",
   "suffix": "        yope3d.window.set_f11_fullscreen(False)\n        v = yope3d.capsule_overlap(yope3d.Vec3(3.68, 4.06, 0.89), 0.34, 1.03)\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1.43\n\n",
   "target": "look_at",
   "rest": "((yope3d.Vec3(1.23, 0, 1.35) - origin).normalize())",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0091.py:61:34:BUS_MUSIC",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0091.py",
   "line": 61,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth091Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.7},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"SphereForm\")\n        m.radius = 0.2\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.b = 0.1\n        rot = yope3d.Quat.from_euler(-0.1, -0.88)\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.quadratic = 0.57\n        world.remove_spring_between(entity, entity)\n\n    def on_ui_press(self, world, entity):\n        h = world.add_vehicle(entity, [])\n        if yope3d.input.is_key_down(yope3d.KEY_F12):\n            world.apply_impulse(entity, yope3d.Vec3(0, 5.66, 0))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 16.77)\n        t = yope3d.reg_add(entity, \"SpringConstraint\")\n        t.rest_length = 0.92\n        t.target = entity\n        t.k = 2.65\n        for ent, script, uiback in yope3d.view(\"ScriptComponent\", \"UIBackground\"):\n            script.script_class = \"player\"\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.asset_path = \"textures/tnail.png\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        rot = yope3d.Quat.from_euler(1.05, -0.83)\n        hull = yope3d.reg_add(entity, \"ScriptComponent\")\n        hull.script_class = \"ground\"\n        hull.params_blob = \"ground\"\n        hull = yope3d.reg_add(entity, \"CompoundCollider\")\n        hull.loaded = True\n        hull.asset_path = \"textures/tnail.png\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.loop = 2\n\n    def update(self, world, entity, dt):\n        h = world.add_vehicle(entity, [])\n        c = yope3d.reg_add(entity, \"LightSource\")\n        c.constant = 0.5\n        c.color = yope3d.Vec3(1.08, 1.82, 0.37)\n        c.direction = yope3d.Vec3(0.42, 0.94, 0.76)\n        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.46, 0.73, 0.19)\n        c = yope3d.reg_add(entity, \"OBBForm\")\n        c.extent = yope3d.Vec3(0.92, 0.1, 0.58)\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "\n",
   "target": "BUS_MUSIC",
   "rest": ", 0.93)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0070.py:76:8:clear_collision_observers",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0070.py",
   "line": 76,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth070Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.85},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_get(entity, \"Material\")\n        if hull is not None:\n            hull.metallic = 0.54\n        world.add_spring_with_proxies(entity, entity, 1.85, 0.84, 4, 0.53)\n        for ent, uiback, uitext in yope3d.view(\"UIBackground\", \"UITexturedBackground\"):\n            uiback.a = 4.19\n        cmp = yope3d.reg_add(entity, \"Hull\")\n        cmp.omega = yope3d.Vec3(2.58, 2.88, 3.47)\n        cmp.linear_damping = 0.13\n        form = yope3d.reg_get(entity, \"Name\")\n        if form is not None:\n            form.value = \"ground\"\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 1.4\n        hull.half_height = 1\n        for ent, pointj in yope3d.view(\"PointJointConstraint\"):\n            pointj.local_anchor_a = yope3d.Vec3(2.31, 2.02, 0.47)\n        for ent, audios in yope3d.view(\"AudioSource\"):\n            audios.pitch = 0.87\n\n    def on_collision_enter(self, world, entity, other, contact):\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.size_meters = 0.97\n        world.attach_sphere_collider(entity, 86.71, 1.36)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(3.35, 0, 1.3) - origin).normalize())\n        c = yope3d.reg_get(entity, \"OBBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.18, 0.85, 0.32)\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_SLASH):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.5)\n        cmp = yope3d.reg_add(entity, \"LightSource\")\n        cmp.position = yope3d.Vec3(0.34, 0.71, 0.79)\n        cmp.direction = yope3d.Vec3(0.11, 0.31, 0.23)\n        cmp.type = 0.96\n        cmp.constant = 0.65\n        hull = yope3d.reg_add(entity, \"LightSource\")\n        hull.color = yope3d.Vec3(4.01, 3.61, 3.49)\n        hull.intensity = 1.01\n        hull.constant = 0.69\n        hull.position = yope3d.Vec3(0.28, 0.47, 0.77)\n        hull.direction = yope3d.Vec3(0.68, 0.37, 0.81)\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.loaded = False\n        t.asset_path = \"models/prop.glb\"\n        target = world.add_point_light(yope3d.Vec3(1.74, 3.83, 4.64), yope3d.Vec3(0.53, 0.06, 3.12), 5.8)\n\n    def on_ui_press(self, world, entity):\n        for ent, lights in yope3d.view(\"LightSource\"):\n            lights.outer_cone_angle = 0.14\n        yope3d.window.set_f11_fullscreen(True)\n        world.apply_impulse(entity, yope3d.Vec3(12.38, 5.77, 1.52))\n        hull = yope3d.reg_add(entity, \"UITexturedBackground\")\n        hull.tint_b = 0.76\n        hull.tint_r = 0.87\n        body = world.add_aabb(yope3d.Vec3(0.25, 0.69, 0.61), 64.12)\n        world.set_shadow_caster(entity)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"player\"\n        yope3d.",
   "suffix": "\n",
   "target": "clear_collision_observers",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0075.py:65:36:KEY_F11",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0075.py",
   "line": 65,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth075Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.14},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"TextLabel3D\")\n        m.font = \"fonts/inter_ascii.msdf\"\n        m.a = 1.68\n        m.r = 4.59\n        m.text = \"Score: 0\"\n        v = yope3d.get_position(entity)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_g = 3.23\n        c.normal_r = 1.27\n        c.normal_b = 1.63\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.billboard = 3\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.text = \"Press E to interact\"\n        hull.font = \"fonts/inter_ascii.msdf\"\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.61)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.direction = yope3d.Vec3(0.09, 0.27, 0.51)\n        t.type = 0.26\n        c = yope3d.reg_get(entity, \"AABBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.52, 0.14, 0.33)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.74)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 2.94, yope3d.EASE_CUBIC_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.play_sound(\"models/prop.glb\")\n        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.36\n        cmp.has_texture = False\n        cmp.path = \"audios/impact.ogg\"\n        cmp.tint_b = 0.08\n        cmp.tint_g = 0.03\n        v = yope3d.audio.load_sound(\"textures/tnail.png\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.37, 0, 4.17) - origin).normalize())\n\n    def init(self, world, entity, params):\n        yope3d.settings.clear()\n        t = yope3d.reg_add(entity, \"AnimationPlayer\")\n        t.time = 0.55\n        t.speed = 5.64\n        t.playing = 1\n        t.loop = 0\n        t.clip = \"player\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.54)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.96, 0.43, yope3d.EASE_CUBIC_IN)\n        if yope3d.input.is_key_down(yope3d.KEY_N):\n            world.apply_impulse(entity, yope3d.Vec3(0, 22.17, 0))\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 19.91, 0))\n        world.set_mesh_visible(entity, False)\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.velocity = yope3d.Vec3(0.18, 0.03, 0.22)\n        c.omega = yope3d.Vec3(4.63, 2.13, 3.87)\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 0.44\n\n",
   "target": "KEY_F11",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:24:4:set_text",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 24,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.",
   "suffix": "    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "set_text",
   "rest": "(self.hud, f\"Score: {self.score}\")",
   "freq": 12,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0015.py:77:34:BUS_SFX",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0015.py",
   "line": 77,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth015Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.71},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.text = \"Paused\"\n        hull.a = 3.35\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.63)\n        m = yope3d.reg_add(entity, \"Material\")\n        m.normal_map = \"textures/metal_normal.png\"\n        m.albedo_map = \"textures/crate_albedo.png\"\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(0.85, 2.55, 2.64), yope3d.Vec3(1.52, 3.61, 2.22))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.97)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.44, 1.91, yope3d.EASE_QUAD_OUT)\n        hull = yope3d.reg_add(entity, \"UIBackground\")\n        hull.b = 1.19\n        hull.a = 3.28\n        hull.g = 2.3\n        hull.r = 1.47\n\n    def update(self, world, entity, dt):\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(1.76, 0.29, 4.48), yope3d.Vec3(4.69, 3.42, 4.59), False, 0.1, 0.85, False)\n        c = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if c is not None:\n            c.swing_limit = 0.37\n        v = yope3d.clamp(2.74, 1.22, 2.93)\n        c = yope3d.reg_get(entity, \"ScriptComponent\")\n        if c is not None:\n            c.script_class = \"player\"\n        c = yope3d.reg_get(entity, \"AudioSource\")\n        if c is not None:\n            c.bus = 1\n        for ent, name, lights in yope3d.view(\"Name\", \"LightSource\"):\n            name.value = \"ground\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_get(entity, \"CapsuleForm\")\n        if c is not None:\n            c.radius = 1.35\n        v = yope3d.play_sound(\"textures/tnail.png\")\n        for ent, uibutt in yope3d.view(\"UIButton\"):\n            uibutt.pressed_a = 3.06\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.37)\n        world.attach_cylinder_mesh(other, 0.74, 0.54)\n        anchor = world.add_audio_source_entity()\n        wheel = world.add_kinematic_box(yope3d.Vec3(4.06, 2.99, 4.67), yope3d.Vec3(0.58, 0.25, 0.03))\n        rot = yope3d.Quat.from_euler(0.69, 0.47)\n\n    def init(self, world, entity, params):\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.65\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.font = \"fonts/inter_ascii.msdf\"\n        t.b = 3.69\n        t.text = \"Score: 0\"\n        t.g = 0.07\n        t.a = 2.81\n        v = yope3d.get_position(entity)\n        c = yope3d.reg_add(entity, \"UITexturedBackground\")\n        c.tint_a = 0.41\n        c.tint_r = 0.4\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.42)\n        form = yope3d.reg_get(entity, \"ScriptComponent\")\n        if form is not None:\n            form.script_class = \"crate\"\n        yope3d.draw_line(yope3d.Vec3(1.33, 2.5, 1.71), yope3d.Vec3(3.28, 3.2, 3.33))\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "\n",
   "target": "BUS_SFX",
   "rest": ", 0.84)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0033.py:29:8:draw_ray",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0033.py",
   "line": 29,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth033Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.2},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        hull = yope3d.reg_add(entity, \"UITransform\")\n        hull.max_y = 0.63\n        hull.visible = True\n        hull.depth = 0\n        hull.min_y = 1.17\n        world.remove_spring_between(entity, entity)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.52, 0, 0.83)).length()\n        if yope3d.input.is_key_pressed(yope3d.KEY_UP):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1.39\n        world.set_wheel_drive(entity, 1.96)\n        h = world.get_shadow_caster()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_skybox([])\n        yope3d.",
   "suffix": "        hull = yope3d.reg_get(entity, \"Parent\")\n        if hull is not None:\n            hull.parent = entity\n        for ent, name, transf in yope3d.view(\"Name\", \"Transform\"):\n            name.value = \"hud_root\"\n\n    def update(self, world, entity, dt):\n        for ent, uitran in yope3d.view(\"UITransform\"):\n            uitran.max_y = 3.73\n        c = yope3d.reg_get(entity, \"OBBForm\")\n",
   "target": "draw_ray",
   "rest": "(yope3d.Vec3(4.27, 1.51, 2.48), yope3d.Vec3(1.66, 3.2, 2.93))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:23:44:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 23,
   "col": 44,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.",
   "suffix": "    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "Vec3",
   "rest": "(0, 5, 0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0079.py:61:36:KEY_6",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0079.py",
   "line": 61,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth079Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.83},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        c = yope3d.reg_add(entity, \"Material\")\n        c.albedo_map = \"textures/metal_basecolor.png\"\n        c.metal_rough_map = \"textures/crate_mr.png\"\n        c.emissive_map = \"textures/lamp_emissive.png\"\n        c.occlusion_map = \"textures/crate_ao.png\"\n        c.albedo = (0.02, 2.44, 3.27, 0.99)\n        yope3d.settings.remove(\"ground\")\n        world.attach_aabb_collider(entity, 70.19, yope3d.Vec3(0.7, 0.11, 0.19), True)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.45, 1.24, yope3d.EASE_QUAD_OUT)\n        cmp = yope3d.reg_add(entity, \"UIText\")\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        cmp.a = 1.31\n        world.wake(entity)\n        if yope3d.input.is_key_pressed(yope3d.KEY_W):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.82, 0, 1.91) - origin).normalize())\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.fix_entity(other)\n        r = world.get_island_count()\n        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 2.59\n        t.text = \"Score: 0\"\n        t.g = 2.73\n        t.a = 0.14\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.34, 0, 3.35) - origin).normalize())\n        v = yope3d.reg_valid(other)\n        c = yope3d.reg_add(entity, \"AudioSource\")\n        c.loop = True\n        c.bus = 4\n        if yope3d.input.is_key_pressed(yope3d.KEY_F9):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_add(entity, \"SpringConstraint\")\n        c.target = entity\n        c.rest_length = 0.81\n        c.k = 3.03\n\n    def update(self, world, entity, dt):\n        yope3d.reg_remove(entity, \"CapsuleForm\")\n        world.set_mesh_color(entity, 4, 2.2, 1.32)\n        yope3d.clear_collision_observers()\n        c = yope3d.reg_add(entity, \"Hull\")\n        c.mass = 82.59\n        c.angular_damping = 0.16\n        c.velocity = yope3d.Vec3(0.38, 0.87, 0.03)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 8.13, 0))\n\n    def init(self, world, entity, params):\n        yope3d.camera.look_at(yope3d.Vec3(0.93, 1, 0.84))\n        r = world.attach_animation(entity, \"audios/impact.ogg\")\n        c = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(1.36, 4.98, 1.93)\n        c.local_anchor_b = yope3d.Vec3(2.29, 0.48, 2.44)\n        yope3d.settings.remove(\"ground\")\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(1.79, 1.21, 0.99), yope3d.Vec3(3.81, 3.59, 4.61), True, -0.24, 0.61, True)\n",
   "target": "KEY_6",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0048.py:47:12:get_world_position",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0048.py",
   "line": 47,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth048Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.55},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.load_game(\"player\")\n        cmp = yope3d.reg_add(entity, \"Parent\")\n        cmp.parent = entity\n        world.set_wheel_steer(entity, -0.33)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"hud_root\"\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.local_anchor_a = yope3d.Vec3(4.15, 1.25, 3.25)\n        prop = world.add_capsule(0.4, 0.29, 82.35, yope3d.Vec3(0.64, 3.8, 4.69))\n        world.remove_joint_between(prop, entity)\n\n    def init(self, world, entity, params):\n        form = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if form is not None:\n            form.swing_limit = 0.62\n        if yope3d.input.is_key_down(yope3d.KEY_P):\n            world.apply_impulse(entity, yope3d.Vec3(0, 15.66, 0))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.72)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.94, 2.58, yope3d.EASE_CUBIC_IN)\n        world.add_point_joint(entity, entity, yope3d.Vec3(4.23, 0.12, 2.8))\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_r = 1.9\n        c.hover_g = 4.97\n        c.normal_b = 2.11\n        c.normal_a = 3.61\n        v = yope3d.clamp(4.05, 1.88, 2.88)\n        c = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(1.85, 4.9, 3.22)\n        c.local_anchor_b = yope3d.Vec3(3.8, 1.72, 4.1)\n        c.target = entity\n        v = yope3d.",
   "suffix": "        v = yope3d.settings.get_bool(\"crate\")\n        body = world.add_ui_text(\"Paused\", \"Score: 0\", yope3d.Vec2(3.89, 4.23), yope3d.Vec2(0.53, 4.09), 0)\n        v = yope3d.play_music(\"audios/impact.ogg\")\n\n    def on_ui_press(self, world, entity):\n        for ent, compou, hingej in yope3d.view(\"CompoundCollider\", \"HingeJointConstraint\"):\n            compou.asset_path = \"audios/impact.ogg\"\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_b = yope3d.Vec3(0.71, 4.21, 0.51)\n        cmp.local_anchor_a = yope3d.Vec3(3.2, 4.32, 1.51)\n",
   "target": "get_world_position",
   "rest": "(entity)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0050.py:53:39:KEY_M",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0050.py",
   "line": 53,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth050Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.finalize_entity(entity, \"player\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.99)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.43, 0.71, yope3d.EASE_QUAD_IN_OUT)\n        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.57, 0.33, 0.37)\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        r = yope3d.to_radians(7.0)\n        t = yope3d.reg_add(entity, \"TextLabel3D\")\n        t.g = 0.93\n        t.text = \"Paused\"\n        t.r = 0.47\n        t.a = 2.36\n        t.font = \"fonts/inter_ascii.msdf\"\n        form = yope3d.reg_get(entity, \"Parent\")\n        if form is not None:\n            form.parent = entity\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.23)\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.visible = False\n        m.min_y = 2.81\n        m.min_x = 2.55\n        hull = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_b = yope3d.Vec3(4.84, 4.55, 2.29)\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(4.19, 3.11, 0.75), yope3d.Vec3(0.52, 0.4, 2.95))\n        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.path = \"models/prop.glb\"\n        t.pitch = 1.06\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.21)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.54, 1.57, yope3d.EASE_QUAD_IN)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.98)\n        world.remove_entity(entity)\n        if yope3d.input.is_key_down(yope3d.KEY_W):\n            world.apply_impulse(entity, yope3d.Vec3(0, 3.52, 0))\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.58)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.91, 0.57, yope3d.EASE_CUBIC_OUT)\n        m = yope3d.reg_add(entity, \"Name\")\n        m.value = \"hud_root\"\n        if yope3d.input.is_key_down(yope3d.KEY_C):\n            world.apply_impulse(entity, yope3d.Vec3(0, 17.28, 0))\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.41)\n        for ent, cylind, parent in yope3d.view(\"CylinderForm\", \"Parent\"):\n",
   "target": "KEY_M",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0004.py:42:51:EASE_QUAD_IN",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0004.py",
   "line": 42,
   "col": 51,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth004Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.77},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(98.0)\n        v = yope3d.attach_script(entity, \"hud_root\", \"player\")\n        form = yope3d.reg_get(entity, \"Name\")\n        if form is not None:\n            form.value = \"player\"\n        n = yope3d.Vec3(2.12, 0, 3.27).normalize()\n        h = world.ui_consumed_click()\n\n    def update(self, world, entity, dt):\n        rot = yope3d.Quat.from_euler(1.25, -0.43)\n        m = yope3d.reg_get(entity, \"SpringConstraint\")\n        if m is not None:\n            m.k = 1.26\n        c = yope3d.reg_add(entity, \"Name\")\n        c.value = \"player\"\n        hull = yope3d.reg_get(entity, \"TextLabel3D\")\n        if hull is not None:\n            hull.billboard = 0\n\n    def init(self, world, entity, params):\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.params_blob = \"crate\"\n        world.attach_sphere_collider(entity, 23.6, 1.85, True)\n        rot = yope3d.Quat.from_euler(-0.37, 0.91)\n        body = world.add_text_label_3d(\"Press E to interact\", \"Press E to interact\", yope3d.Vec3(2.14, 1.59, 0.79))\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.params_blob = \"crate\"\n        c.script_class = \"hud_root\"\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.23)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.7, 1.45, yope3d.",
   "suffix": "\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.to_radians(2.05)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.23, 0, 1.35) - origin).normalize())\n        yope3d.window.set_f11_fullscreen(False)\n        v = yope3d.capsule_overlap(yope3d.Vec3(3.68, 4.06, 0.89), 0.34, 1.03)\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1.43\n\n",
   "target": "EASE_QUAD_IN",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:99:19:WheelSpec",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 99,
   "col": 19,
   "prefix": "\"\"\"\nRaycast-wheel vehicle demo (see physics/Joint.h's SuspensionJoint/\nWheelFrictionJoint). The chassis is one OBB rigid body; each of its 4 wheels\nis a raycast \u2014 no separate wheel rigid bodies, no hinge/motor joints \u2014 with a\nspring/damper suspension plus lateral (grip) and longitudinal (drive/brake)\nfriction rows solved in the same island-parallel PGS pass as everything else.\n\nControls: W/S throttle+reverse, A/D steer the front wheels. No mouse-look\n(unlike mouse_drag_ragdoll.py) \u2014 the camera is a fixed-offset chase cam.\n\nDRIVETRAIN \u2014 front-wheel drive. The two front wheels both steer AND drive; the\ntwo rear wheels free-roll (lateral grip only). This is the realistic model the\nuser asked for: steering rotates the front wheels, and because those same\nwheels carry the drive force, the propulsion vector turns with the steering.\nIt relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.",
   "suffix": "            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n",
   "target": "WheelSpec",
   "rest": "()",
   "freq": 1,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:98:19:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 98,
   "col": 19,
   "prefix": "\"\"\"Sandbox gallery \u2014 the 14 demo scenes from the pre-M13 C++ SandboxScript.\n\nPorted 1:1 from src/scripting/SandboxScript.cpp (dropped from the build in M13);\nevery constant below matches the original, so the pyramids stack and the cloths\nsag exactly the way they used to.\n\n    0-2    Pyramid (Small / Medium / Large)  \u2014 4, 7, 10-row OBB pyramids\n    3      Mass-Ratio Stack \u2014 a 200x-heavier box on light ones (warm-start demo)\n    4-12   Spring cloth, 3 anchor variants x 3 node shapes (sphere/AABB/OBB)\n    13     Stress test \u2014 walled arena + a bulk body grid\n    14     Doppler test \u2014 a looping audio source falling past the listener\n\nAttach to any entity in a scene file (assets/scenes/sandboxGallery.yscene does it)\nand press Play.\n\nControls: LEFT/RIGHT switch scene, UP/DOWN pick spawn shape, LMB fire it,\nWASD+mouse fly, P collider overlay, H hide HUD, V cloth self-collision.\n\nThe cloth scenes default to self-collision OFF (a collision-layer filter \u2014 see\napply_cloth_filter): 400 nodes whose AABBs all overlap their neighbours' is what\nmade these scenes crawl. V toggles it live, with the cost visible in the HUD's\nbody/island counters.\n\nparamsBlob keys (all optional): start_scene, fly_speed, sprint_mult, mouse_sens,\nfont, stress_n (bodies in the stress scene), stress_shape (sphere/aabb/obb),\ncloth_self_collision (default false).\n\"\"\"\nimport yope3d\n\nfrom behaviors._gallery import GalleryBase\n\n# ---- Constants (verbatim from SandboxScript.cpp) ----\n\nPYR_HALF = 0.45\nPYR_SPACING = 1.0\n\nGRID_N = 20\nGRID_STEP = 1.0\nNODE_HALF = 0.45\nNODE_MASS = 1.0\nSPRING_K = 300.0\n\nSTRESS_HALF = 20.0\nSTRESS_CEILING = 25.0\n\n# A PGS solver's iteration budget converges a uniform-mass stack in well under\n# PGS_VELOCITY_ITERATIONS regardless of warm-starting \u2014 that's why the pyramid\n# scenes don't show the toggle doing anything. A large mass ratio is the\n# textbook counterexample: the heavy box's weight has to propagate down through\n# every light-box contact below it, and each contact's effective mass (dominated\n# by the LIGHTER of the pair) badly underestimates how much impulse the joint\n# actually needs. Warm-starting carries last step's converged impulse forward so\n# the light boxes are already \"primed\" to resist that weight; without it, every\n# step restarts from zero and visibly sinks/jitters under the heavy box.\nMR_HALF = 0.5\nMR_LEVELS = 6          # 5 light boxes + 1 heavy one on top\nMR_LIGHT_MASS = 1.0\nMR_HEAVY_MASS = 200.0\n\nFLOOR_COLOR = (0.32, 0.28, 0.24)\nWALL_COLOR = (0.22, 0.22, 0.28)\n\n\nALL_LAYERS = 0xFFFFFFFF\n\n\nclass SandboxGallery(GalleryBase):\n    PARAMS = dict(GalleryBase.PARAMS)\n    PARAMS.update({\n        \"stress_n\": {\"type\": \"int\", \"default\": 400, \"label\": \"Stress scene body count\"},\n        \"stress_shape\": {\"type\": \"str\", \"default\": \"sphere\",\n                         \"label\": \"Stress scene shape (sphere/aabb/obb)\"},\n        \"cloth_self_collision\": {\"type\": \"bool\", \"default\": False,\n                                 \"label\": \"Cloth nodes collide with each other\"},\n    })\n\n    # ---- Cloth self-collision filter -------------------------------------\n    # A 20x20 cloth is 400 bodies whose AABBs all overlap their neighbours', so\n    # node-vs-node contacts dominate narrowphase and fuse the whole sheet into one\n    # giant island \u2014 that is where the framerate went. The springs alone hold the\n    # sheet together, so self-collision is a *visual* upgrade (the cloth can no\n    # longer pass through itself), not a structural requirement. Default OFF; V\n    # toggles it live so the cost is legible on the HUD.\n    #\n    # Contacts need (A.layer & B.mask) && (B.layer & A.mask). Nodes sit on their own\n    # layer and mask it out of themselves, so they still collide with the floor and\n    # with anything you fire at them (both of which keep layer/mask = ALL).\n\n    def cloth_bit(self, world):\n        if not world.layers.has(\"cloth\"):\n            return world.layers.add(\"cloth\")\n        return world.layers[\"cloth\"]\n\n    def apply_cloth_filter(self, world):\n        bit = self.cloth_bit(world)\n        mask = ALL_LAYERS if self.self_collide else (ALL_LAYERS & ~bit)\n        for e in self.cloth_nodes:\n            hull = yope3d.",
   "suffix": "            if hull is None:\n                continue\n            hull.collision_layer = bit\n            hull.collision_mask = mask\n            world.wake(e)   # a sleeping island would ignore the new filter\n\n    # ---- Pyramids --------------------------------------------------------\n\n    def build_pyramid(self, world, base_n):\n        half_floor = (base_n + 15) * 1.0\n",
   "target": "reg_get",
   "rest": "(e, \"Hull\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0101.py:33:12:clamp",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0101.py",
   "line": 33,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth101Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.23},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.39, 0.04, 0.39)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_a = 0.4\n        c.normal_r = 3.38\n        c.hover_g = 0.11\n        c.hover_r = 0.74\n        c.normal_b = 4.16\n        v = yope3d.reg_add(entity, 0.07)\n        c = yope3d.reg_get(entity, \"Hull\")\n        if c is not None:\n            c.gravity = False\n        world.apply_impulse(entity, yope3d.Vec3(33.96, 31.06, 32.55))\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.asset_path = \"audios/impact.ogg\"\n        m.loaded = False\n\n    def update(self, world, entity, dt):\n        v = yope3d.camera.screen_to_ray(2.94, 3.83)\n        world.attach_obb_collider(entity, 71.39, yope3d.Vec3(0.73, 0.9, 0.47))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.23)\n        t = yope3d.",
   "suffix": "        v = yope3d.lerp(3.29, 2.5, t)\n        body = world.add_obb(yope3d.Vec3(0.03, 0.98, 0.52), 48.15)\n\n    def on_ui_press(self, world, entity):\n        world.attach_obb_collider(entity, 84.17, yope3d.Vec3(0.37, 0.03, 0.39), True)\n        r = yope3d.to_radians(116.0)\n        yope3d.set_position(entity, yope3d.Vec3(3.81, 3.95, 4.06))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.97)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.29, 2.47, yope3d.EASE_CUBIC_OUT)\n",
   "target": "clamp",
   "rest": "(dt * 4.59, 0.0, 1.0)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0050.py:60:36:KEY_C",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0050.py",
   "line": 60,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth050Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.finalize_entity(entity, \"player\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.99)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.43, 0.71, yope3d.EASE_QUAD_IN_OUT)\n        c = yope3d.reg_add(entity, \"AABBForm\")\n        c.extent = yope3d.Vec3(0.57, 0.33, 0.37)\n        t = yope3d.reg_add(entity, \"Parent\")\n        t.parent = entity\n        r = yope3d.to_radians(7.0)\n        t = yope3d.reg_add(entity, \"TextLabel3D\")\n        t.g = 0.93\n        t.text = \"Paused\"\n        t.r = 0.47\n        t.a = 2.36\n        t.font = \"fonts/inter_ascii.msdf\"\n        form = yope3d.reg_get(entity, \"Parent\")\n        if form is not None:\n            form.parent = entity\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.23)\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"UITransform\")\n        m.visible = False\n        m.min_y = 2.81\n        m.min_x = 2.55\n        hull = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if hull is not None:\n            hull.local_anchor_b = yope3d.Vec3(4.84, 4.55, 2.29)\n        world.add_hinge_joint(entity, entity, yope3d.Vec3(4.19, 3.11, 0.75), yope3d.Vec3(0.52, 0.4, 2.95))\n        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.path = \"models/prop.glb\"\n        t.pitch = 1.06\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.21)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.54, 1.57, yope3d.EASE_QUAD_IN)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.98)\n        world.remove_entity(entity)\n        if yope3d.input.is_key_down(yope3d.KEY_W):\n            world.apply_impulse(entity, yope3d.Vec3(0, 3.52, 0))\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_pressed(yope3d.KEY_M):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.58)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.91, 0.57, yope3d.EASE_CUBIC_OUT)\n        m = yope3d.reg_add(entity, \"Name\")\n        m.value = \"hud_root\"\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 17.28, 0))\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.41)\n        for ent, cylind, parent in yope3d.view(\"CylinderForm\", \"Parent\"):\n            cylind.radius = 1.57\n        world.set_mesh_visible(entity, True)\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"OBBForm\")\n        hull.extent = yope3d.Vec3(0.34, 0.16, 0.31)\n        hull = yope3d.reg_get(entity, \"Material\")\n",
   "target": "KEY_C",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:7:17:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 7,
   "col": 17,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.",
   "suffix": "    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n",
   "target": "Vec3",
   "rest": "(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0006.py:17:39:KEY_C",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0006.py",
   "line": 17,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth006Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.24},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if m is not None:\n            m.clip = \"crate\"\n        for ent, uibutt, transf in yope3d.view(\"UIButton\", \"Transform\"):\n            uibutt.normal_g = 0.97\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        world.fix_entity(entity)\n        v = yope3d.input.is_mmb_down()\n        c = yope3d.reg_get(entity, \"SphereForm\")\n        if c is not None:\n            c.radius = 0.9\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_add(entity, \"OBBForm\")\n        m.extent = yope3d.Vec3(0.48, 0.79, 0.67)\n",
   "target": "KEY_C",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:20:51:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 20,
   "col": 51,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.",
   "suffix": "    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n",
   "target": "Vec3",
   "rest": "(1, 1, 0.3))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/sandbox_gallery.py:288:13:reg_get",
   "stratum": "real",
   "origin": "scripts/behaviors/sandbox_gallery.py",
   "line": 288,
   "col": 13,
   "prefix": "                if shape == 0:\n                    color = (0.9 - 0.4 * fi, 0.3 + 0.4 * fj, 0.15 + 0.3 * fi)\n                    e = self.sphere(world, NODE_MASS, NODE_HALF, pos, color)\n                elif shape == 1:\n                    # The Java-era cloth gradient (SpringDemo.java, 30x30 grid):\n                    # setColor(i/30, j/30, 0.4 + (i+j)/120) \u2014 i.e. i/N, j/N,\n                    # 0.4 + (i+j)/(4N), generalized to GRID_N.\n                    color = (i / GRID_N, j / GRID_N,\n                             0.4 + (i + j) / (4.0 * GRID_N))\n                    e = self.box(world, half, NODE_MASS, pos, color)\n                else:\n                    color = (fi, fj, 0.4 + 0.3 * (fi + fj) * 0.5)\n                    e = self.obb(world, half, NODE_MASS, pos, color)\n\n                grid[i][j] = e\n                self.cloth_nodes.append(e)\n\n                if variant == 0:\n                    fix = j == 0\n                elif variant == 1:\n                    fix = i in (0, GRID_N - 1) and j in (0, GRID_N - 1)\n                else:\n                    fix = j == 0 and i in (0, GRID_N - 1)\n                if fix:\n                    world.fix_entity(e)\n\n        for j in range(GRID_N):\n            for i in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i + 1][j], SPRING_K, GRID_STEP)\n        for i in range(GRID_N):\n            for j in range(GRID_N - 1):\n                self.spring(world, grid[i][j], grid[i][j + 1], SPRING_K, GRID_STEP)\n\n        self.apply_cloth_filter(world)\n\n        dist = (GRID_N - 1) * GRID_STEP\n        if horizontal:\n            self.set_camera((0.0, top_y + dist, dist * 0.7), pitch=-0.8)\n        else:\n            self.set_camera((0.0, top_y * 0.5, dist + 5.0))\n\n    # ---- Stress ----------------------------------------------------------\n\n    def build_stress(self, world):\n        self.static_box(world, (0.0, -0.5, 0.0), (STRESS_HALF, 0.5, STRESS_HALF),\n                        FLOOR_COLOR)\n\n        wh = STRESS_CEILING * 0.5\n        for pos, ext in (\n            ((-STRESS_HALF - 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((STRESS_HALF + 0.4, wh, 0.0), (0.4, wh, STRESS_HALF)),\n            ((0.0, wh, -STRESS_HALF - 0.4), (STRESS_HALF, wh, 0.4)),\n            ((0.0, wh, STRESS_HALF + 0.4), (STRESS_HALF, wh, 0.4)),\n        ):\n            self.static_box(world, pos, ext, WALL_COLOR)\n\n        n = int(self.params.get(\"stress_n\", 400))\n        shape = self.params.get(\"stress_shape\", \"sphere\")\n\n        # 36x36 columns, stacked upward \u2014 same packing the Phase E sweep used, so\n        # the stack stays under STRESS_CEILING well past 40k bodies.\n        kx, kz, spacing, half = 36, 36, 1.05, 0.5\n        x0 = -((kx - 1) * 0.5) * spacing\n        z0 = -((kz - 1) * 0.5) * spacing\n        color = (0.7, 0.4, 0.3)\n\n        for i in range(max(n, 0)):\n            ix = i % kx\n            iz = (i // kx) % kz\n            iy = i // (kx * kz)\n            pos = (x0 + ix * spacing, 1.0 + iy * spacing, z0 + iz * spacing)\n            if shape == \"aabb\":\n                self.box(world, (half, half, half), 1.0, pos, color)\n            elif shape == \"obb\":\n                self.obb(world, (half, half, half), 1.0, pos, color)\n            else:\n                self.sphere(world, 1.0, half, pos, color)\n\n        self.set_camera((0.0, 3.5, STRESS_HALF - 2.0))\n\n    # ---- Doppler ---------------------------------------------------------\n\n    def build_doppler(self, world):\n        # Floor sits 100 m down so the ball keeps falling past the listener long\n        # enough to hear the pitch sweep.\n        self.static_box(world, (0.0, -100.5, 0.0), (50.0, 0.5, 50.0), FLOOR_COLOR)\n\n        self.doppler_ent = self.sphere(world, 1.0, 0.5, (0.0, 30.0, 0.0),\n                                       (1.0, 0.35, 0.1), vel=(0.0, -40.0, 0.0))\n\n        buf = yope3d.audio.load_sound(\"audios/test.ogg\")\n        if buf is not None:\n            self.doppler_src = yope3d.audio.create_source(buf)\n            self.doppler_src.enable_looping(True)\n            self.doppler_src.play()\n\n        self.set_camera((0.0, 2.0, 8.0))\n\n    # ---- shell hooks -----------------------------------------------------\n\n    def init(self, world, entity, params):\n        self.self_collide = bool(params.get(\"cloth_self_collision\", False))\n        self.cloth_nodes = []\n        self.prev_v = False\n        GalleryBase.init(self, world, entity, params)\n\n    def on_update(self, world, dt):\n        v = yope3d.input.is_key_down(yope3d.KEY_V)\n        if v and not self.prev_v and self.cloth_nodes:\n            self.self_collide = not self.self_collide\n            self.apply_cloth_filter(world)\n            self.refresh_stats(world)\n        self.prev_v = v\n\n        # The audio source is not an entity, so it needs a manual per-frame sync of\n        # position + velocity (velocity is what OpenAL derives the Doppler shift from).\n        src = getattr(self, \"doppler_src\", None)\n        ent = getattr(self, \"doppler_ent\", None)\n        if src is None or ent is None:\n            return\n        tf = yope3d.",
   "suffix": "        hull = yope3d.reg_get(ent, \"Hull\")\n        if tf is not None and hull is not None:\n            src.set_position(tf.position)\n            src.set_velocity(hull.velocity)\n\n    def controls_extra(self):\n        return \"   V self-collide\" if self.cloth_nodes else \"\"\n\n    def stats_extra(self, world):\n        if not self.cloth_nodes:\n",
   "target": "reg_get",
   "rest": "(ent, \"Transform\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0095.py:45:39:KEY_F11",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0095.py",
   "line": 45,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth095Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.9},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        res = world.get_contact_count()\n        for ent, uiback, aabbfo in yope3d.view(\"UIBackground\", \"AABBForm\"):\n            uiback.b = 0.25\n        target = world.add_trigger_sphere(yope3d.Vec3(4.79, 2.64, 3.07), 0.79)\n        m = yope3d.reg_get(entity, \"CapsuleForm\")\n        if m is not None:\n            m.radius = 0.85\n        yope3d.audio.resume_all()\n        v = yope3d.settings.get_bool(\"ground\")\n        v = yope3d.input.get_mouse_delta()\n\n    def init(self, world, entity, params):\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 2\n        if yope3d.input.is_key_pressed(yope3d.KEY_P):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        anchor = world.add_ui_background(yope3d.Vec2(0.39, 0.23), yope3d.Vec2(1.63, 4.9), yope3d.Vec4(3.38, 0.72, 0.91, 1.74))\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.93)\n        world.attach_box_mesh(entity, yope3d.Vec3(1.92, 4.93, 3.51), 4.45, 1.79, 2.55)\n        res = world.ui_hovered()\n        c = yope3d.reg_get(entity, \"UITransform\")\n        if c is not None:\n            c.size_mode = 3\n\n    def on_ui_press(self, world, entity):\n        wheel = world.add_kinematic_capsule(0.93, 0.68, yope3d.Vec3(2.97, 1.68, 1.22))\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 0.67\n        hull.half_height = 0.54\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.asset_path = \"models/prop.glb\"\n        m.loaded = True\n        h = world.get_contact_count()\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def update(self, world, entity, dt):\n        world.set_mesh_color(entity, 4.09, 3.61, 2.7)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.66)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.31, 2.81, yope3d.EASE_QUAD_IN)\n        v = yope3d.reg_add(entity, 0.87)\n        cmp = yope3d.reg_add(entity, \"SpringConstraint\")\n        cmp.k = 3.41\n",
   "target": "KEY_F11",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0049.py:15:52:EASE_QUAD_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0049.py",
   "line": 15,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth049Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.73},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.audio.fade_gain(entity, 0.82, 1.59)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.82)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.26, 0.64, yope3d.",
   "suffix": "        yope3d.audio.set_bus_gain(1, 0.47)\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.min_x = 3.1\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.77, 0, 4.51) - origin).normalize())\n        if yope3d.input.is_key_down(yope3d.KEY_F2):\n            world.apply_impulse(entity, yope3d.Vec3(0, 4.76, 0))\n\n    def on_ui_press(self, world, entity):\n",
   "target": "EASE_QUAD_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0048.py:59:34:BUS_VOICE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0048.py",
   "line": 59,
   "col": 34,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth048Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.55},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        yope3d.load_game(\"player\")\n        cmp = yope3d.reg_add(entity, \"Parent\")\n        cmp.parent = entity\n        world.set_wheel_steer(entity, -0.33)\n        c = yope3d.reg_add(entity, \"ScriptComponent\")\n        c.script_class = \"crate\"\n        c.params_blob = \"hud_root\"\n        m = yope3d.reg_get(entity, \"HingeJointConstraint\")\n        if m is not None:\n            m.local_anchor_a = yope3d.Vec3(4.15, 1.25, 3.25)\n        prop = world.add_capsule(0.4, 0.29, 82.35, yope3d.Vec3(0.64, 3.8, 4.69))\n        world.remove_joint_between(prop, entity)\n\n    def init(self, world, entity, params):\n        form = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if form is not None:\n            form.swing_limit = 0.62\n        if yope3d.input.is_key_down(yope3d.KEY_P):\n            world.apply_impulse(entity, yope3d.Vec3(0, 15.66, 0))\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.72)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.94, 2.58, yope3d.EASE_CUBIC_IN)\n        world.add_point_joint(entity, entity, yope3d.Vec3(4.23, 0.12, 2.8))\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_r = 1.9\n        c.hover_g = 4.97\n        c.normal_b = 2.11\n        c.normal_a = 3.61\n        v = yope3d.clamp(4.05, 1.88, 2.88)\n        c = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        c.local_anchor_a = yope3d.Vec3(1.85, 4.9, 3.22)\n        c.local_anchor_b = yope3d.Vec3(3.8, 1.72, 4.1)\n        c.target = entity\n        v = yope3d.get_world_position(entity)\n        v = yope3d.settings.get_bool(\"crate\")\n        body = world.add_ui_text(\"Paused\", \"Score: 0\", yope3d.Vec2(3.89, 4.23), yope3d.Vec2(0.53, 4.09), 0)\n        v = yope3d.play_music(\"audios/impact.ogg\")\n\n    def on_ui_press(self, world, entity):\n        for ent, compou, hingej in yope3d.view(\"CompoundCollider\", \"HingeJointConstraint\"):\n            compou.asset_path = \"audios/impact.ogg\"\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_b = yope3d.Vec3(0.71, 4.21, 0.51)\n        cmp.local_anchor_a = yope3d.Vec3(3.2, 4.32, 1.51)\n        cmp.target = entity\n        yope3d.audio.set_bus_gain(yope3d.",
   "suffix": "        hull = yope3d.reg_get(entity, \"Material\")\n        if hull is not None:\n            hull.albedo = (4.32, 3.83, 0.13, 3.88)\n        n = yope3d.Vec3(4.26, 0, 2.02).normalize()\n\n",
   "target": "BUS_VOICE",
   "rest": ", 0.23)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0012.py:25:36:KEY_RIGHT_ALT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0012.py",
   "line": 25,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth012Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.3},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(90.0)\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(4.57, 0, 1.87)).length()\n        world.attach_sphere_mesh(entity, 0.21)\n        for ent, obbfor in yope3d.view(\"OBBForm\"):\n            obbfor.extent = yope3d.Vec3(0.69, 0.32, 0.67)\n        v = yope3d.clamp(3.34, 0.14, 0.12)\n        m = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if m is not None:\n            m.tint_g = 0.7\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.11)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 2.17, 0))\n\n    def init(self, world, entity, params):\n        world.finalize_entity(entity, \"player\")\n        m = yope3d.reg_get(entity, \"AABBForm\")\n        if m is not None:\n            m.extent = yope3d.Vec3(0.47, 0.33, 0.76)\n        v = yope3d.to_radians(2.52)\n        m = yope3d.reg_add(entity, \"AABBForm\")\n        m.extent = yope3d.Vec3(0.72, 0.21, 0.73)\n",
   "target": "KEY_RIGHT_ALT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0001.py:50:36:KEY_DELETE",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0001.py",
   "line": 50,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth001Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        cmp = yope3d.reg_add(entity, \"Transform\")\n        cmp.scale = yope3d.Vec3(1.42, 1.83, 2)\n        cmp.position = yope3d.Vec3(0.63, 0.34, 0.79)\n        cmp.rotation = yope3d.Quat.from_euler(0.0, 0.15)\n        wheel = world.add_kinematic_box(yope3d.Vec3(2.68, 0.88, 4.84), yope3d.Vec3(0.09, 0.81, 0.76))\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        if yope3d.input.is_key_pressed(yope3d.KEY_F10):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.font = \"fonts/inter_ascii.msdf\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_anchor_a = yope3d.Vec3(1.14, 3.57, 3.38)\n        t.target = entity\n        t.local_anchor_b = yope3d.Vec3(2.93, 1.9, 1.71)\n        t.local_twist_axis_a = yope3d.Vec3(0.64, 0.45, 0.92)\n        h = world.get_registry()\n        h = world.get_island_count()\n        rot = yope3d.Quat.from_euler(0.68, -0.39)\n        if yope3d.input.is_key_down(yope3d.KEY_N):\n            world.apply_impulse(entity, yope3d.Vec3(0, 1.52, 0))\n        v = yope3d.window.is_cursor_locked()\n\n    def init(self, world, entity, params):\n        world.attach_cylinder_collider(entity, 17.79, 1.99, 0.81, False)\n        for ent, animat, textla in yope3d.view(\"AnimationPlayer\", \"TextLabel3D\"):\n            animat.speed = 3.03\n        n = yope3d.Vec3(0.37, 0, 1.74).normalize()\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.97, 0.95, 0.29)\n\n    def on_ui_press(self, world, entity):\n        for ent, compou, textla in yope3d.view(\"CompoundCollider\", \"TextLabel3D\"):\n            compou.loaded = True\n        v = yope3d.get_position(entity)\n        world.set_mesh_color(entity, 2.47, 1.94, 2.88)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 19.09, 0))\n        body = world.add_audio_source_entity()\n\n",
   "target": "KEY_DELETE",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0022.py:32:39:KEY_B",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0022.py",
   "line": 32,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth022Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 3.52},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        t = yope3d.reg_add(entity, \"Hull\")\n        t.velocity = yope3d.Vec3(0.62, 0.77, 0.61)\n        t.angular_damping = 0.07\n        t.mass = 6.69\n        t.omega = yope3d.Vec3(2.67, 3.62, 1.09)\n        form = yope3d.reg_get(entity, \"UIBackground\")\n        if form is not None:\n            form.r = 0.54\n        h = world.get_contact_count()\n        rot = yope3d.Quat.from_euler(1.07, -0.87)\n        m = yope3d.reg_add(entity, \"AnimationPlayer\")\n        m.loop = 0\n        m.speed = 4.45\n        m.clip = \"player\"\n        m.time = 0.81\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.pressed_g = 2.43\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        c = yope3d.reg_get(entity, \"Transform\")\n        if c is not None:\n            c.rotation = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.95)\n        v = yope3d.play_sound(\"models/prop.glb\")\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.target = entity\n        hull.rest_length = 0.08\n        hull.k = 2.69\n        world.set_ui_focus(entity)\n",
   "target": "KEY_B",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0108.py:27:39:KEY_E",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0108.py",
   "line": 27,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth108Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.95},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(0.42, 0.64, 2.62)\n        cmp.swing_limit = 0.88\n        cmp.local_twist_axis_b = yope3d.Vec3(0.6, 0.44, 0.25)\n        cmp.local_anchor_b = yope3d.Vec3(3.66, 1.59, 3.24)\n        world.remove_joint_between(entity, entity)\n        yope3d.set_velocity(entity, yope3d.Vec3(0.1, 0.71, 0.65))\n        v = yope3d.save_path(\"textures/tnail.png\")\n        m = yope3d.reg_get(entity, \"UITransform\")\n        if m is not None:\n            m.depth = 2\n        e = world.add_ui_curved_background(yope3d.Vec2(3.52, 4.54), yope3d.Vec2(2.69, 1.98), yope3d.Vec4(0.95, 0, 3.14, 2.53))\n\n    def init(self, world, entity, params):\n        world.make_kinematic(entity)\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"PointJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(3.35, 1.3, 1.06)\n        cmp.target = entity\n        cmp.local_anchor_b = yope3d.Vec3(4.47, 1.27, 0.72)\n        for ent, script, uitext in yope3d.view(\"ScriptComponent\", \"UITexturedBackground\"):\n            script.params_blob = \"crate\"\n        v = yope3d.scene_payload()\n        h = world.get_contact_count()\n\n",
   "target": "KEY_E",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0003.py:17:39:KEY_F4",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0003.py",
   "line": 17,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth003Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 9.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        world.attach_box_mesh(entity, yope3d.Vec3(1.2, 2.18, 2.85))\n        c = yope3d.reg_add(entity, \"SphereForm\")\n        c.radius = 1.6\n        n = yope3d.Vec3(1.72, 0, 1.55).normalize()\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n\n    def update(self, world, entity, dt):\n        yope3d.audio.resume_all()\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.54)\n        for ent, textla, aabbfo in yope3d.view(\"TextLabel3D\", \"AABBForm\"):\n            textla.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_add(entity, \"Name\")\n        hull.value = \"ground\"\n        hull = yope3d.reg_add(entity, \"HingeJointConstraint\")\n",
   "target": "KEY_F4",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0029.py:43:39:KEY_7",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0029.py",
   "line": 43,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth029Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 5.98},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        form = yope3d.reg_get(entity, \"SphereForm\")\n        if form is not None:\n            form.radius = 1.05\n        v = yope3d.input.is_lmb_down()\n        v = yope3d.window.is_cursor_locked()\n        yope3d.settings.set_int(\"ground\", 2)\n        v = yope3d.get_behavior(entity)\n        rot = yope3d.Quat.from_euler(0.5, -0.08)\n        world.attach_cylinder_mesh(entity, 1.27, 1.11)\n        v = yope3d.play_sound(\"audios/impact.ogg\")\n\n    def on_collision_enter(self, world, entity, other, contact):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.43)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.39, 1.01, yope3d.EASE_QUAD_IN)\n        for ent, script in yope3d.view(\"ScriptComponent\"):\n            script.script_class = \"hud_root\"\n        c = yope3d.reg_add(entity, \"UIBackground\")\n        c.b = 3.14\n        c.a = 3.23\n        c.g = 1.17\n        c.r = 1.44\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.98)\n        if yope3d.input.is_key_pressed(yope3d.KEY_SEMICOLON):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        cmp = yope3d.reg_add(entity, \"SphereForm\")\n        cmp.radius = 1\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 0.34\n\n    def on_ui_press(self, world, entity):\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        prop = world.add_kinematic_capsule(1.31, 1.04)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.25)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.32, 2, yope3d.EASE_CUBIC_OUT)\n        body = world.add_ui_textured_background(yope3d.Vec2(1.67, 2.64), yope3d.Vec2(1.4, 3.37), yope3d.Vec4(0.97, 0.19, 0.62, 0.16), \"models/prop.glb\")\n        yope3d.audio.fade_gain(body, 0.1, 2.37)\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.a = 4.71\n        hull.font = \"fonts/inter_ascii.msdf\"\n",
   "target": "KEY_7",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0053.py:13:8:draw_ray",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0053.py",
   "line": 13,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth053Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        h = world.get_island_count()\n        yope3d.",
   "suffix": "        t = yope3d.reg_add(entity, \"UIBackground\")\n        t.g = 3.82\n        t.r = 3.63\n        t.a = 2.92\n        t.b = 4.28\n        world.add_point_joint(entity, entity, yope3d.Vec3(3.87, 1.71, 2.34))\n        world.set_ui_group_opacity(entity, 0.74)\n\n    def init(self, world, entity, params):\n        world.add_point_joint(entity, entity, yope3d.Vec3(2.42, 4.75, 2.72))\n",
   "target": "draw_ray",
   "rest": "(yope3d.Vec3(0.44, 0.19, 1.01), yope3d.Vec3(2.7, 2.97, 2.15))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:4:46:attach_script",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 4,
   "col": 46,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.",
   "suffix": "*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n",
   "target": "attach_script",
   "rest": "(...) to give",
   "freq": 8,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0076.py:35:52:EASE_QUAD_IN",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0076.py",
   "line": 35,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth076Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.b = 3.15\n        cmp.r = 0.05\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.speed = 10.06\n        r = world.get_ui_focus()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.17, 0, 4.46) - origin).normalize())\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.55, 2.56, 0.75)\n        m.target = entity\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 25.37)\n        world.make_kinematic(entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 0.84\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.66, 0.29, yope3d.",
   "suffix": "        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n\n    def on_collision_enter(self, world, entity, other, contact):\n        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.65, 0.93, 0.15)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.61)\n",
   "target": "EASE_QUAD_IN",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0099.py:15:8:observe_collisions",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0099.py",
   "line": 15,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth099Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.07},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 23.76)\n        yope3d.",
   "suffix": "        t = yope3d.reg_add(entity, \"UIText\")\n        t.r = 0.11\n        t.font = \"fonts/inter_ascii.msdf\"\n        t.text = \"Paused\"\n        t.g = 2.49\n        prop = yope3d.spawn(\"textures/tnail.png\")\n        h = world.get_registry()\n        res = world.ui_consumed_click()\n        world.set_ui_focus(entity)\n        hull = yope3d.reg_get(entity, \"ScriptComponent\")\n",
   "target": "observe_collisions",
   "rest": "(4, 3.23)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0076.py:55:39:KEY_F6",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0076.py",
   "line": 55,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth076Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.b = 3.15\n        cmp.r = 0.05\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.speed = 10.06\n        r = world.get_ui_focus()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.17, 0, 4.46) - origin).normalize())\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.55, 2.56, 0.75)\n        m.target = entity\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 25.37)\n        world.make_kinematic(entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 0.84\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.66, 0.29, yope3d.EASE_QUAD_IN)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n\n    def on_collision_enter(self, world, entity, other, contact):\n        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.65, 0.93, 0.15)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.61)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.81)\n        v = yope3d.input.get_mouse_delta()\n        yope3d.camera.set_clip_planes(1.85, 4.3)\n        v = yope3d.input.is_mouse_down(0)\n\n    def init(self, world, entity, params):\n        r = yope3d.to_radians(163.0)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 1.25)\n        world.apply_impulse_at(entity, yope3d.Vec3(18.89, 15.09, 32.6), yope3d.Vec3(0.21, 0.86, 0.26))\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, uitran, hull in yope3d.view(\"UITransform\", \"Hull\"):\n            uitran.offset_x_px = 0.64\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(3.63, 0, 4.85)).length()\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.target = entity\n        cmp.local_anchor_a = yope3d.Vec3(3.66, 2.79, 3.93)\n        cmp.local_twist_axis_a = yope3d.Vec3(0.76, 0.61, 0.48)\n\n",
   "target": "KEY_F6",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:13:51:Vec3",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 13,
   "col": 51,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.",
   "suffix": "    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n",
   "target": "Vec3",
   "rest": "(1, 0.6, 0.1))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0096.py:14:36:KEY_GRAVE_ACCENT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0096.py",
   "line": 14,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth096Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.11},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        for ent, audios, uibutt in yope3d.view(\"AudioSource\", \"UIButton\"):\n            audios.pitch = 0.84\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 12.25, 0))\n        t = yope3d.reg_add(entity, \"CompoundCollider\")\n        t.asset_path = \"textures/tnail.png\"\n        t.loaded = False\n        m = yope3d.reg_add(entity, \"CompoundCollider\")\n        m.loaded = True\n        m.asset_path = \"textures/tnail.png\"\n        hull = yope3d.reg_get(entity, \"LightSource\")\n        if hull is not None:\n            hull.intensity = 6.37\n",
   "target": "KEY_GRAVE_ACCENT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0016.py:37:8:draw_ray",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0016.py",
   "line": 37,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth016Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.61},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        res = world.ui_hit_test(2.82, 4.68)\n        rot = yope3d.Quat.from_euler(-0.29, 0.54)\n        world.make_kinematic(other)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = True\n        v = yope3d.raycast(yope3d.Vec3(1.23, 3.78, 4.87), yope3d.Vec3(3.28, 1.89, 3.01), 0.24)\n        world.detach_physics_body(other)\n        for ent, cylind in yope3d.view(\"CylinderForm\"):\n            cylind.radius = 1.14\n        m = yope3d.reg_add(entity, \"UIText\")\n        m.r = 4.37\n        m.font = \"fonts/inter_ascii.msdf\"\n        m.text = \"Press E to interact\"\n        m.a = 3.67\n        m.g = 0.38\n\n    def on_ui_press(self, world, entity):\n        world.attach_sphere_mesh(entity, 0.95)\n        wheel = world.add_aabb(yope3d.Vec3(0.97, 0.83, 0.53), 48.76, yope3d.Vec3(4.23, 4.26, 2.48))\n        hull = yope3d.reg_add(entity, \"SpringConstraint\")\n        hull.target = entity\n        hull.k = 3.07\n        if yope3d.input.is_key_pressed(yope3d.KEY_D):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.22)\n        yope3d.",
   "suffix": "\n    def init(self, world, entity, params):\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.35\n        c = yope3d.reg_get(entity, \"Material\")\n        if c is not None:\n            c.metallic = 0.23\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.tint_a = 0.26\n",
   "target": "draw_ray",
   "rest": "(yope3d.Vec3(2.05, 1.34, 2.12), yope3d.Vec3(4.26, 0.43, 3.55))",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:135:28:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 135,
   "col": 28,
   "prefix": "It relies on WheelFrictionJoint.driven (WheelSpec.driven): a wheel with\ndriven=False skips its longitudinal row entirely and rolls free, instead of\ntargeting wheel_angular_vel*radius (which for an un-driven wheel is 0, i.e. it\nwould *brake*). Driving all four wheels was the earlier workaround for that\nbrake behavior before the driven flag existed \u2014 no longer needed.\n\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.",
   "suffix": "                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n",
   "target": "Vec3",
   "rest": "(0.0, 0.68, 0.55),",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/physics_gallery.py:319:42:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/physics_gallery.py",
   "line": 319,
   "col": 42,
   "prefix": "        # launched across the level.\n        self.plane_floor(world)\n        self.static_box(world, (0, 3, 0), (2.0, 2.0, 2.0), BOX_PURPLE)\n        self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def sa_fast(self, world):\n        self.plane_floor(world)\n        self.static_box(world, (6, 3, 0), (0.2, 3.0, 3.0), PROP)\n        self.sphere(world, 1.0, R, (-10, 3, 0), BALL_FAST, vel=(30.0, 0, 0))\n        self.set_camera((0.0, 5.0, 18.0), pitch=-0.1)\n\n    # ---- Springs ---------------------------------------------------------\n\n    def sp_two_free(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-3, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (3, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 2.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    def sp_anchor(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (0, 8, 0), FIXED, fixed=True)\n        b = self.sphere(world, 1.0, R, (0, 3, 0), BALL)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_compressed(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-0.5, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (0.5, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 12.0), pitch=-0.1)\n\n    def sp_stretched(self, world):\n        self.plane_floor(world)\n        a = self.sphere(world, 1.0, R, (-4, 5, 0), BALL)\n        b = self.sphere(world, 1.0, R, (4, 5, 0), BALL_HOT)\n        self.spring(world, a, b, 5.0, 3.0)\n        self.set_camera((0.0, 5.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs floor ---------------------------------------------------\n\n    def a_drop(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def a_drop_fast(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 50, 0), CUBE_FAST)\n        self.set_camera((0.0, 14.0, 32.0), pitch=-0.2)\n\n    def a_rest(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (0, 0.5, 0), CUBE)\n        self.set_camera((0.0, 2.5, 8.0), pitch=-0.15)\n\n    def a_ramp(self, world):\n        self.ramp(world)\n        self.box(world, H, 1.0, (-4, 8, 0), CUBE)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- AABB vs floating panel -----------------------------------------\n\n    def ap_center(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 10, 0), CUBE)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_miss(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (4.0, 10, 0), CUBE_MISS)\n        self.set_camera((0.0, 6.0, 16.0), pitch=-0.1)\n\n    def ap_rest(self, world):\n        self.plane_floor(world)\n        self.panel(world, 4.0, 3.0)\n        self.box(world, H, 1.0, (0, 4.5, 0), CUBE)\n        self.set_camera((0.0, 6.0, 14.0), pitch=-0.1)\n\n    # ---- AABB vs AABB ----------------------------------------------------\n\n    def aa_static(self, world):\n        self.aabb_floor(world)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_dynamic(self, world):\n        self.plane_floor(world)\n        self.box(world, H, 1.0, (-5, 3, 0), CUBE, vel=(4.0, 0, 0))\n        self.box(world, H, 1.0, (5, 3, 0), BALL_HOT, vel=(-4.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def aa_stack(self, world):\n        self.plane_floor(world)\n        self.box(world, (1.0, 0.5, 1.0), 2.0, (0, 0.5, 0), FIXED)\n        self.box(world, H, 1.0, (0, 8, 0), CUBE)\n        self.set_camera((0.0, 4.0, 12.0), pitch=-0.1)\n\n    # ---- Comparison ------------------------------------------------------\n\n    def s_ramp(self, world):\n        self.ramp(world)\n        self.sphere(world, 1.0, R, (-4, 8, 0), BALL)\n        self.set_camera((0.0, 5.0, 16.0), pitch=-0.1)\n\n    # ---- OBB -------------------------------------------------------------\n\n    def o_drop(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(20.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_vs_sphere(self, world):\n        self.plane_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.",
   "suffix": "        self.obb(world, (0.6, 0.6, 0.6), 2.0, (0, 3, 0), OBB_A, rot=rot)\n        self.sphere(world, 1.0, R, (-6, 3, 0), BALL, vel=(6.0, 0, 0))\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n    def o_on_static(self, world):\n        self.aabb_floor(world)\n        rot = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 0, 1), math.radians(15.0))\n        self.obb(world, H, 1.0, (0, 8, 0), OBB_A, rot=rot)\n        self.set_camera((0.0, 4.0, 14.0), pitch=-0.1)\n\n",
   "target": "Vec3",
   "rest": "(0, 1, 0), math.radians(30.0))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0001.py:19:39:KEY_F10",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0001.py",
   "line": 19,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth001Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        cmp = yope3d.reg_add(entity, \"Transform\")\n        cmp.scale = yope3d.Vec3(1.42, 1.83, 2)\n        cmp.position = yope3d.Vec3(0.63, 0.34, 0.79)\n        cmp.rotation = yope3d.Quat.from_euler(0.0, 0.15)\n        wheel = world.add_kinematic_box(yope3d.Vec3(2.68, 0.88, 4.84), yope3d.Vec3(0.09, 0.81, 0.76))\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.font = \"fonts/inter_ascii.msdf\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_anchor_a = yope3d.Vec3(1.14, 3.57, 3.38)\n        t.target = entity\n        t.local_anchor_b = yope3d.Vec3(2.93, 1.9, 1.71)\n        t.local_twist_axis_a = yope3d.Vec3(0.64, 0.45, 0.92)\n",
   "target": "KEY_F10",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0001.py:33:36:KEY_N",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0001.py",
   "line": 33,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth001Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.29},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        cmp = yope3d.reg_add(entity, \"Transform\")\n        cmp.scale = yope3d.Vec3(1.42, 1.83, 2)\n        cmp.position = yope3d.Vec3(0.63, 0.34, 0.79)\n        cmp.rotation = yope3d.Quat.from_euler(0.0, 0.15)\n        wheel = world.add_kinematic_box(yope3d.Vec3(2.68, 0.88, 4.84), yope3d.Vec3(0.09, 0.81, 0.76))\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n        if yope3d.input.is_key_pressed(yope3d.KEY_F10):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.font = \"fonts/inter_ascii.msdf\"\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_anchor_a = yope3d.Vec3(1.14, 3.57, 3.38)\n        t.target = entity\n        t.local_anchor_b = yope3d.Vec3(2.93, 1.9, 1.71)\n        t.local_twist_axis_a = yope3d.Vec3(0.64, 0.45, 0.92)\n        h = world.get_registry()\n        h = world.get_island_count()\n        rot = yope3d.Quat.from_euler(0.68, -0.39)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 1.52, 0))\n        v = yope3d.window.is_cursor_locked()\n\n    def init(self, world, entity, params):\n        world.attach_cylinder_collider(entity, 17.79, 1.99, 0.81, False)\n        for ent, animat, textla in yope3d.view(\"AnimationPlayer\", \"TextLabel3D\"):\n            animat.speed = 3.03\n        n = yope3d.Vec3(0.37, 0, 1.74).normalize()\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.97, 0.95, 0.29)\n",
   "target": "KEY_N",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:19:4:observe_collisions",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 19,
   "col": 4,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.",
   "suffix": "    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n",
   "target": "observe_collisions",
   "rest": "(LAYER_DEBRIS, self.on_debris_hit)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:198:26:Quat",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 198,
   "col": 26,
   "prefix": "        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.camera.set_position(spawn_pos + self._cam_offset)\n        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n        inp = yope3d.input\n\n        throttle = 0.0\n        if inp.is_key_down(yope3d.KEY_W): throttle += 1.0\n        if inp.is_key_down(yope3d.KEY_S): throttle -= 1.0\n\n        # Inverted steer: A turns right, D turns left.\n        steer_input = 0.0\n        if inp.is_key_down(yope3d.KEY_A): steer_input += 1.0\n        if inp.is_key_down(yope3d.KEY_D): steer_input -= 1.0\n\n        target_steer = steer_input * MAX_STEER\n        self.steer_angle += (target_steer - self.steer_angle) * min(1.0, STEER_RATE * dt)\n\n        drive_angvel = throttle * MAX_DRIVE_ANGVEL\n\n        # Front wheels (0,1): steer + drive. Rear wheels (2,3): free-rolling\n        # (driven=False), so nothing to drive \u2014 they just grip. Calling steer/\n        # drive on the fronts every frame also keeps the chassis awake.\n        world.set_wheel_steer(self.wheels[0], self.steer_angle)\n        world.set_wheel_steer(self.wheels[1], self.steer_angle)\n        world.set_wheel_drive(self.wheels[0], drive_angvel)\n        world.set_wheel_drive(self.wheels[1], drive_angvel)\n\n        tf = yope3d.reg_get(self.chassis, \"Transform\")\n        rot = tf.rotation\n\n        # Governor: clamp yaw rate so a sharp turn can't spin the car up faster\n        # than it can stay upright (world-up ~= chassis-up while roughly level).\n        hull = yope3d.reg_get(self.chassis, \"Hull\")\n        omega = hull.omega\n        if omega.y > MAX_YAW_RATE:\n            omega.y = MAX_YAW_RATE\n            hull.omega = omega\n        elif omega.y < -MAX_YAW_RATE:\n            omega.y = -MAX_YAW_RATE\n            hull.omega = omega\n\n        yope3d.camera.set_position(tf.position + self._cam_offset)\n        yope3d.camera.look_at(tf.position)\n\n        # Ride the chassis: rotate each part's body-frame offset by the chassis\n        # orientation. Front wheels additionally carry the live steer angle.\n        for e, offset, is_front in self._wheels_visual:\n            wt = yope3d.reg_get(e, \"Transform\")\n            wt.position = tf.position + rot.rotate(offset)\n            if is_front:\n                steer_q = yope3d.",
   "suffix": "                wt.rotation = rot * (steer_q * self._wheel_align)\n            else:\n                wt.rotation = rot * self._wheel_align\n\n        for e, offset, local_rot in self._decor:\n            dtf = yope3d.reg_get(e, \"Transform\")\n            dtf.position = tf.position + rot.rotate(offset)\n            dtf.rotation = (rot * local_rot) if local_rot is not None else rot\n",
   "target": "Quat",
   "rest": ".from_axis_angle(yope3d.Vec3(0.0, 1.0, 0.0), self.steer_angle)",
   "freq": 14,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0118.py:48:8:clear_collision_observers",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0118.py",
   "line": 48,
   "col": 8,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth118Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 2.96},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_get(entity, \"PointJointConstraint\")\n        if hull is not None:\n            hull.target = entity\n        yope3d.load_scene(\"scenes/level_01.yscene\")\n        v = yope3d.input.get_mouse_delta()\n        v = yope3d.input.is_mouse_down(4)\n        v = yope3d.window.get_width()\n        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.78\n        cmp = yope3d.reg_add(entity, \"ScriptComponent\")\n        cmp.params_blob = \"player\"\n        cmp.script_class = \"player\"\n\n    def init(self, world, entity, params):\n        for ent, parent in yope3d.view(\"Parent\"):\n            parent.parent = entity\n        for ent, uitext in yope3d.view(\"UIText\"):\n            uitext.g = 2.64\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.87)\n        world.fix_entity(entity)\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(0.21, 0, 2.86) - origin).normalize())\n\n    def on_collision_enter(self, world, entity, other, contact):\n        c = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        c.local_axis_b = yope3d.Vec3(0.11, 2.38, 1.85)\n        c.target = entity\n        c.local_anchor_b = yope3d.Vec3(0.39, 3.21, 3.44)\n        m = yope3d.reg_get(entity, \"Parent\")\n        if m is not None:\n            m.parent = entity\n        hull = yope3d.reg_get(entity, \"AudioSource\")\n        if hull is not None:\n            hull.loop = True\n        for ent, name, uitran in yope3d.view(\"Name\", \"UITransform\"):\n            name.value = \"player\"\n        yope3d.",
   "suffix": "        rot = yope3d.Quat.from_euler(1.22, -0.33)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.half_height = 0.65\n        t = yope3d.reg_add(entity, \"AABBForm\")\n        t.extent = yope3d.Vec3(0.45, 0.77, 0.21)\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_key_down(yope3d.KEY_F):\n            world.apply_impulse(entity, yope3d.Vec3(0, 32.21, 0))\n        for ent, uitext, uiback in yope3d.view(\"UIText\", \"UIBackground\"):\n",
   "target": "clear_collision_observers",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "scripts/behaviors/vehicle_demo.py:140:8:camera",
   "stratum": "real",
   "origin": "scripts/behaviors/vehicle_demo.py",
   "line": 140,
   "col": 8,
   "prefix": "\nSTEERING IS NOT A DIRECT TORQUE. set_wheel_steer only rotates the front\nWheelFrictionJoints' longitudinal direction by steer_angle about the wheel\nup-axis (precomputeWheelFriction, ColliderDiscrete.cpp). Drive + lateral-grip\nimpulses are then applied at each wheel's ground contact point, offset from the\nchassis COM, so `angularImpulse += rChassis x imp` yaws the car \u2014 real\ntyre-force steering. Because those contacts sit well BELOW the COM (a long roll\nlever arm) sharp cornering rolls the body hard and, unchecked, flips it. A car\ntips when lateral accel exceeds (half_track / com_height) * g \u2014 a purely\nGEOMETRIC threshold, independent of mass (both the cornering force and gravity\nscale with mass, so it cancels; dropping mass does not help, and with these\nsprings would raise the car and make it worse). We stay under that threshold\ninstead of fighting it after the fact: a wide track (WHEEL_TRACK_X) and low\nride height (REST_LENGTH) push the tip point to ~1.9 g, and MU_LAT is held at\n1.0 g so the tyres SLIDE (understeer) at the limit rather than gripping hard\nenough to trip. Stiffer springs (SUSP_STIFFNESS) curb body roll, a modest top\nspeed (MAX_DRIVE_ANGVEL) limits v*yaw lateral load, and the chassis yaw rate is\nclamped to MAX_YAW_RATE each frame as a final governor.\n\nVISUALS: chassis OBB = lower body (box 1); a cabin (box 2) and a leaned\nwindshield panel at the front/+Z (box 3) are collider-less meshes riding the\nchassis so heading is legible. The 4 wheels are collider-less CYLINDERS (axis\nrotated to lateral); the front pair also carries the live steer angle so you\ncan see them turn. Nothing here is simulated \u2014 the wheels aren't animated with\nreal suspension travel, and body/wheels are cosmetic. Everything stays\ncollider-less (detach_physics_body strips the Hull + Form that add_obb/\nadd_cylinder create): the wheels sit on the suspension-raycast contact points,\nso as real colliders they'd hijack the raycasts and collide with the chassis \u2014\nthe original \"drives off then won't steer\" bug.\n\nparamsBlob keys (all optional):\n  spawn_y float 2.0 \u2014 chassis spawn height (m)\n\"\"\"\nimport math\nimport yope3d\n\nMAX_STEER = 0.5            # radians (~28 deg)\nMAX_DRIVE_ANGVEL = 28.0    # rad/s (top speed ~= this * WHEEL_RADIUS m/s)\nSTEER_RATE = 8.0           # 1/s, how fast steer_angle chases the input\nMAX_YAW_RATE = 1.3         # rad/s \u2014 chassis yaw is clamped to this each frame\nSUSP_STIFFNESS = 70000.0   # N/m per wheel \u2014 also sets roll stiffness (see docstring)\nSUSP_DAMPING = 7000.0      # N/(m/s) per wheel\n\n# Anti-rollover tuning \u2014 the tip threshold is (half_track / com_height) * g and\n# is INDEPENDENT of mass. We keep MU_LAT below that threshold so the car slides\n# (understeers) at the limit instead of tripping over its tyres. See docstring.\nWHEEL_TRACK_X = 1.15       # half-track (m) \u2014 wider = harder to tip\nREST_LENGTH = 0.30         # suspension rest length (m) \u2014 shorter = lower COM\nMU_LAT = 1.0               # lateral grip: 1.0 g, under the ~1.9 g tip threshold\nMU_LONG = 1.3              # longitudinal grip (drive/brake) can stay high\n\nWHEEL_RADIUS = 0.35\nWHEEL_HALF_WIDTH = 0.11\nWHEEL_VISUAL_Y = -0.27     # cylinder centre height (chassis-local) so it sits on the ground\n\n\nclass VehicleDemo:\n    PARAMS = {\n        \"spawn_y\": {\"type\": \"float\", \"default\": 2.0, \"label\": \"Spawn Height (m)\"},\n    }\n\n    def init(self, world, entity, params):\n        spawn_y = params.get(\"spawn_y\", 2.0)\n        chassis_half = yope3d.Vec3(1.0, 0.4, 2.0)\n        spawn_pos = yope3d.Vec3(0.0, spawn_y, 0.0)\n\n        self.chassis = world.add_obb(chassis_half, 800.0, spawn_pos)\n        world.attach_box_mesh(self.chassis, chassis_half, 0.75, 0.13, 0.13)  # lower body\n\n        # Wheel mounts, chassis-local \u2014 front is +Z, rear is -Z. Indices 0,1 are\n        # the front (steered + driven) pair; 2,3 the rear (free-rolling) pair.\n        self._wheel_positions = [\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35,  1.7),   # 0: front-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35,  1.7),   # 1: front-right\n            yope3d.Vec3(-WHEEL_TRACK_X, -0.35, -1.7),   # 2: rear-left\n            yope3d.Vec3( WHEEL_TRACK_X, -0.35, -1.7),   # 3: rear-right\n        ]\n        wheels = []\n        for i, pos in enumerate(self._wheel_positions):\n            spec = yope3d.WheelSpec()\n            spec.local_pos   = pos\n            spec.rest_length = REST_LENGTH\n            spec.max_travel  = 0.3\n            spec.stiffness   = SUSP_STIFFNESS\n            spec.damping     = SUSP_DAMPING\n            spec.radius      = WHEEL_RADIUS\n            spec.mu_long     = MU_LONG\n            spec.mu_lat      = MU_LAT\n            spec.driven      = (i < 2)   # FWD: only the front wheels drive\n            wheels.append(spec)\n\n        self.wheels = world.add_vehicle(self.chassis, wheels)\n        self.steer_angle = 0.0\n\n        # Collider-less cylinder wheels. add_cylinder's mesh axis is +Y, so this\n        # quat rotates it to the wheel's lateral spin axis; the front pair also\n        # gets the live steer angle composed on top in update().\n        self._wheel_align = yope3d.Quat.from_axis_angle(yope3d.Vec3(0.0, 0.0, 1.0), math.pi * 0.5)\n        self._wheels_visual = []   # (entity, chassis-local offset, is_front)\n        for i, pos in enumerate(self._wheel_positions):\n            voff = yope3d.Vec3(pos.x, WHEEL_VISUAL_Y, pos.z)   # lifted to sit on the ground\n            e = world.add_cylinder(WHEEL_RADIUS, WHEEL_HALF_WIDTH, 1.0, spawn_pos + voff)\n            world.attach_cylinder_mesh(e, WHEEL_RADIUS, WHEEL_HALF_WIDTH, 0.09, 0.09, 0.10)\n            world.detach_physics_body(e)   # collider off \u2014 decorative only\n            self._wheels_visual.append((e, voff, i < 2))\n\n        # Decorative body \u2014 collider-less boxes riding the chassis (boxes 2 & 3).\n        # Each: (entity, chassis-local offset, chassis-local rotation | None).\n        self._decor = []\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.7, 0.32, 0.8),      # cabin greenhouse\n                            yope3d.Vec3(0.0, 0.72, -0.25),\n                            0.85, 0.86, 0.90, None)\n        self._add_decor_box(world, spawn_pos,\n                            yope3d.Vec3(0.62, 0.30, 0.05),    # windshield panel (front, +Z)\n                            yope3d.Vec3(0.0, 0.68, 0.55),\n                            0.22, 0.42, 0.62,\n                            yope3d.Quat.from_axis_angle(yope3d.Vec3(1.0, 0.0, 0.0), 0.6))\n\n        self._cam_offset = yope3d.Vec3(0.0, 5.0, -9.0)\n        yope3d.",
   "suffix": "        yope3d.camera.look_at(spawn_pos)\n\n    def _add_decor_box(self, world, spawn_pos, half, offset, r, g, b, local_rot):\n        e = world.add_obb(half, 1.0, spawn_pos + offset)\n        world.attach_box_mesh(e, half, r, g, b)\n        world.detach_physics_body(e)   # collider off \u2014 decorative only\n        self._decor.append((e, offset, local_rot))\n        return e\n\n    def update(self, world, entity, dt):\n",
   "target": "camera",
   "rest": ".set_position(spawn_pos + self._cam_offset)",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0044.py:45:52:EASE_QUAD_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0044.py",
   "line": 45,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth044Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        hull = yope3d.reg_get(entity, \"CylinderForm\")\n        if hull is not None:\n            hull.radius = 1.13\n        m = yope3d.reg_add(entity, \"Parent\")\n        m.parent = entity\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 3.45)\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.albedo = (0.89, 2.26, 1.31, 1.31)\n        hull = yope3d.reg_add(entity, \"CapsuleForm\")\n        hull.radius = 0.34\n        hull.half_height = 0.44\n        v = yope3d.input.get_mouse_delta()\n        world.apply_impulse(entity, yope3d.Vec3(1.84, 5.02, 6.42))\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 2.83)\n\n    def init(self, world, entity, params):\n        r = world.get_ui_focus()\n        hull = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if hull is not None:\n            hull.path = \"audios/impact.ogg\"\n        t = yope3d.reg_add(entity, \"SphereForm\")\n        t.radius = 1.3\n        rot = yope3d.Quat.from_euler(1.24, 0.02)\n\n    def on_ui_press(self, world, entity):\n        res = world.get_contact_point_count()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.88)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.72, 0.42, yope3d.",
   "suffix": "        yope3d.set_position(entity, yope3d.Vec3(1.77, 2.24, 2.33))\n        world.apply_impulse(entity, yope3d.Vec3(12.68, 22.66, 10.73))\n        cmp = yope3d.reg_add(entity, \"CompoundCollider\")\n        cmp.asset_path = \"audios/impact.ogg\"\n        cmp.loaded = False\n        v = yope3d.window.get_tab_pauses()\n\n    def on_collision_enter(self, world, entity, other, contact):\n        res = world.ui_consumed_click()\n        m = yope3d.reg_get(entity, \"Name\")\n",
   "target": "EASE_QUAD_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0019.py:48:12:to_radians",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0019.py",
   "line": 48,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth019Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.23},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        cmp = yope3d.reg_add(entity, \"CylinderForm\")\n        cmp.half_height = 0.76\n        cmp.radius = 1.89\n        res = world.get_pair_count()\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.target = entity\n        cmp.local_twist_axis_b = yope3d.Vec3(0.22, 0.22, 0.38)\n        cmp.local_anchor_a = yope3d.Vec3(2.53, 3, 1.83)\n        world.finalize_entity(entity, \"player\")\n        c = yope3d.reg_get(entity, \"SphereForm\")\n        if c is not None:\n            c.radius = 0.34\n        body = world.add_text_label_3d(\"Score: 0\", \"Score: 0\", yope3d.Vec3(3.39, 4.2, 1.52))\n        for ent, hingej, uitran in yope3d.view(\"HingeJointConstraint\", \"UITransform\"):\n            hingej.local_anchor_b = yope3d.Vec3(0.9, 3.15, 3.61)\n        hull = yope3d.reg_get(entity, \"OBBForm\")\n        if hull is not None:\n            hull.extent = yope3d.Vec3(0.49, 0, 0.47)\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"SpringConstraint\")\n        cmp.rest_length = 0.9\n        cmp.target = entity\n        cmp.k = 0.49\n        c = yope3d.reg_add(entity, \"TextLabel3D\")\n        c.a = 4.02\n        c.b = 2.67\n        c.text = \"Paused\"\n        world.set_mesh_color(entity, 4.81, 3.79, 4)\n        v = yope3d.settings.get_bool(\"crate\")\n        c = yope3d.reg_get(entity, \"UITexturedBackground\")\n        if c is not None:\n            c.tint_g = 0.31\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.91)\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.occlusion_map = \"textures/crate_ao.png\"\n        r = yope3d.",
   "suffix": "\n    def on_collision_enter(self, world, entity, other, contact):\n        world.set_paused(True)\n        c = yope3d.reg_add(entity, \"LightSource\")\n        c.direction = yope3d.Vec3(0.34, 0.27, 0.21)\n        c.color = yope3d.Vec3(0.9, 3.27, 3.68)\n        c.type = 0.41\n        c.position = yope3d.Vec3(0.54, 0.9, 0.6)\n        c.intensity = 2.76\n        world.remove_entity(other)\n",
   "target": "to_radians",
   "rest": "(5.0)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0097.py:20:52:EASE_CUBIC_IN_OUT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0097.py",
   "line": 20,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth097Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.21},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        rot = yope3d.Quat.from_euler(0.19, 0.32)\n        for ent, uitext in yope3d.view(\"UITexturedBackground\"):\n            uitext.tint_b = 0.03\n        world.add_spring_with_proxies(entity, entity, 5, 0.1, 0, 0.74)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 1.06\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.44)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 1.43, yope3d.",
   "suffix": "        rot = yope3d.Quat.from_euler(-1.03, -0.32)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        t = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        t.local_twist_axis_a = yope3d.Vec3(0.45, 0.15, 0.46)\n        t.target = entity\n        t.local_anchor_a = yope3d.Vec3(4.97, 4.03, 0.09)\n        t.local_anchor_b = yope3d.Vec3(2.91, 0.68, 2.73)\n        for ent, materi in yope3d.view(\"Material\"):\n            materi.metal_rough_map = \"textures/crate_mr.png\"\n",
   "target": "EASE_CUBIC_IN_OUT",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0063.py:49:52:EASE_QUAD_IN",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0063.py",
   "line": 49,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth063Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        rot = yope3d.Quat.from_euler(-0.37, 1.02)\n        v = yope3d.to_radians(3.77)\n        m = yope3d.reg_add(entity, \"UIText\")\n        m.a = 1.53\n        m.b = 3.09\n        m.g = 1.04\n        for ent, animat in yope3d.view(\"AnimationPlayer\"):\n            animat.clip = \"ground\"\n        m = yope3d.reg_get(entity, \"TextLabel3D\")\n        if m is not None:\n            m.r = 1.2\n        v = yope3d.play_music(\"models/prop.glb\")\n\n    def update(self, world, entity, dt):\n        v = yope3d.settings.get_float(\"ground\")\n        c = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if c is not None:\n            c.playing = 0\n        r = yope3d.to_radians(48.0)\n        yope3d.camera.set_fov(47.69)\n        for ent, cylind, script in yope3d.view(\"CylinderForm\", \"ScriptComponent\"):\n            cylind.radius = 1.11\n        m = yope3d.reg_get(entity, \"Parent\")\n        if m is not None:\n            m.parent = entity\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"UIButton\")\n        hull.normal_r = 4.14\n        hull.hover_r = 3.4\n        hull.normal_g = 3.81\n        hull.normal_b = 0.82\n        world.attach_cylinder_collider(entity, 29.53, 1.88, 0.79)\n        c = yope3d.reg_add(entity, \"Parent\")\n        c.parent = entity\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.2)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.44, 0.65, yope3d.",
   "suffix": "        if yope3d.input.is_key_pressed(yope3d.KEY_Y):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        if yope3d.input.is_key_pressed(yope3d.KEY_F3):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        h = world.get_contact_point_count()\n\n    def init(self, world, entity, params):\n        world.detach_physics_body(entity)\n        world.set_ui_parent(entity, entity)\n        c = yope3d.reg_add(entity, \"UITexturedBackground\")\n",
   "target": "EASE_QUAD_IN",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0030.py:20:12:is_sleeping",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0030.py",
   "line": 20,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth030Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 4.43},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 24.09)\n        for ent, script, pointj in yope3d.view(\"ScriptComponent\", \"PointJointConstraint\"):\n            script.script_class = \"crate\"\n        for ent, sphere, cylind in yope3d.view(\"SphereForm\", \"CylinderForm\"):\n            sphere.radius = 1.75\n        target = world.add_kinematic_capsule(0.77, 0.22, yope3d.Vec3(2.18, 0.28, 4.7))\n        v = yope3d.",
   "suffix": "        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.65)\n        world.wake(target)\n        yope3d.set_velocity(target, yope3d.Vec3(0.7, 0.93, 0.35))\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_add(entity, \"CapsuleForm\")\n        m.radius = 1.31\n        m.half_height = 1.03\n        world.apply_impulse(other, yope3d.Vec3(8.76, 17.61, 27.87))\n        anchor = world.add_ui_curved_background(yope3d.Vec2(3.99, 3.35), yope3d.Vec2(2.14, 2.13), yope3d.Vec4(0.91, 4.73, 4.99, 2.29))\n",
   "target": "is_sleeping",
   "rest": "(target)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:26:11:camera",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 26,
   "col": 11,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.",
   "suffix": "    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "camera",
   "rest": ".screen_to_ray(px, py)",
   "freq": 51,
   "tier": "head"
  },
  {
   "id": "scripts/behaviors/attach_script_demo.py:60:42:Vec3",
   "stratum": "real",
   "origin": "scripts/behaviors/attach_script_demo.py",
   "line": 60,
   "col": 42,
   "prefix": "\"\"\"attach_script demo \u2014 give runtime-spawned entities their own live behavior.\n\nAttach AttachScriptDemo to any scripted entity and press Play. Every second it\nspawns a falling sphere and immediately calls yope3d.attach_script(...) to give\n*that specific entity* a live SpawnedCounter instance \u2014 the use case this\nunlocks is \"spawn N things at runtime, each needs independent behavior state\"\n(enemy waves, pickups, procedural placement), which previously required\nhand-authoring every instance in a scene file.\n\nWatch the console:\n  - [attach] spawned <entity> with SpawnedCounter\n  - [counter] <entity> tag=<N> update #<k>   (each instance counts independently)\n  - [counter] <entity> tag=<N> landed after <k> updates   (on_collision_enter fires\n    normally from the very next frame -- no different from a scene-authored script)\n\nExercises: yope3d.attach_script (module, class_name, params -> init() dict),\nindependent per-instance state, get_behavior reaching a runtime-attached instance.\n\"\"\"\nimport yope3d\n\n\nclass SpawnedCounter:\n    \"\"\"The behavior given to each dynamically spawned entity.\"\"\"\n\n    def init(self, world, entity, params):\n        self.tag = params.get(\"tag\", -1)\n        self.count = 0\n        print(f\"[counter] {entity} tag={self.tag} init\")\n\n    def update(self, world, entity, dt):\n        self.count += 1\n\n    def on_collision_enter(self, world, entity, other):\n        print(f\"[counter] {entity} tag={self.tag} landed after {self.count} updates\")\n\n\nclass AttachScriptDemo:\n    PARAMS = {\n        \"spawn_interval\": {\"type\": \"float\", \"default\": 1.0, \"label\": \"Seconds between spawns\"},\n    }\n\n    def init(self, world, entity, params):\n        self.t = 0.0\n        self.interval = params.get(\"spawn_interval\", 1.0)\n        self.spawned = 0\n        self.pos = yope3d.get_position(entity) or yope3d.Vec3(0, 5, 0)\n\n        floor = world.add_static_aabb(\n            yope3d.Vec3(self.pos.x, -0.5, self.pos.z), yope3d.Vec3(4, 0.5, 4))\n        world.attach_box_mesh(floor, yope3d.Vec3(4, 0.5, 4), 0.5, 0.5, 0.55)\n\n    def update(self, world, entity, dt):\n        self.t += dt\n        if self.t < self.interval:\n            return\n        self.t = 0.0\n        self.spawned += 1\n\n        x = self.pos.x + (self.spawned % 3 - 1) * 1.5\n        proj = world.add_sphere(1.0, 0.3, yope3d.",
   "suffix": "        world.attach_sphere_mesh(proj, 0.3, 0.3, 0.6, 1.0)\n\n        ok = yope3d.attach_script(\n            proj, \"behaviors.attach_script_demo\", \"SpawnedCounter\",\n            {\"tag\": self.spawned})\n        print(f\"[attach] spawned {proj} with SpawnedCounter -> {ok}\")\n",
   "target": "Vec3",
   "rest": "(x, self.pos.y, self.pos.z))",
   "freq": 191,
   "tier": "head"
  },
  {
   "id": "typings/yope3d/__init__.pyi:16:8:reg_get",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 16,
   "col": 8,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.",
   "suffix": "    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n",
   "target": "reg_get",
   "rest": "(self.plat, \"Hull\")",
   "freq": 52,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0100.py:22:12:to_degrees",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0100.py",
   "line": 22,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth100Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 7.44},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        res = world.ui_hovered()\n        for ent, conetw, materi in yope3d.view(\"ConeTwistJointConstraint\", \"Material\"):\n            conetw.target = entity\n        for ent, conetw in yope3d.view(\"ConeTwistJointConstraint\"):\n            conetw.local_twist_axis_b = yope3d.Vec3(0.31, 0.79, 0.77)\n        m = yope3d.reg_get(entity, \"ScriptComponent\")\n        if m is not None:\n            m.params_blob = \"hud_root\"\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.58)\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 0.55)\n        v = yope3d.",
   "suffix": "        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(2.17, 0, 1.4)).length()\n\n    def update(self, world, entity, dt):\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.55)\n        t = yope3d.reg_add(entity, \"Transform\")\n        t.position = yope3d.Vec3(0.03, 0.51, 0.9)\n        t.scale = yope3d.Vec3(0.97, 1.39, 0.45)\n        t.rotation = yope3d.Quat()\n        h = world.get_island_count()\n",
   "target": "to_degrees",
   "rest": "(0.82)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:29:31:raycast",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 29,
   "col": 31,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.window.get_cursor_pos()\n    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.",
   "suffix": "    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "raycast",
   "rest": "(o, d, 100.0)",
   "freq": 2,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0081.py:69:12:scene_payload",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0081.py",
   "line": 69,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth081Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 11.5},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        if yope3d.input.is_key_down(yope3d.KEY_9):\n            world.apply_impulse(entity, yope3d.Vec3(0, 39.06, 0))\n        world.attach_capsule_collider(entity, 50.37, 0.35, 1.17, False)\n        cmp = yope3d.reg_add(entity, \"UIBackground\")\n        cmp.g = 2.51\n        cmp.a = 3.63\n        cmp.r = 1.57\n        cmp.b = 3.04\n        yope3d.draw_line(yope3d.Vec3(3.94, 3.48, 0.9), yope3d.Vec3(0.62, 2.72, 3.77))\n\n    def on_ui_press(self, world, entity):\n        v = yope3d.reg_valid(entity)\n        m = yope3d.reg_add(entity, \"UIBackground\")\n        m.a = 0.13\n        m.g = 0.53\n        c = yope3d.reg_get(entity, \"ConeTwistJointConstraint\")\n        if c is not None:\n            c.local_anchor_b = yope3d.Vec3(1.57, 2.99, 4.68)\n        prop = yope3d.spawn(\"textures/tnail.png\")\n        v = yope3d.view()\n        yope3d.draw_ray(yope3d.Vec3(0.88, 1.5, 1.95), yope3d.Vec3(1.21, 0.04, 1.54))\n        t = yope3d.reg_add(entity, \"AudioSource\")\n        t.pitch = 1.27\n        t.loop = True\n        t.bus = 0\n        t.path = \"audios/impact.ogg\"\n        t.autoplay = False\n        world.add_spring(entity, entity, 1.87, 0.54)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        hull = yope3d.reg_get(entity, \"Name\")\n        if hull is not None:\n            hull.value = \"ground\"\n        v = yope3d.input.get_mouse_delta()\n        v = yope3d.look_at(yope3d.Vec3(4.58, 3.56, 0.71))\n        v = yope3d.settings.get_float(\"hud_root\")\n        target = world.add_point_light(yope3d.Vec3(2.1, 4.56, 3.9), yope3d.Vec3(3.21, 3.57, 2.65), 7.75)\n        v = yope3d.reg_has(target, \"CylinderForm\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.41, 0, 4.61) - origin).normalize())\n\n    def update(self, world, entity, dt):\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.normal_r = 4.23\n        c.normal_g = 2.44\n        c.hover_g = 3.56\n        c.hover_r = 1.7\n        c = yope3d.reg_add(entity, \"Transform\")\n        c.rotation = yope3d.Quat.from_euler(0.0, 0.47)\n        c.scale = yope3d.Vec3(1.86, 0.49, 0.34)\n        c.position = yope3d.Vec3(0.53, 0.14, 0.85)\n        c = yope3d.reg_get(entity, \"Transform\")\n        if c is not None:\n            c.rotation = yope3d.Quat()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.28)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.56, 2.79, yope3d.EASE_LINEAR)\n        v = yope3d.",
   "suffix": "        c = yope3d.reg_add(entity, \"AnimationPlayer\")\n        c.speed = 9.46\n        c.clip = \"ground\"\n        c.loop = 3\n        form = yope3d.reg_get(entity, \"Material\")\n        if form is not None:\n            form.albedo_map = \"textures/metal_basecolor.png\"\n\n",
   "target": "scene_payload",
   "rest": "()",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:25:13:window",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 25,
   "col": 13,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.clamp(self.pitch - dy * SENS, -1.4, 1.4)\n    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n    h.velocity = yope3d.Vec3(0, math.cos(self.t) * 1.5, 0)\n    yope3d.reg_get(e, \"Hull\").observe_layers = LAYER_DEBRIS\n    yope3d.observe_collisions(LAYER_DEBRIS, self.on_debris_hit)\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 1, 0.3))\n    world.attach_box_mesh(entity, yope3d.Vec3(1, 1, 1), 1.0, 0.85, 0.15)\n    world.add_trigger_sphere(yope3d.Vec3(3, 0, 0), 1.0)\n    world.add_text_label_3d(font, \"spawn!\", yope3d.Vec3(0, 5, 0))\n    yope3d.set_text(self.hud, f\"Score: {self.score}\")\n    px, py = yope3d.",
   "suffix": "    o, d = yope3d.camera.screen_to_ray(px, py)\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0)\n    m = yope3d.reg_add(e, \"Material\")\n    hp = yope3d.get_behavior(other, \"Health\")\n    yope3d.attach_script(e, \"behaviors.enemy\", \"Enemy\", {\"hp\": 30})\n    e = yope3d.spawn(\"prefabs/enemy.ytemplated\", spawn_point.position)\n",
   "target": "window",
   "rest": ".get_cursor_pos()",
   "freq": 9,
   "tier": "head"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0076.py:27:38:MOUSE_RIGHT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0076.py",
   "line": 27,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth076Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.b = 3.15\n        cmp.r = 0.05\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.speed = 10.06\n        r = world.get_ui_focus()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.17, 0, 4.46) - origin).normalize())\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.55, 2.56, 0.75)\n        m.target = entity\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 25.37)\n        world.make_kinematic(entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 0.84\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.66, 0.29, yope3d.EASE_QUAD_IN)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n",
   "target": "MOUSE_RIGHT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0086.py:30:38:MOUSE_RIGHT",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0086.py",
   "line": 30,
   "col": 38,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth086Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 1.37},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(2.15, 0, 1.09) - origin).normalize())\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.time = 0.74\n        m = yope3d.reg_get(entity, \"AudioSource\")\n        if m is not None:\n            m.loop = False\n        world.set_mesh_visible(entity, False)\n\n    def init(self, world, entity, params):\n        r = yope3d.to_radians(58.0)\n        hull = yope3d.reg_get(entity, \"UIButton\")\n        if hull is not None:\n            hull.hover_g = 1.42\n        m = yope3d.reg_get(entity, \"SphereForm\")\n        if m is not None:\n            m.radius = 1.02\n        if yope3d.input.is_mouse_down(yope3d.",
   "suffix": "            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 27.45)\n\n    def on_ui_press(self, world, entity):\n        r = yope3d.to_radians(139.0)\n        m = yope3d.reg_add(entity, \"LightSource\")\n        m.constant = 0.32\n        m.direction = yope3d.Vec3(0.26, 0.18, 0.66)\n        world.add_spring_with_proxies(entity, entity, 4.36, 0.36, 4, 1.83)\n        v = yope3d.clamp(2.23, 4.6, 3.13)\n",
   "target": "MOUSE_RIGHT",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0104.py:21:36:KEY_F8",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0104.py",
   "line": 21,
   "col": 36,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth104Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.08},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def init(self, world, entity, params):\n        yope3d.scene_manager.load_scene(\"scenes/menu.yscene\")\n        world.tween_ui_opacity(entity, 0.78, 0.41, 4)\n        cmp = yope3d.reg_add(entity, \"OBBForm\")\n        cmp.extent = yope3d.Vec3(0.42, 0.71, 0.02)\n        world.add_spring(entity, entity, 4.93, 0.16)\n        n = yope3d.Vec3(1.5, 0, 2.02).normalize()\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.23)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.7, 2.48, yope3d.EASE_QUAD_IN)\n        if yope3d.input.is_key_down(yope3d.",
   "suffix": "            world.apply_impulse(entity, yope3d.Vec3(0, 22.26, 0))\n        yope3d.set_position(entity, yope3d.Vec3(4.49, 4.35, 4.47))\n\n    def on_ui_press(self, world, entity):\n        rot = yope3d.Quat.from_euler(0.38, 1.39)\n        anchor = world.add_cylinder(0.26, 0.21, 76.82)\n        for ent, conetw, capsul in yope3d.view(\"ConeTwistJointConstraint\", \"CapsuleForm\"):\n            conetw.swing_limit = 1.14\n        target = world.add_text_label_3d(\"Paused\", \"Press E to interact\", yope3d.Vec3(2.56, 2.54, 1.78))\n        n = yope3d.Vec3(1.86, 0, 3.12).normalize()\n",
   "target": "KEY_F8",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0076.py:52:12:to_radians",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0076.py",
   "line": 52,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth076Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.75},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_ui_press(self, world, entity):\n        cmp = yope3d.reg_add(entity, \"TextLabel3D\")\n        cmp.b = 3.15\n        cmp.r = 0.05\n        cmp.font = \"fonts/inter_ascii.msdf\"\n        hull = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if hull is not None:\n            hull.speed = 10.06\n        r = world.get_ui_focus()\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(1.17, 0, 4.46) - origin).normalize())\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(4.55, 2.56, 0.75)\n        m.target = entity\n\n    def update(self, world, entity, dt):\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_RIGHT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 25.37)\n        world.make_kinematic(entity)\n        for ent, capsul in yope3d.view(\"CapsuleForm\"):\n            capsul.radius = 0.84\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.73)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.66, 0.29, yope3d.EASE_QUAD_IN)\n        for ent, compou in yope3d.view(\"CompoundCollider\"):\n            compou.loaded = False\n\n    def on_collision_enter(self, world, entity, other, contact):\n        form = yope3d.reg_get(entity, \"AABBForm\")\n        if form is not None:\n            form.extent = yope3d.Vec3(0.65, 0.93, 0.15)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 26.61)\n        yope3d.audio.set_bus_gain(yope3d.BUS_VOICE, 0.81)\n        v = yope3d.input.get_mouse_delta()\n        yope3d.camera.set_clip_planes(1.85, 4.3)\n        v = yope3d.input.is_mouse_down(0)\n\n    def init(self, world, entity, params):\n        r = yope3d.",
   "suffix": "        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), 1.25)\n        world.apply_impulse_at(entity, yope3d.Vec3(18.89, 15.09, 32.6), yope3d.Vec3(0.21, 0.86, 0.26))\n        if yope3d.input.is_key_pressed(yope3d.KEY_F6):\n            yope3d.play_sound(\"audios/impact.ogg\")\n        for ent, uitran, hull in yope3d.view(\"UITransform\", \"Hull\"):\n            uitran.offset_x_px = 0.64\n        p = yope3d.get_position(entity)\n        d = (p - yope3d.Vec3(3.63, 0, 4.85)).length()\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.target = entity\n",
   "target": "to_radians",
   "rest": "(163.0)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0075.py:41:12:play_sound",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0075.py",
   "line": 41,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth075Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 6.14},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def update(self, world, entity, dt):\n        m = yope3d.reg_add(entity, \"TextLabel3D\")\n        m.font = \"fonts/inter_ascii.msdf\"\n        m.a = 1.68\n        m.r = 4.59\n        m.text = \"Score: 0\"\n        v = yope3d.get_position(entity)\n        c = yope3d.reg_add(entity, \"UIButton\")\n        c.hover_g = 3.23\n        c.normal_r = 1.27\n        c.normal_b = 1.63\n        for ent, textla in yope3d.view(\"TextLabel3D\"):\n            textla.billboard = 3\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"UIText\")\n        hull.text = \"Press E to interact\"\n        hull.font = \"fonts/inter_ascii.msdf\"\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.61)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.direction = yope3d.Vec3(0.09, 0.27, 0.51)\n        t.type = 0.26\n        c = yope3d.reg_get(entity, \"AABBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.52, 0.14, 0.33)\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.74)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.89, 2.94, yope3d.EASE_CUBIC_IN)\n\n    def on_collision_enter(self, world, entity, other, contact):\n        v = yope3d.",
   "suffix": "        cmp = yope3d.reg_add(entity, \"UITexturedBackground\")\n        cmp.tint_a = 0.36\n        cmp.has_texture = False\n        cmp.path = \"audios/impact.ogg\"\n        cmp.tint_b = 0.08\n        cmp.tint_g = 0.03\n        v = yope3d.audio.load_sound(\"textures/tnail.png\")\n        origin = yope3d.get_position(entity)\n        q = yope3d.look_at((yope3d.Vec3(4.37, 0, 4.17) - origin).normalize())\n\n",
   "target": "play_sound",
   "rest": "(\"models/prop.glb\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0007.py:54:52:EASE_LINEAR",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0007.py",
   "line": 54,
   "col": 52,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth007Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.72},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        world.attach_capsule_collider(entity, 88.02, 1.06, 0.25)\n        v = yope3d.lerp(3.4, 2.19, 0.96)\n        yope3d.camera.set_rotation(yope3d.Vec3(0.59, 0.17, 1.51))\n        yope3d.audio.set_bus_gain(yope3d.BUS_MUSIC, 0.57)\n        yope3d.camera.set_position(yope3d.Vec3(1.73, 1.1, 3.42))\n        yope3d.audio.stop_all()\n        wheel = world.add_ui_curved_background(yope3d.Vec2(0.82, 1.56), yope3d.Vec2(3.06, 0.77), yope3d.Vec4(0.36, 2.83, 1.75, 1.06), 0.96, 3)\n\n    def init(self, world, entity, params):\n        m = yope3d.reg_add(entity, \"AudioSource\")\n        m.path = \"models/prop.glb\"\n        m.autoplay = False\n        m.bus = 0\n        m.loop = False\n        v = yope3d.window.get_escape_closes()\n        n = yope3d.Vec3(1.65, 0, 3.16).normalize()\n        hull = yope3d.reg_add(entity, \"PointJointConstraint\")\n        hull.local_anchor_b = yope3d.Vec3(4.7, 3.07, 0.73)\n        hull.local_anchor_a = yope3d.Vec3(4.18, 2.05, 0.74)\n        r = yope3d.to_radians(134.0)\n        v = yope3d.attach_script(entity, \"ground\", \"crate_01\")\n\n    def on_ui_press(self, world, entity):\n        m = yope3d.reg_add(entity, \"PointJointConstraint\")\n        m.local_anchor_b = yope3d.Vec3(3.77, 0.61, 4.55)\n        m.target = entity\n        m.local_anchor_a = yope3d.Vec3(1.02, 1.2, 3.83)\n        m = yope3d.reg_add(entity, \"Parent\")\n        m.parent = entity\n        spin = yope3d.Quat.from_axis_angle(yope3d.Vec3(0, 1, 0), -0.97)\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_MIDDLE):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 13.34)\n        t = yope3d.reg_add(entity, \"LightSource\")\n        t.position = yope3d.Vec3(0.63, 0.87, 0.16)\n        t.intensity = 4.08\n        world.remove_spring_between(entity, entity)\n        world.finalize_entity(entity, \"crate_01\")\n\n    def update(self, world, entity, dt):\n        src = yope3d.play_sound(\"audios/impact.ogg\", None, 0.86)\n        if src is not None:\n            yope3d.audio.fade_gain(src, 0.68, 0.72, yope3d.",
   "suffix": "        for ent, sphere in yope3d.view(\"SphereForm\"):\n            sphere.radius = 1.99\n        if yope3d.input.is_mouse_down(yope3d.MOUSE_LEFT):\n            hit, ent, point, normal, dist = yope3d.raycast(\n                yope3d.get_position(entity), yope3d.Vec3(0, 0, -1), 7.36)\n        e = world.add_trigger_sphere(yope3d.Vec3(3.02, 1.3, 0.73), 0.64)\n\n",
   "target": "EASE_LINEAR",
   "rest": ")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "typings/yope3d/__init__.pyi:6:18:clamp",
   "stratum": "stub",
   "origin": "typings/yope3d/__init__.pyi",
   "line": 6,
   "col": 18,
   "prefix": "import yope3d\n\n\ndef usage(world, entity, dt, inp, self, move):\n    inp  = yope3d.input\n    self.pitch  = yope3d.",
   "suffix": "    fwd, right = yope3d.Vec3(-sy, 0, -cy), yope3d.Vec3(cy, 0, -sy)\n    pos = yope3d.camera.position + yope3d.camera.get_forward() * 3.0\n    o, d = yope3d.camera.position, yope3d.camera.get_forward()\n    hit, e, point, normal, t = yope3d.raycast(o, d, 100.0, entity)\n    hp = yope3d.get_behavior(other)         # other entity's live instance\n    yope3d.play_sound(\"sfx/thud.ogg\", volume=min(1.0, contact.impulse / 40.0))\n    yope3d.draw_ray(contact.point, contact.normal, yope3d.Vec3(1, 0.6, 0.1))\n    self.plat = world.add_kinematic_box(yope3d.Vec3(0, 1, 0), yope3d.Vec3(2, 0.2, 2))\n    world.attach_box_mesh(self.plat, yope3d.Vec3(2, 0.2, 2), 0.6, 0.6, 0.7)\n    h = yope3d.reg_get(self.plat, \"Hull\")\n",
   "target": "clamp",
   "rest": "(self.pitch - dy * SENS, -1.4, 1.4)",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0063.py:23:12:play_music",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0063.py",
   "line": 23,
   "col": 12,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth063Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 10.41},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        rot = yope3d.Quat.from_euler(-0.37, 1.02)\n        v = yope3d.to_radians(3.77)\n        m = yope3d.reg_add(entity, \"UIText\")\n        m.a = 1.53\n        m.b = 3.09\n        m.g = 1.04\n        for ent, animat in yope3d.view(\"AnimationPlayer\"):\n            animat.clip = \"ground\"\n        m = yope3d.reg_get(entity, \"TextLabel3D\")\n        if m is not None:\n            m.r = 1.2\n        v = yope3d.",
   "suffix": "\n    def update(self, world, entity, dt):\n        v = yope3d.settings.get_float(\"ground\")\n        c = yope3d.reg_get(entity, \"AnimationPlayer\")\n        if c is not None:\n            c.playing = 0\n        r = yope3d.to_radians(48.0)\n        yope3d.camera.set_fov(47.69)\n        for ent, cylind, script in yope3d.view(\"CylinderForm\", \"ScriptComponent\"):\n            cylind.radius = 1.11\n",
   "target": "play_music",
   "rest": "(\"models/prop.glb\")",
   "freq": 0,
   "tier": "tail"
  },
  {
   "id": "fim-finetuning/corpus/probe_synth/synth_0061.py:47:39:KEY_TAB",
   "stratum": "synth",
   "origin": "fim-finetuning/corpus/probe_synth/synth_0061.py",
   "line": 47,
   "col": 39,
   "prefix": "\"\"\"Generated API-coverage sample. Not a real behavior \u2014 see synth_pyi.py.\"\"\"\nimport yope3d\n\n\nclass Synth061Behavior:\n    PARAMS = {\n        \"speed\": {\"type\": \"float\", \"default\": 8.85},\n        \"enabled\": {\"type\": \"bool\", \"default\": True},\n    }\n\n    def on_collision_enter(self, world, entity, other, contact):\n        m = yope3d.reg_get(entity, \"CapsuleForm\")\n        if m is not None:\n            m.half_height = 0.48\n        world.remove_light(entity)\n        res = world.get_contact_point_count()\n        for ent, uitext, sphere in yope3d.view(\"UIText\", \"SphereForm\"):\n            uitext.display_px = 4\n\n    def on_ui_press(self, world, entity):\n        hull = yope3d.reg_add(entity, \"TextLabel3D\")\n        hull.r = 4.48\n        hull.font = \"fonts/inter_ascii.msdf\"\n        world.wake(entity)\n        yope3d.audio.set_bus_gain(yope3d.BUS_SFX, 0.87)\n        c = yope3d.reg_get(entity, \"AABBForm\")\n        if c is not None:\n            c.extent = yope3d.Vec3(0.53, 0.97, 1)\n        cmp = yope3d.reg_add(entity, \"ConeTwistJointConstraint\")\n        cmp.local_anchor_a = yope3d.Vec3(2.74, 4.82, 0.49)\n        cmp.target = entity\n        cmp.local_twist_axis_a = yope3d.Vec3(0.16, 0.89, 0.79)\n        hull = yope3d.reg_add(entity, \"CylinderForm\")\n        hull.half_height = 0.57\n        hull.radius = 1.26\n        if yope3d.input.is_key_down(yope3d.KEY_APOSTROPHE):\n            world.apply_impulse(entity, yope3d.Vec3(0, 20.76, 0))\n\n    def init(self, world, entity, params):\n        wheel = world.add_kinematic_capsule(0.7, 1.1)\n        for ent, obbfor, uitext in yope3d.view(\"OBBForm\", \"UIText\"):\n            obbfor.extent = yope3d.Vec3(0.18, 0.91, 0.66)\n        hull = yope3d.reg_get(entity, \"SphereForm\")\n        if hull is not None:\n            hull.radius = 1.82\n        prop = world.add_static_aabb(yope3d.Vec3(2.41, 4.73, 2.71), yope3d.Vec3(0.19, 0.32, 0.3))\n        if yope3d.input.is_key_pressed(yope3d.",
   "suffix": "            yope3d.play_sound(\"audios/impact.ogg\")\n        world.remove_entity(entity)\n        v = yope3d.is_fixed(wheel)\n        v = yope3d.camera.screen_to_ray(4.3, 2.07)\n\n    def update(self, world, entity, dt):\n        t = yope3d.reg_add(entity, \"HingeJointConstraint\")\n        t.limit_enabled = False\n        t.target = entity\n        t.local_axis_a = yope3d.Vec3(2.41, 4.93, 0.64)\n",
   "target": "KEY_TAB",
   "rest": "):",
   "freq": 0,
   "tier": "tail"
  }
 ]
}